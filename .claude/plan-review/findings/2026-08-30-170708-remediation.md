# Plan remediation — accepted 2026-08-30 review findings

Scope: all 47 indexed specification documents after applying the user's accepted corrections and
scope recommendations. This is a remediation verification, not one of the two independent clean
sweeps required to call the plan `stable`.

## Result

All 10 blocking and 10 should-fix findings from
[`2026-08-30-075753-full-tree.md`](2026-08-30-075753-full-tree.md) were addressed. The mechanical
check reports 47 documents, 47 index rows, all 43 requirements claimed, dependency direction clean,
frontmatter/open-question counts consistent, links resolving, and no duplicated document ownership.
The user accepted the recommended defaults; `D-30` therefore closes the remaining 40 implementation-
shaping questions with explicit v1 behavior or evidence gates. The active open-question count is zero.

No known contradiction from that report remains. A fresh semantic sweep is still required before
implementation readiness is declared, especially to independently recompute the revised exchanger
mode and validation examples.

## Blocking finding disposition

| Finding | Disposition |
|---|---|
| F-1 exchanger modes | `D-19` now propagates Duty/Rated/Coupled triggers, flow groups, equations, sizing diagnostics, lowering, glossary, tests, and wire `mode`. |
| F-2 symbol ownership | Core owns `SymbolId`/`SymbolDefinition`; model/metadata carry them; canvas/export are generic consumers. |
| F-3 sensor scope | `D-23` supersedes `D-21`; persistent sensors move post-v1 under `R-36`, while controllers directly reference properties. |
| F-4 edit/run conflict | `D-22` fixes an immutable RunSnapshot. Edits continue independently and never block/cancel/mutate it; any isolation or worker/protocol breach stops the run at its last verified frame. |
| F-5 syntax inventory | Normative result is nine components and six `FS1510`; binder, REST, diagnostics, and log examples agree. |
| F-6 `FS4008` | Live in M2b for both extended exchanger modes. |
| F-7 bare connections | `D-25`: ideal zero-loss links; physical resistance requires an explicit component. |
| F-8 quality/tolerances | `07-quality-attributes` owns measurable performance, resource, validity, and accessibility gates; `62` owns independent fixtures. |
| F-9 pressure/temperature | `D-26`: explicit absolute/gauge pressure boundary and distinct K versus dK/dC syntax. |
| F-10 external properties | M0 package/threading/accuracy spike is a hard milestone gate; reference values are independent IAPWS/ASHRAE data. |

## Should-fix disposition

- `05` maps every `R-` requirement to a milestone or explicit deferral.
- Closed questions were removed where decisions now own the answer; remaining questions stay active.
- This report and state update make review history atomic.
- “v1” now denotes M0–M5; milestone numbers denote delivery steps.
- Glossary covers component hierarchy, run/draft, symbols, pressure semantics, ideal connections, and
  three exchanger modes.
- M3 owns durable local New/Open/Save/Save As/recovery/conflict behavior and SVG/PNG export.
- `18` owns source versioning, catalogue pinning, compatibility, and migration.
- `07` owns the cross-cutting operational envelope and hydronic-only validity claim.
- `D-28` removes misleading fan/duct aliases; humid-air property validation remains without an
  unsupported air-network claim.
- The model's top-level diagnostics collection is the sole wire authority.

## Additional consistency corrections made during remediation

- Backend transient integration uses a dedicated bounded worker; browser delta/layout/render
  preparation uses Web Workers; the UI thread performs only a bounded coalesced DOM commit.
- Manual placement overrides and canvas topology deletion are post-v1 research. v1 placement is
  computed and deterministic; stable ids remain for reconciliation, selection, accessibility, and
  export.
- PI is required and PID is optional; evolutionary sizing, subsystem composition, DXF, and versioned
  model interchange are M6 evidence-gated. XML, DWG, STEP, and IFC are not promised.
- File lifecycle, source compatibility, resource limits, fault injection, accessibility, and static
  export received explicit contracts and acceptance tests.

## Verification

Command: `python3 .claude/plan-review/check.py plan`

Result: `documents 47 · index rows 47 · requirements 43 (43 claimed) · open questions 0` and
`all structural checks pass`.

Next action: run a fresh full semantic review. Only two consecutive clean sweeps may promote documents
from `draft` to `stable`.
