# Plan review remediation — pass 4

**Result:** all four blocking and four should-fix findings from
[`2026-08-31-060829-full-tree.md`](2026-08-31-060829-full-tree.md) have been applied and verified.
This remediation is not an independent clean sweep, so `cleanSweeps` remains zero and no document is
promoted to `stable`.

## Applied corrections

1. **F-1 — pipe discretization.** `nodes=n` now means n thermodynamic nodes, n thermal cells of
   volume V/n, and n+1 hydraulic sub-pipes of length L/(n+1). The declared pipe remains the stable
   group parent and all expanded children are serialized. The `PB nodes=4` examples consistently use
   four 9.6 s thermal cells and five hydraulic sub-pipes.
2. **F-2 — overlap policy.** Supported post-collapse models must reflow deterministically to zero
   overlap. Explicitly over-limit scenes may retain overlap only with `FS5001` and `degraded: true`.
   Milestone, quality, renderer, and test gates now share that boundary.
3. **F-3 — omission semantics.** Absence is unresolved input to the kind registry: normally sizing,
   or a binding visible default where a decision defines one. Stated values remain constraints.
   `D-02`, language/semantic contracts, Core, UI guidance, docs templates, and repository guidance now
   use the same rule.
4. **F-4 — symbol phasing.** The inventory identifies the first required milestone per kind: M3 owns
   kinds delivered through M2b; tank and controller first become required in M4.
5. **F-5 — thermal stages.** The plan now defines the staged graph, collapse precedence, transport and
   heat-transfer edges, classification, SCC handling, longest-path ranking, neutral attachment, and
   deterministic ordering. Cooling-loop, substation, storage-header, and two-conversion goldens are
   owned across Core serialization, canvas placement, export, and testing.
6. **F-6 — decision citations.** Missing active-decision citations were added at their normative
   clauses. `check.py` now verifies every linked plan target in an active decision's `Constrains`
   section cites that decision.
7. **F-7 — requirement references.** LLM navigation now points to `R-28`/`R-29` and reviewer export to
   `R-31`; machine-readable diagnostics remain `R-20`.
8. **F-8 — terminology.** Thermal **pipe cell**, hydraulic **sub-pipe**, **pressure drop**, and
   **lamella** are canonical. Remaining banned spellings occur only in the glossary definitions or a
   rejected decision alternative, which the glossary explicitly permits.

## Verification

`python3 .claude/plan-review/check.py` passes:

> documents 47 · index rows 47 · requirements 45 (45 claimed) · open questions 0  
> all structural checks pass

Focused semantic scans found no active contradiction in discretization cardinality,
omission/default provenance, overlap behavior, symbol milestone ownership, target-user requirement
links, or canonical terminology. The next plan-review activity remains an independent full-tree sweep;
two consecutive clean sweeps are still required for stability promotion.
