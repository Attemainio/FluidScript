---
id: 00-foundation-defects
title: What implementing against the foundation found
tier: 00-foundation
owns: [defect and observation record for documents 01-08]
---

# What implementing against the foundation found

Defects, deferrals and observations from implementing against `01`–`08`. The rule and its reasoning
are in [`08-implementation-sequence`](../08-implementation-sequence.md).

**Next id: `F-28`.** The columns, their vocabularies, and the rule for filing, reopening and closing
are in [`08`](../08-implementation-sequence.md) under *Every package writes down what it found*.

## Open

| # | Filed | Effort | Risk | Basis | Document | What | Why it is still open |
|---|---|---|---|---|---|---|---|
| F-27 | 2026-09-20 | medium | low | measured | [`06`](06-decision-log.md), [`25`](../20-core-domain/25-layout-hints.md), [`28`](../20-core-domain/28-layout-solver.md), [`29`](../20-core-domain/29-layout-ladder.md), [`09`](../09-project-state.md) | **The mechanical plan check has been dead since `08` and `09` moved to the plan root, and the backlog it hid is 38 decisions their constrained documents never cite** | `.claude/plan-review/check.py` is step 1 of the plan-review skill and the only thing that verifies links, counts and decision citations without spending a reviewer. It crashed on its first line of real work — `tier_of` reads the parent folder's number, and `plan/08-…` has none — from the day those two files were added, so every `/plan-review` since has skipped the step it says it starts with, and nobody noticed because a crash and a pass both print nothing the session reads. Repaired 2026-09-20 (root documents rank above every tier; registers checked by their own rules; the decision index checked and generated). What the repaired run still reports, none of it fixed here: (1) **38 decision targets do not cite the decision** that says it constrains them — nine of them in `25-layout-hints`, which `D-103` replaced with `28` without the log's `Constrains` lines following; the rest scatter over `21`–`27`, `31`, `36`, `41`, `43`, `53`, `55`, `59`, `62`, `63`, `07`, `01`, `15`, `17`. Each is either a missing citation or a `Constrains` line that names a document the decision no longer touches; deciding which needs the entry read. (2) `28`, `29`, `28.source` and `25` lack the template's sections, and `28.source` declares an id with a dash where its filename has a dot. (3) `28` declares one open question and lists two. (4) `09` is not a contract document and fails the template by design; the check needs a documented exemption for it rather than three permanent failures. | Every item is a document edit, but 38 of them each need a decision entry read against its target, which is a sweep, not a fix-in-passing. Until it runs clean, `/plan-review` reports on a tree its own first step has not checked. |
| F-19 | 2026-09-03 | medium | low | measured | [`07`](07-quality-attributes.md) | **The property-call cost the whole latency budget rests on is 20 000× what the M0 spike recorded** | The spike measured 0.003 µs per property read; the diagnostics harness measures ~63 µs for one water (T,p) property and ~204 µs for a seven-property `FluidState`. The spike was almost certainly measuring a cached value rather than a flash. Every downstream decision that assumed the small number is now suspect — `C-12`'s per-solve-cache decision most directly, and `07`'s own interactive budget, since a single Newton iteration on a small circuit costs tens of milliseconds in property evaluation alone (`S-2`). This is not a defect in `07`'s targets; it is a defect in the evidence they were set against, and it stays open until the budget is re-derived from measured numbers. **Re-measured 2026-09-14 after `C-76`:** the 63 µs was mostly a native-state clone per call; with one instance updated in place, water (p, T) is **15.9 µs** median and (p, h) **133 µs**, saturation 3.4 µs. Still 5 000× the spike's 0.003 µs, which was a cached read, so the budget still needs re-deriving --- but from these numbers, not the old ones. **Re-measured 2026-09-21, release build** (the user's measuring package): water from `(p, T)` 17.8 µs, from `(p, h)` 143 µs, saturation 3.6 µs, and the debug build within 10 % of each, since the cost is CoolProp's flash and not managed code. A report generated the same day at 13:36 UTC showed `(p, h)` at 705 µs with a standard deviation of 368 µs; it was taken while the full Core test suite ran on the same machine, and the harness now records nothing that was not measured on a quiet one. What the budget rests on is therefore: about 140 µs per node state fixed from `(p, h)`, one per node per residual and one per node column per Jacobian sweep, so a Newton iteration on the 24-declaration header costs 4 ms and the whole two-pass solve 52 ms (`C-68`'s re-measurement). `07`'s only gate a solve touches is transient playback at ten frames a second; deriving that row from these numbers, and stating the steady solve's own expectation beside the compile gates, is what closes this. |

**`F-18`, `F-19` and `F-20` were `F-5`, `F-6` and `F-7`** until `P3.4c`. Three ids each named two
different defects — one still open, one long closed — because the packages that appended to this file
numbered from the Open table alone and did not look below it. Every citation outside this file meant
the closed one, so the open three moved and nothing else had to. Numbers here are now allocated across
both tables, as `D-` numbers already are.

## Traps

What a session working against these documents gets wrong first. Promoted from *Observations*
or from a closed entry when a session hits it a second time; read before working in this tier.

- `.claude/plan-review/check.py` crashed on every run from the day `08` and `09` were added at the plan root (`tier_of` parsed `plan` as a tier number), so nothing mechanical has checked the tree since; the 2026-09-20 repair found the backlog filed as `F-27`.

## Closed

| # | Filed | Closed | Effort | Document | What was wrong | What changed |
|---|---|---|---|---|---|---|
| F-25 | 2026-09-19 | 2026-09-19 | — | [`07`](07-quality-attributes.md), [`21`](../20-core-domain/21-fluid-and-state.md), `D-121` | **The water row's lower pressure edge, 100 kPa absolute, was an accuracy band read as a physical limit, and it sat exactly on every closed circuit's arbitrary zero** | What the row meant: the domain the density oracle had been checked over -- Kell's equation is stated at one atmosphere, and the row said the backend's figures were trusted from there to ten bar. What it did: `Water.ValidRange` refused any state below 100 kPa absolute, which is 0 gauge, which is where a closed circuit's picked datum sits (`D-98`); so a bookkeeping zero decided whether the solve's field was admissible, and any node the physics put below its datum -- the second pump's suction on a ring (`S-29`), the suction behind a header datum (`S-62`) -- ended the run `NonFinite` as if liquid water ceased to exist at 0.99 bar. It does not: IAPWS-IF97 Region 1 is stated from the saturation pressure up at every temperature in the row, and water at 20 °C and 72 kPa absolute is the ordinary contents of a pump's suction line. | **Closed by `D-121`, 2026-09-19.** The edge is the triple-point pressure, 611.657 Pa, where no liquid exists at any temperature; the boiling line (`F-13`) bounds the liquid by phase. The oracle's reach below one atmosphere rests on the same incompressibility that carried it to ten bar, asserted down to 10 kPa absolute in `PropertyAccuracyTests`. Whether a plant *should* be sub-atmospheric became `FS2220`/`FS2221`'s question. `07`'s row and its paragraph, `21`, `Water.ValidRange`. |
| F-24 | 2026-09-14 | 2026-09-14 | — | [`05`](05-milestones-and-acceptance.md), `D-45` | **`M2a` asked for the model-contract payload measurement before the model contract exists** | `05`'s last M2a criterion records the 200-component model's uncompressed bytes and server serialization time against `07`'s 512 KiB budget. The model contract is `P5.1`'s deliverable, under `M3`; in `M2a` there is no serializer to time and no payload to weigh. `D-45` already says the milestone that first produces an artifact is the one that records its baseline, and `M0` deferred this row to `M2a` on the assumption that the contract would land with the solver. It did not: `08` put it with the API. | **Closed in the same change.** The row moved to `M3`'s criteria with its `D-45` provenance; the solver-scale row that `M2a` *can* measure stayed. Filed here rather than left as a silent edit because `05` is the acceptance record and a criterion moving between milestones is the kind of change a reader should be able to find. |
| F-23 | 2026-09-09 | 2026-09-14 | — | [`01`](01-vision-and-scope.md), [`05`](05-milestones-and-acceptance.md) | **The distribution header's stated reason for not fixing `HS1`'s outlet is measurably false, and with the outlet stated the circuit converges --- at the wrong header temperature** | **Closed by `S-58`, and option (a) was right after all.** `out=60` on the header — the cheapest of the three ways out, recorded here as *tried and does not work* — converges in one iteration on `01`'s figures once a junction mixes by mass rather than by average: 0.1914 and 0.2392 kg/s drawn, 0.4307 kg/s through the source. The `IterationCap` at 0.3216 that ruled it out was the same defect: with the mixed temperature insensitive to position the solver had nowhere to go and drove the radiator head to zero looking. The 80 °C result recorded above stands as a measurement — it is the other valid 54 kW design, and `S-53` says so — but the sample now states the temperature `01` specifies, so `01` and `05` need no change and the fixture decision this entry deferred to the user never has to be made. The sample's header comment has been rewritten; it no longer argues from a singularity that does not occur. |
| F-3 | 2026-09-02 | 2026-09-09 | — | [`07`](07-quality-attributes.md) | No latency budget is measured | **Closed as superseded by `F-19`.** It is measured: `diagnostics/pipeline-timings.md` and `fluid-state-timings.md` are regenerated by the diagnostics harness on every run --- parse, bind, lower, count and whole-solve timings per sample, with the machine and build they are bound to --- and `C-68` quotes them for the figure that matters (`m2-cooling-loop` at 1.8 ms on constant properties against 122 ms on water). What this entry actually wanted and did not say is the *second* half: `07`'s budget re-derived from those numbers rather than from the M0 spike. That is `F-19`, word for word, so the two were one entry. |
| F-22 | 2026-09-08 | 2026-09-08 | — | [`01`](01-vision-and-scope.md) | **A diverting valve recirculating into a fixed-duty source cannot change that source's outlet temperature, so the arrangement `F-21` proposes does not regulate anything** | The arrangement is `R1 - N1 - HX1 - PU1 - TV3 - S1` with `TV3 - N1`: the valve sends part of the heated water back to the source's own inlet. The intent is that `TV3` holds the supply temperature the network sees. It cannot. Recycling hot outlet water raises the source's inlet temperature and raises the flow through it by exactly the same amount, so with `Q` and the net draw fixed, `T_out * (m_net + R) = m_net * T_return + R * T_out + Q/cp` reduces to `T_out = T_return + Q/(m_net * cp)` --- **independent of `R`**. On this plant that is `30 + 30 = 60 degC` for every recirculation rate. The energy balance is then satisfied identically along `R`, `TV3.position` is constrained by nothing thermal, and the circuit is degenerate in a way rank counting does not catch because the valve's own Kv law keeps the matrix square. Confirmed by hand and by the solve, which reports the valve laws as its worst residuals at every iteration. | **Closed** by rebuilding the fixture. `m2-distribution-header`'s source now runs an **uncontrolled bypass** -- `N1 - HS1 - N2 - PU_MAIN - N3` with `N3 - PB - BV - N1` -- and `HS1` states no `out`, so its outlet floats and the primary flow is `PU_MAIN`'s own. Stating both the duty and the outlet pins the source flow to the network draw and leaves the bypass carrying exactly zero, which is `S-21`'s singularity, so the floating outlet is required rather than tidier. What this entry ruled out stays ruled out: a diverting valve there regulates nothing. Supply-temperature control by a **mixing** valve is not lost, it is deferred -- it needs a position promoted against a stated setpoint, which is a controller (`plan/30-solver/34`), and the arithmetic that would close it is recorded above. |
| F-21 | 2026-09-08 | 2026-09-08 | — | [`01`](01-vision-and-scope.md) | **The distribution header's plant side has no loop of its own, and that is what makes it unsolvable** | `HS1` and `PU_MAIN` sit directly in the header path, so the source's flow **is** the network's flow and the plant has no freedom left to regulate with. The consequence is not subtle and it is not only physical: the fixture states `HS1 power=54 out=60` *and* both loads' `power` and `out`, and 24 + 30 = 54 exactly --- so the plant's energy balance is already implied by the loads', and the circuit states the same energy fact twice. **Measured**: with the consumers written as real mixing circuits, the in-line plant is **rank deficient** (37 unknowns, 37 equations, one row implied --- `N3` and `N5` mass balances against the energy balances) while the same consumers on a plant that has its own loop are **full rank** at 44 unknowns. Not a solver defect: a plant nobody would build. | **Closed.** The fixture carries the source loop, and the measurement that justified it stands: in-line plant is rank deficient at 37 unknowns, the same consumers on a plant with its own loop are full rank at 44. What changed since this was filed is the *form* of that loop -- an uncontrolled bypass rather than the three-way valve this entry proposed, for the reason in `F-22`. The fixture's inference inventory, tag ordinals and connection list moved with it and `ReferenceCircuitTests` asserts the new shape. It no longer blocks `m2-distribution-header`: what blocks that is `S-38`. |
| F-4 | 2026-09-02 | 2026-09-08 | — | [`03`](03-repository-layout.md) | **The nine-file list is a definition of "done" that no single package can satisfy**, and P3.3 satisfied four rows of it | **Closed.** `03`'s worked example gained a **Supplied by** column, so each row names the package that owes it, and the text now says outright that the list defines a finished *component* rather than a checklist for the package introducing one. The documentation row is marked as the one that never moves. The two conventions this entry surfaced are recorded there too: a test file may cover two kinds where the physics is shared (`ValveAndPumpTests`, whose curve and law are tested against the same regularisation), and a sizing rule may be an `OuterLoop` pass rather than an `ISizer` where choosing what to size against needs sibling branches (`C-61`, `C-63`, `C-66`). |
| F-17 | 2026-09-03 | 2026-09-08 | — | [`01`](01-vision-and-scope.md), [`05`](05-milestones-and-acceptance.md) | **The distribution header's recirculation leg is not connected**, so the fixture cannot produce the flows its own design point assumes | **Closed.** Both recirculation legs are wired -- `TV_AHU.b - NM_AHU` and `TV_RAD.b - NM_RAD` -- so each consumer is a real mixing circuit and `in=50` on a 60 &deg;C header has something to mix with. The reasons this stayed open are all discharged: the inference inventory and tag ordinals moved with the fixture, `ReferenceCircuitTests` was updated, and a solver exists. The one thing still unverified is the *flows* -- the circuit terminates `Singular` on `S-38`, so `01`'s design figures cannot yet be confirmed against a solve. That is `S-38`'s to carry, not this entry's; the wiring defect is fixed and re-filing it here would double-count one blocker. |
| F-20 | 2026-09-03 | 2026-09-08 | — | [`03`](03-repository-layout.md) | **`global.json` was in the repository, load-bearing, and named by no plan document** | **Closed, and it had been fixed for a while.** The concrete half was done when this was filed --  `rollForward` narrowed to `latestPatch`, `global.json` documented in `03` with its trade. It stayed open for "the general form", which the very paragraph written alongside it already stated. `03` now says it as a rule rather than as an example: **a repository's SDK selection is part of its contract with its tooling**, every tool resolves an SDK before it resolves a reference, two that resolve differently share one `ArtifactsPath` and corrupt each other quietly, and any new build-adjacent input gets a subsection there. **An entry that outlives its own fix costs the same attention as a real one** -- this one was re-read several times across sessions before anyone checked whether the text it asked for existed. |
| F-26 | 2026-09-20 | 2026-09-20 | — | [`07`](07-quality-attributes.md) | **(Was a second `F-11`; renumbered 2026-09-20 when the register check found the duplicate.) Two-phase states were listed among the things that produce diagnostics, which would have refused the refrigerant circuit the project needs** | "Unsupported fluids, two-phase states, cavitation, compressible networks, condensation, and extrapolation outside the table produce diagnostics" predates any thought of a refrigeration cycle. An expansion valve discharges into the dome by definition and an evaporator is two-phase over almost its whole length, so the line excluded a plant `01`'s own flagship system contains. | `D-78`. Two-phase and supercritical states are valid, and the validity claim rests on the property backend's own range rather than on a table of ours. The two-phase **pressure drop** correlation stays out of v1, which is a genuinely separate and research-grade thing. `07`'s line now says so. |
| F-18 | 2026-09-03 | 2026-09-03 | — | [`08`](../08-implementation-sequence.md) | **P3.4 is sequenced before P3.5, and lowering cannot build a pipe without the catalogue** | `23`'s lowering step 1 turns every flow-carrying symbol into a component "with its stated parameters converted to SI", which is true of every kind but one: a pipe needs an inside diameter, a script states `dn`, and DN25 is a 27.3 mm bore rather than 25 mm. That mapping is `27`'s and ships in P3.5. `08`'s row for P3.4 gives its reason as "the no-syntax-type-in-`CircuitGraph` assertion goes live here" and does not mention geometry at all. P3.4a worked around it with an injected `IBoreLookup` and a test fixture, which is the right seam for an unrelated reason — P3.7's outer loop re-instantiates components as sizing chooses values, so lowering has to be re-runnable against changing geometry anyway. **Closed by `P3.5`:** `08` should say so, and the packages should not swap. `CatalogBoreLookup` now sits behind the seam, and the seam earned its keep for the second reason rather than the first. Recorded from the other side as `C-24`. |
| F-15 | 2026-09-03 | 2026-09-03 | — | [`01`](01-vision-and-scope.md) | **The simple-loop reference circuit had no steady solution.** `HE1 power=30 in=20 out=50` on a closed adiabatic ring: 30 kW entering and no sink anywhere in the script | Fixed in `P3.4c` by adding `LOAD heat_exchanger power=-30` on the return leg, which is what the user's own reading of it called for. Two things came out of the fix that the original report had wrong. The circuit is **square**, not over-specified — the `FS2210` it produced was `C-30`'s missing enthalpy level, and once that is counted `in=20` is the loop's temperature datum rather than a demand nothing can meet. And what makes it wrong is *consistency*, which no count reaches: `FS2203` now says so directly, and the whole class of "closed circuit with a source and no sink" is caught rather than being caught by accident on this one circuit. |
| F-16 | 2026-09-03 | 2026-09-03 | — | [`01`](01-vision-and-scope.md), [`05`](05-milestones-and-acceptance.md), [`08`](../08-implementation-sequence.md) | **The distribution header stated a return temperature two 30 °C loads cannot produce, and its header flow figure was the wrong quantity** | `HS1 in=40` removed: the return header's temperature is something the circuit produces, and stating it demanded a 40 °C return no arrangement of two mixing valves and two 30 °C loads delivers. The more expensive half was arithmetic that reproduced itself and described the wrong circuit. `01` gave the source flow as 0.6460 kg/s, the sum of the two subcircuit **loop** flows, which is what the source would carry only on a 40/60 header. It runs 30/60, so the source carries 0.4306 kg/s — each subcircuit drawing its duty over the header's 30 K (0.1914 and 0.2392) and circulating it over its exchanger's 20 K. Corrected in `01`, `05`, `08` and `ReferenceNumbers`, where the consistency test now checks the two derivations against each other rather than a sum against itself. `P0.3` reproduced all 71 figures and could not have caught this: the arithmetic was right and the circuit it described was not. |
| F-14 | 2026-09-02 | 2026-09-02 | — | [`07`](07-quality-attributes.md) | **The water row's lower endpoint is not a state.** 0 °C is stated as a bound of the validated domain, and 0 °C is the melting line | The same shape as `F-13` at the other end, and missed by `F-13`'s own fix because that fix reasoned about vapour. On a phase boundary pressure and temperature are one constraint, so the backend refuses the pair — and before this it surfaced as `FS2004` "could not evaluate", which reads like a backend failure rather than an ill-posed question. `Water` now detects the melting line the way it already detected the boiling line and reports `FS2002`; `07` says the endpoints are limits, and [`fluid`](../../docs/functions/fluid.md) tells a user to move 0.01 °C off it. Found by a validation test that took the stated bound at its word. |
| F-13 | 2026-09-02 | 2026-09-02 | — | [`07`](07-quality-attributes.md) | **The water-properties row states a rectangle, and the rectangle contains steam.** 0–120 °C × 100–1000 kPa absolute reads as two independent bounds; they are only independent below the boiling line | Measured: saturation is 99.61 °C at 100 kPa absolute, 120.21 °C at 200 kPa, 179.88 °C at 1000 kPa. At 100 kPa absolute and 110 °C the backend returns `phase=Gas`, ρ = 0.573 kg/m³ — a factor of 1600 from liquid, silently, inside a range the substance calls valid. `07` now says "below the boiling line at that pressure" and `Water` rejects any state whose phase is not liquid, naming the boiling point as the upper bound in `FS2003`. Found by a question about how CoolProp fixes a state, not by reading the row. |
| F-11 | 2026-09-02 | 2026-09-02 | — | [`01`](01-vision-and-scope.md) | **The distribution header could not produce the flows `05` requires of it.** Both subcircuits declared three components and no `connections`, so there was no path from `supply` to `return` in either; and both declared a heat exchanger of kind `duty`, which does not exist | The same stale kind `L-23` fixed in the syntax tour, still here one document away. Kinds corrected, and `PU_AHU - HE_AHU - TV_AHU` added to each branch — open at both ends, which is what [`23`](../20-core-domain/23-topology-and-graph.md)'s lowering rule needs, since `supply` joins the *first unconnected inlet*. Found by transcribing the fixture into `samples/` and binding it, which is the first thing anything had done to it. |
| F-12 | 2026-09-02 | 2026-09-02 | — | [`01`](01-vision-and-scope.md), [`15`](../10-language/15-semantic-model.md) | `FS2107` warned that both ends of both headers were dead ends | `N4` and `N6` carry one written connection each and get their second from the subcircuit that attaches to them. `23` lowers `supply N4` to exactly that edge — one stage after step 10 runs — so the binder was counting a degree the model does not finally have. A node any circuit attaches to is now exempt. The narrower fix, synthesising the connection in the binder, was rejected: `SemanticModel.Connections` is "every connection, in source order", and `FS1507`/`FS1511` are deliberately judged on the connections the **user wrote**. |
| F-5 | 2026-09-02 | 2026-09-02 | — | [`08`](../08-implementation-sequence.md) | The P2.1 row promised a shared stage-result type that nothing wanted | Recorded in `08` itself: what is genuinely shared is `Diagnostic` and the registry behind it. A generic `StageResult<T>` would have renamed `Root` to `Value` for no consumer. |
| F-6 | 2026-09-02 | 2026-09-02 | — | [`08`](../08-implementation-sequence.md) | **The formatter had no work package at all.** `17` owns it and `52` binds it to `Shift+Alt+F`, but `08` scheduled only the printer (P2.5) | Assigned to P5.5, with the editor command that invokes it rather than beside the printer it must not become. Found in P2.5 only because the phase's scope was checked against the owning document. |
| F-7 | 2026-09-02 | 2026-09-02 | — | [`08`](../08-implementation-sequence.md) | Invariant 3 — the unit tier under two seconds — was being violated at 3.1 s, and had been for some packages | Not the tests: 2.4 s of it was `RepositoryLayout` walking the whole repository, `node_modules` included, five times, filtering build output *after* enumerating it. Pruned during the walk and walked once: 1.0 s. |
| F-8 | 2026-09-02 | 2026-09-02 | — | [`06`](06-decision-log.md) | — | Six decisions were needed during P2.2–P2.6 (`D-51`…`D-56`), every one from cross-checking documents *before* writing the code they governed. Not a defect in `06`; recorded because the rate is information. |
| F-9 | 2026-09-02 | 2026-09-02 | — | [`05`](05-milestones-and-acceptance.md), [`01`](01-vision-and-scope.md) | The last four M1 exit criteria and the nine-diagnostic count were unasserted | P2.8 closed all five: `supply`/`return` bind and resolve a `ParentCircuit`, the `control` line binds its four named arguments (and rejects a bare `actuate=TV1` per `D-43`), a malformed line leaves every other statement bound, and `samples/m1-syntax-reference.fluid` produces exactly one `FS1507`, two `FS2107` and six `FS1510` — asserted from the sample file, not from a copy. |
| F-10 | 2026-09-02 | 2026-09-02 | — | [`08`](../08-implementation-sequence.md) | **[`18-script-compatibility`](../10-language/18-script-compatibility.md) had no work package anywhere in the sequence** | The second occurrence of `F-6`'s defect, and found the same way: by checking a milestone's criteria against the packages meant to deliver them. M1's *first* exit criterion needs `FS1701`, and no row mentioned the document. `18` does not fit one package, so it was split — P2.9 ships `Inspect` and the gate, P3.5 owns `FS1703`, P5.9 owns the save-blocking half of `FS1701`, and migration is deferred with a stated trigger (a language major 2) rather than left unscheduled. |

## Observations

**A hand-check reproduces a number; only a hand-check against the circuit reproduces the right one.**
`P0.3` independently reproduced all 71 asserted figures and passed. The distribution header's
0.6460 kg/s was one of them, and it is the wrong quantity for the circuit it describes (`F-16`) — the
arithmetic `0.2871 + 0.3589` is correct, and it is the sum of two flows that do not both pass through
the source. What caught it was the well-posedness count reporting the fixture over-specified, three
work packages later. `08` says P0.3 pays for itself, and it does; what it cannot do is check that a
figure is about the thing the document says it is about.


**The invariants in `08` are only worth what measures them.** F-7 sat unnoticed because nothing
checked invariant 3 at a package boundary; it was found by measuring at one. Invariant 4 (no P2
package references `Core.Fluids`, `.Components` or `.Solvers`) *is* asserted, by an architecture test,
and has never drifted. The difference between those two outcomes is the test.

**`ArtifactsPath` and MSBuild node reuse both need environment setup that no document owns.** Build
output lands in `~/.dotnet-artifacts`, and `MSBUILDDISABLENODEREUSE=1` is set in the Claude Code
settings because leftover worker nodes on WSL hold file locks. Neither belongs in `03`, but a session
that has not seen it will be confused by both.

**Measuring on WSL over `/mnt/c` distorts every timing.** A full-repository directory walk costs about
half a second there and nothing on a native filesystem. Any performance number in this repository
should say which it was measured on.

**The docs gate is enumerated from code, so a phase pays for it the moment it registers anything.**
P2.6 registered eight component kinds and immediately owed eight pages; it also owed thirteen more,
because the reserved-word half of the gate had been left dead since P2.4. A gate wired but returning
an empty collection is indistinguishable from a gate that passes.

**Invariant 3 held through P2.8 without being re-fixed.** The unit tier is 1.9 s over 551 tests after
adding three binder files and thirty-three tests, which says F-7's fix was the enumeration and not the
test count. It is still only measured by running it; see the observation above.

**Two documents have now turned out to be unscheduled, and both were found by the same question.**
`F-6` (the formatter) and `F-10` (script compatibility) were each caught by asking "which package
delivers this milestone criterion?" rather than by reading `08` for completeness. `08`'s package
tables are a good plan and a poor inventory: nothing in the document's own structure notices that a
tier-10 file is named by no row. A check that every `plan/` document is named by at least one work
package would be cheap, and would have found both.

**Adding `D-63` exercised the decision log's own process, and the process held.** `23` introduced flow
groups and assigned their declaration to the component; `22` owns the component contract and never
mentioned them. `06`'s rule — a change that alters a settled decision adds a new entry rather than
editing the old one — made the right call obvious, and the entry's mandatory "rejected alternatives"
section is what surfaced that a registry-data version could not work, because a tank's flow group holds
every materialized port and how many there are comes from the script. That argument did not exist
before the section demanded it.

**The `defects.md` rule was followed for three tiers and silently skipped for a fourth.** `P3.0`
through `P3.4a` appended to `20-core-domain` every time and to `10-language` when the binder was
touched, and never created `30-solver/defects.md` at all — although every one of those packages was
written against `31`'s and `36`'s contracts. The rule in `08` says "every `plan/` tier whose documents
it worked against", and the failure mode is specific: a tier with *no* defects file reads as a tier
nothing has touched, which is exactly the wrong signal. A tier's file should be created empty when the
tier is first read at implementation depth, not when it first has a defect.

**Two text scans over source, one under-detecting and one over-detecting, in the same week.** The
diagnostic-coverage gate had to strip comments, because a code cited in prose would otherwise count as
a code under test. `D-47`'s serializer scan has the opposite problem: a comment *explaining* why Core
may not name a serialization namespace is itself a violation, and `P3.5` hit it on the first try. Both
are floors and both are worth having; what is worth writing down is that a scan over source text
cannot tell a mention from a use, and every such gate should say which way it errs.

**Two of the ten entries closed in this sweep had already been fixed, and one of them said so in its
own resolution column.** `S-30a` read "**Closed by `S-30b`**" and sat in the Open table; `F-20`'s
concrete fix shipped with the paragraph that was written alongside it, and it stayed open for a "general
form" that the same paragraph stated. `C-66` was moved to Open-with-closing-text by an edit earlier the
same day. **An entry that outlives its own fix costs exactly what a real one costs** -- it is re-read
every time the list is scanned, it inflates the count that makes the list feel unmanageable, and it
crowds out the entries that genuinely need a decision. The check is cheap and was not being done: when a
defect is fixed, the fix moves the row, not just the text.

**The foundation tier's remaining two are honestly open and neither is resolvable by argument.** `F-3`
(no latency budget measured) waits on a frontend to measure; `F-19` (the property-call cost is 20 000x
the M0 spike) waits on the budget being re-derived from the measured numbers rather than the spike's. A
tier whose open list is short and *correct* is more useful than one that is short because things were
closed optimistically.
