---
id: 10-language-defects
title: What implementing tier 10 found
tier: 10-language
owns: [defect and observation record for documents 11-18]
---

# What implementing tier 10 found

Every defect, deferral and observation from implementing against `11`–`18`, newest package last. The
rule and its reasoning are in
[`08-implementation-sequence`](../08-implementation-sequence.md).

A closed entry says what changed. This matters more than it looks: a document that has been corrected
reads as though it was always right, so without this file there is no record that
[`15`](15-semantic-model.md)'s ambiguity example was ever wrong, or that `FS1114` exists because
`FS1101` was cited for something it could not cover.

**Next id: `L-64`.** The columns, their vocabularies, and the rule for filing, reopening and closing
are in [`08`](../08-implementation-sequence.md) under *Every package writes down what it found*.

## Open

| # | Effort | Risk | Basis | Document | What | Why it is still open |
|---|---|---|---|---|---|---|
| L-58 | medium | low | hunch | [`13`](13-type-and-unit-system.md) | **Six quantity symbols are reserved with no dimension to carry them** | `D-123` reserved `s`/`entropy`, `ds`, `mu`/`viscosity` (`eta`), `nu`, `k`/`conductivity`, `v`/`velocity` and `re`/`reynolds` in the property table's text so that no registry row spells a property with one of them for something else -- and they cannot be rows yet: entropy shares specific heat's dimension vector (J/(kg·K)) and needs a named dimension of its own, viscosity has no dimension at all, and `velocity`/`re` exist today only as pipe properties spelled in the registry. Making them rows means the `13` dimension list grows by two or three named dimensions, `SolvedPort` carries viscosity and entropy, and the pipe's `velocity`/`re` become table quantities readable on any port. Filed 2026-09-20; not scheduled. |
| L-52 | small | med | hunch | [`15`](15-semantic-model.md), [`23`](../20-core-domain/23-topology-and-graph.md), [`29`](../20-core-domain/29-layout-ladder.md) | **Does `supply` / `return` fix the direction of flow at a boundary, or only state a condition?** | What `D-64` meant: `supply` is where mass enters the plant and `return` where it leaves, so a boundary's keyword fixes the flow's direction there; the binder's `Enters`, the drawing's head (`28` H10) and the route layers (C16) all read it so. What came up (ladder step 11b, 2026-09-17): rewiring the syntax tour's mixing valve, the user proposed `NB2 - TV2.a` and `TV2.ab - SB1` -- a return feeding the valve, a supply receiving its outlet -- and, told that runs against the keywords, answered that supply nodes are ordinary nodes with stated boundary conditions, return nodes need none, and the direction is the solver's to find from the pressures. The tour was rewired the other way (`NB1` feeding both the chain and the valve), which reads the same under either meaning, so nothing forced the choice. Open: whether the keyword is a direction (as built) or only a boundary-condition marker with the direction found by the solve. The second reading changes the boundary equations and the well-posedness counting (`D-64`, `D-86`), the head rule and the layer rule; it needs a `D-` entry of its own, not an edit to `D-64`. **Closed 2026-09-17 by `D-115`:** the keyword keeps its direction and its kind, is spelled `inlet`/`outlet`, and a boundary has exactly one connection -- the user's later reading, that a supply node which feeds several paths should be a junction after the inlet, is the rule. |
| L-50 | medium | low | measured | [`15`](15-semantic-model.md), [`16`](16-diagnostics.md), [`57`](../50-frontend/57-state-visualization.md) | **The binder skips `show`, so `FS1210`–`FS1214` are unregistered and the contract reads the directive off the syntax** | `Binder` lists `ShowDirectiveSyntax` among the file-wide statements it steps over, and nothing else reads it: `SemanticModel` has no field for it. P5.1b needed `visualization` on the wire and got it from `ScriptSyntax` directly -- the first `show`, its properties by `57`'s long or short names, its range when written -- which is right for the first canvas and wrong as a home: an unknown property (`FS1210`), a property no element has (`FS1211`), a humid-air property on water (`FS1212`), a duplicate (`FS1213`) and a second `show` (`FS1214`) all pass silently today, and the alias table lives in the contract builder rather than a registry. Wants a binder step producing a `VisualizationSymbol` and the five codes; `ModelContractBuilder.Visualization` is the one caller to move. Filed 2026-09-15 with P5.1b. **Narrowed 2026-09-19 (sweep tier 3):** P5.10 registered and raises `FS1210`, `FS1213` and `FS1214` from `ColourScales.Resolve`, so the three mistakes a script can make today are said; what remains is the home (the binder, a `VisualizationSymbol` on the model, the alias table beside the registry) and `FS1211`/`FS1212`, which wait on humid air reaching the wire. Left open for that; the move is a refactor with no user-visible change and was not taken ahead of the solver's defects. **Narrowed again 2026-09-20 (P5.13a, `D-120`):** the alias table is out of the contract builder -- `PropertyTable` beside the registry owns the quantities, their symbols, names and aliases, and `ColourScales.Resolve` reads it, so `show t`, `show temp`, `show vflow` and a port's `in[2].temperature` all resolve through one table. What remains is exactly the home: the binder does not bind `show`, `FS1211`/`FS1212` are unraised, and `velocity`/`reynolds`/`viscosity`/`cp`/`k` from `57`'s table are not quantities of a state the wire carries yet. |
| L-25 | medium | low | hunch | [`15`](15-semantic-model.md) | `ISymbolMap` maps declarations and endpoints, not references inside an expression | `Print(Parse(x))` and hover both work from the spans the binder already holds. A `let` used in `power=Q*2` is recorded as a dependency (`ValueId`), never as a span, so go-to-definition from inside an expression finds nothing. Invariant 6 says "every source position **within a declaration**", which this satisfies; the wider thing an editor will want needs an expression walker, and nothing walks expressions for spans yet. P5.x, with the editor that asks for it. |
| L-35 | small | low | measured | [`14`](14-expressions-and-references.md), `D-57` | **`power=heating kW` — a curve reference with an explicit unit — is unimplemented** | The bare-number half works: `power=heating` takes the parameter's canonical unit through `D-14`, which is the common case and the one the feature was asked for. Overriding it needs an expression form the grammar does not have — a *reference* followed by a unit symbol, where today only a number may carry one. That is a `14` change and its own AST node, so it is deferred rather than smuggled in; the `curve` page does not promise it. Wanted explicitly in the proposal that produced `D-57`, so it is scheduled, not dropped. |

## Traps

What a session working against these documents gets wrong first. Promoted from *Observations*
or from a closed entry when a session hits it a second time; read before working in this tier.

- `dt` is the temperature change across a component (`D-123`), not a time step. A transient step needs another spelling (`step`, `interval`).
- A bare `vflow=0.3` is 0.3 l/s since `D-125` (2026-09-21); before it, 0.3 m³/s. A `.fluid` or a test written between P5.13b and `D-125` with a bare `vflow` means a thousand times what it says.

## Closed

| # | Effort | Document | What was wrong | What changed |
|---|---|---|---|---|
| L-62 | small | [`14`](14-expressions-and-references.md), [`31`](../30-solver/31-solver-architecture.md) | **A deferred target the seed could not supply was absent from the first pass, and a script whose well-posedness hung on it failed there with nothing naming the line.** Pass 0 supplies only what the script anchors; `NPS inlet t=NPR.t + 40 dK` on the substation is lowered without a primary temperature and is singular at pass 1 (`FS3002`), and the only word about the line was `FS1410`'s "not published by any pass", which is false when no pass completed. | Closed 2026-09-21. The row's second option: `FS1412` -- *was still waiting on {waited} when pass {pass} failed, so the circuit was solved without it* -- replaces `FS1410` on a refused pass and on an unconverged last pass, on both of the loop's exits; `FS1410` keeps its meaning for a run that stood. Measured on the substation probe: singular at pass 1, `FS1412` names `NPS.t` and pass 1, no `FS1410`. The first option, lowering pass 1 with the target counted as stated from its sizing rule, was not taken: it fixes the circuit's shape from a rule's guess and hides the walk the feature exists to make. Effort small, under the medium estimated. |
| L-63 | tiny | [`15`](15-semantic-model.md), [`14`](14-expressions-and-references.md) | **A node declared only on a connection line could not be read by an expression** | Rule I1 -- an endpoint naming nothing declared is a node -- ran in the topology pass, after every expression was evaluated, so `N3.t` was `FS1404`. The binder now infers those nodes at the end of `CollectDeclarations`, with every `let` and component known, through the same `Infer` the topology pass uses; a name a `let` holds is still left for the topology pass to refuse. `DeltaPressureTests.ANodeNamedOnlyOnAConnectionLineCanBeReadByAnExpression`. |
| L-61 | tiny | [`13`](13-type-and-unit-system.md), [`06`](../00-foundation/06-decision-log.md) | **No pressure-delta literal existed in an expression** | `D-127`, the user's choice between the row's two options: `dPa`, `dkPa`, `dbar` are `PressureDelta`-only symbols, so `300 kPa - 10 dkPa` is 290 kPa, a reading, and `N1 node p=10 dkPa` is `FS1304`. `DeltaPressureTests`; `units.md` says it beside `dK`. |
| L-60 | small | [`13`](13-type-and-unit-system.md), [`14`](14-expressions-and-references.md), [`06`](../00-foundation/06-decision-log.md) | **A pump's `head` could not be written from a pressure, so `14`'s and `D-120`'s flagship `head=1.2*HE1.dp` answered `FS1304`** | The user's call, `D-126`, over the risk the row stated: a `head` parameter accepts a `Length` value as metres of the pumped fluid (`Quantity.TryAssign`, at the parameter boundary only; arithmetic never makes a head, `Head` still has no symbol), `pi` and `g` are reserved constants (`FS1411` on a `let` of either), `Acceleration` is a dimension with `m/s2`, and `FS1304` on a head target spells the definition. `head=1.2*HE1.dp` itself stays refused, now with the words; `head=1.2*HE1.dp/(998 kg/m3*g)` is the example, **measured** at 2.4522 m from the seed (the exchanger's decided 20 kPa, which the seed now supplies as the script's own number), the valve promoted to Kv 7.01, 24.01 kPa rise, one pass -- not the `FS2303` shortfall the plan had predicted, because the loop had a free valve. What it cost: `head=drop` with `let drop = 25 m` is 25 m of head, pinned as the accepted behaviour. A pump's stated `dp` remains unread (`C-109`). `ConstantsTests`, `DeferredEvaluationTests`. |
| L-59 | medium | [`14`](14-expressions-and-references.md), [`31`](../30-solver/31-solver-architecture.md) | **A deferred reference to a solved or sized value was never evaluated: `PU1 pump head=1.2*HE1.dp` sized the head as if nothing were written, and said nothing** | What the entry believed was right: `SemanticModel.Deferred` had no consumer. P5.13c (2026-09-21) built `14`'s Phase B as `DeferredEvaluation` in the outer loop: each pass evaluates every deferred expression through the ordinary `ExpressionEvaluator` over a scope that reads a stated parameter as stated, a node or port from its solved state and a rated exchanger's second side from the rating (`SolvedStates.Exchanger`, shared with the report's ratings), writes the results in as *stated* values with a basis, and settles when the sizes match and no value moved by more than 1e-6 relative. Pass 0 evaluates against the seed with only the script's own anchors supplied -- the first version supplied the seed's node guesses too and put a 38 °C primary on a 150 kW exchanger. **Measured** on the chained pair: `HE2.in[2].t` written in at pass 1 as 67.146 °C from `HE1.out[2].t` (85 − 30 kW / (0.4 × 4.19)), confirmed at pass 2, two passes; through a `let` (`tprim - 2 dK`) 65.146 °C; `head=1.2*HE1.dp` answers `FS1304` (`L-60`) and the head stays sized; two exchangers reading each other answer `FS1410`, one per line, and settle on sizing's choices. `DeferredEvaluationTests`, five. What it left: `L-60`, `L-61`, `L-62`, `L-63`, and `C-109` (a valve's stated `dp` is read by nothing). `docs/functions/let.md`, the report guide. |
| L-57 | tiny | [`13`](13-type-and-unit-system.md) | **A bare `vflow=0.3` is 300 l/s** | `13`'s rule is that a bare number means the dimension's canonical unit, and `VolumeFlow`'s was m³/s because, until P5.13b, no parameter took a volume flow and the row was never exercised; `vflow` (`S-72`) exercised it, and the pages told the user to write the unit. | Closed 2026-09-21, the user's call: `D-125` makes `l/s` the sixth exception row (fifth unit). `Dimension.Entries`, the two unit tests, `13`'s table and invariant 8, `22`'s rows (0 … 1000 l/s), the two docs pages; `units.md` regenerated. Effort as estimated. |
| L-56 | small | [`15`](15-semantic-model.md), [`23`](../20-core-domain/23-topology-and-graph.md), [`44`](../40-api/44-diagnostics-contract.md) | **The counting table, the sizing decisions and the solve report label a port's state by its key, not its spelling** | `D-120` split every port-indexed parameter into a script spelling (`in[2].t`) and a model key (`in2`), and P5.13a stopped at the binder: `ComponentConstraint.Label` read `HX1.out2`, the solve report's *constraints*, *equations* and *values chosen* lines and its branch rows (`S2 -> T1.in2`, `into T1.in2`) all carried the key beside a script that says `out[2].t`, and `FS2210`/`FS2214` quoted it. | Closed 2026-09-21 (P5.13b), estimated medium, was small. `ComponentConstraint` carries the key as `Parameter` (what the assembler, seed and promotion rules match on) and the script spelling as `Name`; `Label` spells. The report spells its sizing keys and its branch ends through the registry (`SolveExplanation.Spelled`); `Bases`, the wire's `sizes`/`solved` maps, port ids, `ComponentStateWire` fields and unknown owner ids (`S2->T1.in2`) stay keys by `D-120`. Promotions never name a keyed parameter (only `position`, `power`, `head`, `kv`). Trap met on the way: `Branch.cs` may not name the registry (`23` invariant 7, `ArchitectureTests`), so the port spelling is the report's, not `BranchEnd.Label`'s. Substation report: `FixedFlow on HX1.out[2].t -> solved for as PCV.kv`, `HX1.out[2].t stated`. |
| L-55 | small | [`16`](16-diagnostics.md), [`44`](../40-api/44-diagnostics-contract.md) | **Six producers set a diagnostic's component; the wire attributes the rest by span** | Changed 2026-09-19 (sweep tier 3), by a rule rather than a sweep over 46 sites: the binder's `Report` notes any diagnostic whose `name`, `node` or `component` argument might name a component, and at the end of the bind -- once every component, node included, has its slot -- sets `ComponentName` to that value where it is one. A node's `FS1510` is raised before its slot exists, which is why it resolves at the end and not at the site. The contract builder's span fallback is restricted to *declared* components again, because `C-97` gave an implicit pipe its connection line and the fallback had begun handing the line's node notices to the pipe. On the cooling loop every `FS1510` now names what it announces (`PU1__HE1`, `N2`, `HE1__3WV`, `3WV__N3__in`, `3WV__N3`) where the wire read `null` or, after `C-97`, the pipe. Open elsewhere: a producer whose argument is called something else (`curve`, `circuit`) is not attributed, and a `name` that is a `let` binding sharing a component's name would be -- a wrong link in the log, not a wrong number. |
| L-40 | — | [`15`](15-semantic-model.md), `D-60` | A `format=` that is not a quoted string is silently ignored, and every row then fails | Changed 2026-09-19 (sweep tier 3), `D-60`'s two checks written: `FS1534` on the `format=` argument when it is not a quoted string, names no day (`d`) or no month (`M`) -- once, with the reason, and the rows are then neither read nor reported, since they are not the fault; and `FS1535` on the header as the cap on `FS1117`'s cascade when the header is fine and the rows are not: the first five are marked where they are, the rest are one count (*7 more rows could not be read; the first 5 are marked*). `dd/mm/yyyy` -- minutes where months go -- is the case the theory names. `15`'s table and note carry both. |
| L-54 | — | [`13`](13-type-and-unit-system.md), [`22`](../20-core-domain/22-component-model.md) | **The unit table has no symbol for W/(m²·K) or m²·K/W, and the SI spelling it falls back to is in base units** | `D-119` (2026-09-19, sweep tier 2): two named dimensions, `HeatTransferCoefficient` (W/(m2*K), with `kW/(m2*K)`) and `ThermalResistance` (m2*K/W), spelled as `13` spells `J/(kg*K)`. `Dimension.FromVector` maps the vectors to the named entries, so an expression that produces W/(m²·K) by arithmetic lands on the name too; the exchanger's `u` and `fouling` rows name them directly. `metadata` now lists both dimensions with their units, and the wire reports `W/(m2*K)` and `m2*K/W` where it read `kg/(s³·K)` and `s³·K/kg` (`A-3`). `ToSiUnitString` is unchanged and, for these two, unreached. `TheExchangerCoefficientsHaveNamedDimensionsAndSpellings` covers the parse, the print and the registry. |
| L-53 | — | [`16`](16-diagnostics.md), [`44`](../40-api/44-diagnostics-contract.md), [`42`](../40-api/42-rest-contract.md) | **`FS1503`'s span covers `name=value`, not the name** | The binder reported `FS1503` (and `FS1512`) at the parameter syntax's whole span. Changed 2026-09-19 (sweep tier 2): both are raised at `parameter.Name.Span`, so `zzz=30` underlines `zzz` and a quick-fix over the range takes the name alone. `DiagnosticsContractTests` now asserts `length == 3` for the multi-byte case instead of settling for `>= 3`. The corpus check over every name-spanning code was not written; `FS1101`'s family already spans identifiers by construction. |
| L-1 | — | [`16`](16-diagnostics.md) | `FS1201` and `FS1202` were registered nowhere | Both classify a `style` token as a colour or a corner treatment, which needed registries that did not exist; absent deliberately. (`FS1107` was here too and is raised by the binder, the first stage that knows a circuit's mode.) | Closed 2026-09-15 by P5.1d (`D-104`): `StyleTokens.Classify` in the binder classifies against the CSS named colours and the three corner words and raises both; `FS1204`/`FS1205` arrived with the named styles. Sat in the open table until the sweep of 2026-09-19. |
| L-21 | — | [`14`](14-expressions-and-references.md), [`24`](../20-core-domain/24-auto-sizing.md) | `FS1405` is unregistered | Filed when the fixed point could not fail because there was no loop to iterate; the outer loop has existed since P3.7 and `OuterLoopResult.Settled` records the cap being reached with sizes still moving. What is still true: nothing is *raised* for it -- `Settled == false` is a bit on the record that no diagnostic reads, so a script whose sizes never settle gets its last values and no word. | Folded into `C-74` on 2026-09-19: `14` names the same event `FS1405` (an expression's fixed point, three values in the message) and `24` names it `FS2301` (the sizing loop's), and `31` writes them as one line; which code the outer loop raises, and with what values, is that entry's decision, not this one's. |
| L-3 | — | [`17`](17-formatting-and-round-trip.md) | The mutation API and the formatter were unimplemented | By design: the formatter was P5.5's and `IScriptEditor` P7.1's; P2.5 delivered the printer only. | Half closed: P5.5 (2026-09-18) shipped the formatter, and `17`'s formatter criterion is ticked. The mutation API remains P7.1's, tracked by `17`'s unticked `IScriptEditor` criteria rather than by a defect, since nothing is wrong with what exists. Reworded 2026-09-19. |
| L-51 | — | [`12`](12-grammar.md), [`15`](15-semantic-model.md), [`22`](../20-core-domain/22-component-model.md), [`23`](../20-core-domain/23-topology-and-graph.md) | **A pipe was a declared component, but on the drawing the connection line already is the pipe; the user wanted the pipe's properties on the connection line** (filed on the layout ladder's step 5, 2026-09-17, with three options: trailing properties making an implicit pipe per connection; every connection a pipe; status quo) | `D-110` chose the first the same day and P5.1e shipped it 2026-09-18: a connection line may end in `name=value` pipe properties, held on the `ConnectionSyntax` and printed byte for byte; each connection on the line becomes a `pipe` named `{A}__{B}` (rule I7, created in binder step 1 so its parameters bind like a declaration's), I2's nodes beside it are `{pipe}__in` / `__out`, a `length` the line does not state is zero (a decided default, `default` on the wire), and a bare connection stays a lossless link so no existing script changed meaning. The samples, `01`'s reference circuits and the ladder scripts were rewritten to the line form; the tour keeps one declared pipe whose far end an `outlet` attachment wires. The rule is I7, not I4, because `11`'s table already had I4 (flow direction) and I6 (chains). Left open by it: `C-97` (the implicit pipe reports no source span, though its line is a natural one). |
| L-36 | — | [`13`](13-type-and-unit-system.md), `D-60` | A timestamp is documented as a lexical unit and implemented as a **line**-level one | `13` said "a timestamp is a lexical unit". It is not, and cannot be: `2026-01-01` is also a valid subtraction, so no context-free lexer can tell them apart. What ships is narrower and works — a curve row keeps its raw tokens, the binder splits the row's text at its last run of whitespace, and the lexer's only part is a `Colon` token. | **Closed 2026-09-15 by correcting `13`** to say exactly that; no code changed. Found in the pre-`P5.1` audit of open entries. |
| L-49 | — | [`12`](12-grammar.md), [`14`](14-expressions-and-references.md) | **Two ways a script could take the process down instead of getting a diagnostic, and one way it got the same diagnostic eight times** | Found by the 2026-09-14 code review of Core (`.claude/dotnet-toolkit/review/2026-09-14-202157-Core.md`), which was asked for the class of flaw `C-76` was: something no test sees and no diagnostic reports. *No pipeline stage throws on user input* is the rule, and a stack overflow is the one failure that rule cannot even catch — .NET terminates the process, and the parser runs on the host once per keystroke, so one such line ends every session on that host. **The parser** was recursive descent with no depth guard: `let x = ((((…` ten thousand deep, or ten thousand `-`, or ten thousand nested calls, overflowed; measured, the test crashed the runner with `Stack overflow`, exit 134. **The dependency walk** was a recursive DFS whose depth is the longest chain of references in the script — and a chain is well-formed input: twenty thousand `let`s each reading the next overflowed the same way (the same chain declared the other way round does not, because each value finishes before the next starts, which is why a first version of the test passed on the broken code). **The evaluator's unary minus** visited its operand twice — once to classify the result and once to return it — and a visit reports as it goes, so `---nosuchthing` reported `FS1404` eight times, and a chain of `n` minus signs over a bad operand cost 2ⁿ visits. | **Closed 2026-09-14.** Expression nesting is bounded at 64 (`LineParser.MaxExpressionDepth`); past it the line is `FS1104`, and `14` records the bound and why. `DependencyGraph.TopologicalOrder` keeps its path on an explicit stack; the cycle report is unchanged. `Unary` visits once. Pinned by `TenThousandNestedLevelsAreMalformedNotAStackOverflow` (three shapes), `SixtyNestedParenthesesStillParse`, `ADependencyChainTwentyThousandDeepEvaluatesRatherThanOverflowingTheStack` and `AMinusOverAnUnknownNameReportsItOnce`; the first and third were run against the old code and crashed it. |
| L-48 | — | [`15`](15-semantic-model.md) | **Two names that spell alike once normalised crashed the binder on the next typo** | Found while fixing a performance finding of the same review (the suggestion index was rebuilt on every unresolved name). Names are ordinal — `total` and `Total` are two bindings, `PU1` and `pu1` two components — but the *suggestion* index behind `FS1404`'s "did you mean" is keyed by `NameResolution.Normalize`, which folds case and underscores, so the two share one key. It was built with `ToImmutableDictionary`, which throws on a duplicate: `let total = 1`, `let Total = 2`, `let x = totl + 1` took the binder down with `ArgumentException: An element with the same key … already exists`. Not a stack overflow, so the API would have answered 500 rather than dying, but a script that is legal by every rule in `15` produced no diagnostic and no model. | **Closed 2026-09-14.** `BindingRun.SuggestionIndex` is built once per set of declarations with `TryAdd`, so the first declared spelling keeps the key — a suggestion is a hint, not a resolution, and either spelling is the right hint for a typo of both. Pinned by `FS1404_TwoNamesThatSpellAlikeOnceNormalisedStillYieldASuggestion`, which was run first against the old code and reproduced the exception. |
| L-47 | — | [`15`](15-semantic-model.md), [`22`](../20-core-domain/22-component-model.md), `D-91` | **A stated terminal binds to the port the connection order named, not to the port the water actually enters by, and a reversal is silent** | Found in a design discussion on 2026-09-14 about whether `power` should be absolute with its sign declaring flow direction. It should not, and the reason is that one sign cannot carry two facts: which way heat crosses the wall is a property of the device, which way the water runs is a solved unknown, and `D-91` already puts the first on the role word and `D-69` already lets the second be whatever the solve says — a reversed heater heats the node it actually discharges into. The trap is the stated terminals. `HE1 in=50` is bound to the port `in`, which is whichever port the connection order made the inlet; if the solve reverses the branch, the constraint sits on the outlet. That is precisely the automation-system failure it mirrors: T1 is mounted upstream of the flow the installer assumed, the flow reverses, and the computed duty flips sign while nothing says which of the two changed. The model has the same blindness and reports nothing. A second, smaller gap in the same place: `load power=-24` is silently taken as +24 (`D-91` applies the role direction to the magnitude so old scripts keep running), and a minus on a role word is either a typo or a misunderstanding of the convention — both worth a message. | **Closed, both halves, on 2026-09-14.** `FS3013` (`OuterLoop.Reversals`): after a converged solve, a pump or exchanger whose inflow at `in` is negative is named with its flow, its direction and any stated terminal now on the port the water leaves by; read off the port map, not the branch orientation, so a component walked from its outlet end is not a false positive. Pipes are exempt because their `in`/`out` assert nothing. `FS1308` (`BindingRun.CheckRoleSign`): a negative `power` on a role word is taken as its magnitude and says so, with the neutral spellings exempt. Terminals stay port-bound, as the entry asked. Pinned by `AComponentTheWaterRunsThroughBackwardsIsNamedAfterTheSolve` (the simple loop with `N4 - LOAD - N3`, converging unchanged) and three `FS1308` cases. |
| L-46 | — | [`13`](13-type-and-unit-system.md), [`16`](16-diagnostics.md) | **`FS1302` told the user to write a correction that is wrong by 273.15, and the correction compiles** | Closed by the same change that found it, and recorded because of *how* it survived. `FS1302` refuses `20 C + 30 C` --- the invariant the whole affine unit split exists for --- and its message offers the line to write instead, because *"cannot add two temperatures" without the alternative is a dead end*. The offer was built from the right operand's **`SiValue`**, and for a temperature that is kelvin: the message read *"To offset by a difference, write '20 °C + 303.15 dK'"*. That is 323 °C where the user meant 50. **It is worse than an unhelpful message, because the suggestion compiles** --- a reading plus a difference is a reading --- so a user who pastes it gets a second wrong answer and no second error. **Two things hid it.** The only test asserted `Assert.Contains("dK", message)`, which a wrong number passes; and `40 C + 30 K`, the other case anyone tries, comes out **right by accident**, because kelvin has no offset and its SI value *is* the written number. So the case an engineer hits first looks correct and the case they hit second is silently wrong. |
| L-26 | — | [`15`](15-semantic-model.md), [`23`](../20-core-domain/23-topology-and-graph.md) | Nothing says what boundary condition an I3 node carries | **Closed --- `23` answered it, and tier 20 already closed the twin of this entry.** [`23`](../20-core-domain/23-topology-and-graph.md)'s *I3's boundary nodes* section tabulates all three cases: an open port on a valve's bypass, an open port on anything else, and a declared degree-one node with no stated boundary. All three carry **zero flow**, and an I3 node is a boundary in its own right --- which is exactly what makes the binder's `FS2107` exemption correct rather than a guess, because the node *is* the boundary that rule created. `C-7` closed on that same answer and this entry did not, so the pair sat one open and one closed over one resolved question. The consequence this entry feared --- "the reference circuit's nine becomes eleven" --- never arrives, since the condition is stated rather than left to lowering. **One thing found while confirming it:** that table still writes the bypass port as `c`, which `D-85` renamed to `b`. |
| L-20 | — | [`15`](15-semantic-model.md) | The circuit-role registry has no documented entries | **Closed.** `15` gains a **role registries** section enumerating all thirteen circuit roles with their aliases, and states the resolution rule (canonical name or any alias, case-insensitive) and the diagnostic (`FS1519`). It also records the asymmetry that matters for changing them: **adding an entry is not a breaking change and removing one is**, so both lists are additive-only until a `D-` entry says otherwise. |
| L-32 | — | [`15`](15-semantic-model.md) | `FS1507` skips a kind with no ports at all, and skips `node` | **Closed as a decision rather than a change.** `15`'s step 10 now says what `FS1507` skips and why. A kind with **no ports at all** is never warned about: a controller appears in no connection by design, so warning would squiggle the one script using `D-40` correctly. A declared **`node`** is skipped too, and the entry was right that this is a judgement rather than a derivation --- so it is written down as one, with its cost: a genuinely orphaned `node` is silent here, and `FS2107` catches the dead-end case once topology runs, which is where the evidence to tell the two apart exists. |
| L-39 | — | [`15`](15-semantic-model.md), `D-59` | The schedule-role registry has no documented entries | **Closed with `L-20`, in the same section and for the same reason.** All seven schedule drivers are enumerated in `15` with their aliases, so `FS1527`'s "name a known driver" has a list to point at. `D-59` sanctioned `tout`; the other six follow its pattern and are implementation-defined, which `15` now says rather than only `ScheduleRoleRegistry`. |
| L-45 | — | [`12`](12-grammar.md) | **`kind-name = identifier` was load-bearing reasoning and is no longer true** | `12` argued that `node` and `pipe` could be unreserved because a kind name "cannot match a keyword token" — using the production as evidence for the safety of the change. `D-64` puts `supply` and `return` in kind position, so the production is now `identifier | **Closed.** `12` now states the reverse direction as a rule instead of leaving it in `ComponentRegistry.Verify` alone: **a reserved word may be a kind's own keyword and may not be an alias**, enforced as a registry error rather than a script diagnostic, because it is a defect in this project's tables and never in a user's file. The asymmetry is the point --- a keyword spelled with a reserved word is a decision the log sanctions (`D-64` sanctioned exactly two), while an alias that collides buys a second way to write something already writeable and costs a word the grammar wanted. So adding a reserved word an existing alias claims fails the build rather than silently shadowing it. `12` also now records that the *original* argument used the production as evidence for its own safety, and that the change survived losing it. |
| L-4 | — | [`13`](13-type-and-unit-system.md) | `Head`'s canonical unit is metres *of the pumped fluid*, and nothing converts it yet | **Closed.** The conversion exists and is where it has to be: `Pump.EvaluateResiduals` computes `density * Gravity * Head(...)`, taking the density from the solve context, so metres of the pumped fluid become Pa at the point the fluid is known. That is exactly the dependency this entry named --- "turning metres into pressure needs a density, which needs a fluid" --- and `Pipe` does the same for `Elevation`. `13`'s table needed no change: `Head` is metres of the pumped fluid, takes no unit symbol at all (`D-50`), and is not interchangeable with pressure. |
| L-44 | — | [`15`](15-semantic-model.md) | **The binder's step order put attachment resolution after inference, and `23` requires it before** | **Closed.** `15`'s step list now binds attachments at step **8**, before inference at step 9, which is what the code has done since `P3.4b` and what `23` requires. The two consequences are stated there: the `_sourceConnections` snapshot includes attachment-derived connections, which is right because `supply` *is* a connection the user wrote; and `ReportDeadEnds`'s exemption for attachment parent nodes is gone. **The exemption was removed rather than argued about** --- the suite is unchanged at 1408, which is the evidence that the edge it stood in for is now counted by the time the check runs. |
| L-6 | — | [`12`](12-grammar.md) | `%` was both a unit symbol and a modulo operator, and `.` had three meanings | `D-51`: `%` is a unit only; `30..60` lexes as three tokens. |
| L-7 | — | [`12`](12-grammar.md) | Sections were file-wide, so the distribution-header reference circuit did not parse | `D-52`: `connections` and `schedule` are scoped to a circuit. A second of either is `FS1101`. |
| L-8 | — | [`16`](16-diagnostics.md) | Six code ranges named the stage that emitted a diagnostic rather than its subject | `D-53`. A range that names a stage has to move every time a check moves. |
| L-9 | — | [`12`](12-grammar.md) | `FS1101` was cited for a catalogue directive holding two ids — a code whose message is about a duplicated section | `FS1114` allocated, "text after a statement that is already complete", which covers the general case: a second catalogue id, a second circuit number, a stray word after a project name. |
| L-10 | — | [`12`](12-grammar.md), [`17`](17-formatting-and-round-trip.md) | The two documents disagreed about where trivia lives, and under `12`'s reading no node held `let`, `=`, `-` or `(` | `D-55`: trivia lives on tokens, and a node holds the tokens it consumes. The AST could not have round-tripped otherwise. |
| L-11 | — | [`12`](12-grammar.md) | The AST as specified could not round-trip: `fluid`'s mode is optional and four nodes had nowhere to keep the tokens they were printed from | `D-54`. |
| L-12 | — | [`12`](12-grammar.md) | The section table omitted its own two markers, which made every `schedule` below a `connections` line `FS1103` — and the schedule section unreachable in any circuit with connections | `D-56`. `12`'s own worked example was the failing case. |
| L-13 | — | [`12`](12-grammar.md) | One token of lookahead could not classify `3WV.b - N3`, the document's own example of port qualification | `D-56`: a second token of `-` **or** `.` makes a connection. Still one token, and nothing else can put a `.` there. |
| L-14 | — | [`17`](17-formatting-and-round-trip.md) | `FS1604` was assigned to two different triggers | The `ApplyTags` one became `FS1607`. Two triggers behind one code sends the user to a page describing the other one. |
| L-15 | — | [`17`](17-formatting-and-round-trip.md) | Trivia rule 1 never said which side of the line break the terminator falls on | Stated: it opens the next token's leading trivia. Deliberately not Roslyn's convention — it is what makes a statement's tokens cover exactly one line, which the line-granular parser and its recovery rest on. |
| L-16 | — | [`17`](17-formatting-and-round-trip.md) | The first acceptance criterion asked for malformed files in `samples/`, which `12`'s corpus criterion requires to parse clean | Reworded to what is actually enforced: the samples, every fenced block, the adversarial list, and every fuzz mutation. |
| L-17 | — | [`15`](15-semantic-model.md) | The ambiguity margin's worked example does not follow from the formula above it. `valv` scores 0.80 against `valve` and **0.44** — not 0.78 — against `3wayvalve`; the "normalised prefix" it cited is computed nowhere | Replaced with `4_way_valve`, which is exactly one substitution from both `2_way_valve` and `3_way_valve` and scores 0.889 against each. The margin was right; the example was not. |
| L-18 | — | [`15`](15-semantic-model.md) | `FS1502`'s message offers a suggestion, and nothing said when a suggestion stops being worth making | Suggestion floor of 0.60, and a second `FS1502` message for having nothing to suggest. |
| L-19 | — | [`15`](15-semantic-model.md) | `ComponentKindInfo` had no way to say that a node accepts any number of connections | `HasUnlimitedPorts`. Without it the binder has to know that the kind spelled `node` is special, which is the one thing the registry exists to prevent. |
| L-22 | — | [`15`](15-semantic-model.md), [`14`](14-expressions-and-references.md) | `FS1502` and `FS1404`'s message shapes end in an optional "Did you mean '{suggestion}'?" clause, which [`16`](16-diagnostics.md)'s style rules reject: a template ending in a clause that is sometimes empty is not a sentence | Both messages are now sentences, and the suggestion rides on `Diagnostic.Suggestion` — the structured field that already existed, which an editor can offer as a fix rather than prose to parse back out. `15`'s two-row table for `FS1502` is one row again. |
| L-23 | — | [`22`](../20-core-domain/22-component-model.md) | `samples/m1-syntax-tour.fluid` declared three components of kind `duty` | There is no such kind: Duty is a heat-exchanger *mode*, computed at lowering from what the script states, and `22` says outright that there is no script `mode=` parameter. The binder is the first stage that could notice, and did, on its first run over the samples. Changed to `heat_exchanger`. |
| L-27 | — | [`15`](15-semantic-model.md) | **No binding step bound the schedule.** Steps 0–11 never mentioned a disturbance, and the parser produced `DisturbanceSyntax` for nothing to consume | Folded into step 9, which already resolves the cross-circuit references a `control` line needs — a scheduled target resolves exactly the way an actuated one does. Not a twelfth step: renumbering would have invalidated "steps 6–11" in four documents to say the same thing. |
| L-28 | — | [`15`](15-semantic-model.md) | `FS1520`'s message read "takes flow at '{node}' and never returns it", which is false for half of its own trigger | The trigger is `supply` without `return` **or the reverse**. Rewritten direction-neutral: `'{circuit}' declares '{present} {node}' and no '{other}'.` |
| L-29 | — | [`15`](15-semantic-model.md) | Nothing distinguished `FS1507` from `FS1511`, and on the syntax reference both would have fired for `PU1` | They partition one mistake: `FS1507` is a component in no connection, `FS1511` a cluster of two or more joined only to each other. Judged on the connections the user wrote — after I3 nothing is unconnected, and neither code could fire again. Written into step 10. |
| L-30 | — | [`15`](15-semantic-model.md) | `FS2107` would have fired on every I3 boundary node, making the syntax reference's fixed nine eleven | An I3 node *is* the boundary that rule created. Exempted, and stated in step 10 — see L-26 for what still has to hold for that to stay true. |
| L-31 | — | [`06`](../00-foundation/06-decision-log.md) `D-35`, `CircuitRoleRegistry` | `circuit coolingLoop` — the name in the syntax reference, `12`'s example, and `52`'s — resolved to no role, so the documentation's own flagship script emitted `FS1519` about itself | `cooling_loop` registered as an alias of `cooling`, alongside the `solar_loop` and `district_loop` that were already there. Found by asserting `01`'s nine-diagnostic count, which came back as ten. |
| L-33 | — | [`18`](18-script-compatibility.md) | `FS1705` and `FS1112` were two codes for one trigger | `FS1705` was "version or catalogue directive is misplaced or duplicated", which is exactly what `FS1112` already says and already raises. A misplaced line is a grammar error, and `D-53` puts a code in the range naming its *subject*. `FS1705` narrowed to what only the gate can judge — a file whose directives name **different majors**, from which no semantics can be selected. Not the redefinition `16`'s invariant 7 forbids: `FS1705` had never been registered, and `18`'s table was its only reference. |
| L-34 | — | [`18`](18-script-compatibility.md) | Nothing said how `Inspect` finds the directive without parsing, though invariant 2 requires exactly that | It cannot ask the parser which major to parse under without asking the question it exists to answer. Stated: it scans the raw text past a BOM, blank lines and comments, and matches `fluidscript` plus one unsigned decimal — a prefix fixed across majors by construction, since a major that changed its own version line's spelling could not be detected by an application that did not already know its version. |
| L-37 | — | [`14`](14-expressions-and-references.md), [`13`](13-type-and-unit-system.md) | **A unit spelling shared by two dimensions evaluated as a bare number, so `p=2 bar` bound as two kilopascals** | The evaluator resolved a literal with the overload that *refuses* an ambiguous spelling, and fell through to "bare" on refusal — under a comment asserting that reaching there would be a lexer defect. `kPa` survived by accident, being the canonical spelling of both pressure dimensions; `bar`, `Pa`, `MPa`, `mbar`, `psi` and `mH2O` did not. `UnitTable.Resolve(text, expected)` added, and the evaluator now carries the dimension its result is being assigned to. Found by a design value in the wrong dimension not raising `FS1304`. |
| L-38 | — | [`13`](13-type-and-unit-system.md) | **A negative temperature could not be written with a unit anywhere in the language** | `13`'s unary table makes `−Temperature` an error, and the evaluator applied it to `-26 C` — so `design tout=-26 C`, which is `D-59`'s own worked example, did not bind, and neither did `NB1 node t=-5 C`. The bare `-26` worked, which is what made it a trap rather than an inconvenience. `D-62`: a sign directly on a quantity literal is part of the literal. `-(26 C)` is still a negation and still an error. |
| L-43 | — | [`13`](13-type-and-unit-system.md) | **`FS1307` is specified for "a strictly positive parameter" and nothing says which parameters those are** | The row names a trigger with no source for it: no registry field marks a parameter as strictly positive, and `13` proposes none. The answer was already in the table — a parameter's `UsualRange` declares its sign, since `dt` is `0.1…200` and `power` is `-100…100 kW` — but it takes a reader who knows both documents to see it, and the obvious alternative is a second per-parameter flag that would then have to agree with the range. `13` now says so. It also now states the exemption the rule needs: an absolute temperature is written in °C and held in K, so `t=-50` is 223.15 and a value that did go negative would be below absolute zero, where the message is wrong. |
| L-42 | — | [`15`](15-semantic-model.md) | **Three paragraphs still said `D-23` defers sensors**, a decision `D-61` reversed | `ControlBindingSymbol`'s own remarks justified the setpoint's placement by it; the tag section said the supply/return branch case "needs the sensors `D-23` defers"; and binding step 3 said a `measure=` reference is unresolved "under `D-23`". `22` carried a fourth. All four were written before `D-61` and none was found when `D-61` landed, because the amendment was applied where sensors are *specified* and not where they are *presupposed*. What `D-23` still governs is instrument dynamics. Found by reading `15` for `P3.0`, not by a review pass. |
| L-41 | — | [`12`](12-grammar.md), [`15`](15-semantic-model.md) | The classification rule meant a malformed curve row is often not a row | `nonsense here` inside a curve section classifies on its first token as a *declaration*, so it is `FS1103` and never reaches the row reader. `FS1117` from the binder covers only a line that starts like a row and fails to read — `12 34 56`, or a timestamp the stated format does not fit. That is the right split and it is not written down anywhere: a test asserting `FS1117` for `nonsense here` was written and failed. |

## Observations

**The corpus test is what finds these.** Nearly every entry above was found by running the
specification's own examples through the implementation, not by reading the document again. `12`
records that both reference circuits once failed to parse while every acceptance criterion in that
document passed. A block that is meant to be wrong declares its codes on its fence
(` ```fluidscript expects=FS1203 `); a block that is not a script at all does not claim to be one.

**A port name that reaches the graph is not evidence that anyone wrote one** (`D-88`). `Unqualified`
resolves an endpoint with no port to a real port by preference order — outlets first on the left of the
dash, inlets first on the right — and `Lowering.Build.End` then records that port's name exactly as it
records a written one. So `3WV.a` in a branch table may be the user's word or may be connection order,
and downstream nothing could tell. This mattered once and will again: a three-way valve's `a` is its
control path and `b` its bypass, and on `m2-cooling-loop`, wired `HE1 - 3WV` / `3WV - N2` / `3WV - P1`,
positional binding puts the inferred `a` on the **recirculation** leg — the opposite of what the letter
means. `EndpointSymbol.PortStated` and `CircuitGraph.StatedPorts` carry the distinction now. Anything
else tempted to read meaning into a port name has to consult them first.

**Three constants in `15` were chosen, not derived.** The resolve threshold (0.70) and ambiguity
margin (0.05) come from the document; the suggestion floor (0.60) does not, and was set by the `fan`
case. If any of them is ever tuned, the tests that pin them name the input that fixed them.

**`at` and `over` are ordinary identifiers**, classified by position inside a `schedule` section.
Reserving two common English words to buy nothing is the trade `P6` exists to refuse — worth knowing
before someone "fixes" the classifier by reserving them.

**A statement's tokens never include the newline that ends its line.** Anything computing an edit
span over a statement has to add its own line break. See L-15.

**A bare number is reinterpreted at assignment, not at evaluation.** The evaluator reports whether a
unit took part anywhere in an expression; the binder converts a bare result into the parameter's
canonical unit (`D-14`). Doing it the other way — handing the evaluator the target's dimension —
would make the same expression mean different things in different places, and `let x = 30` would have
no meaning at all.

**A stated parameter is readable immediately whatever its property's availability says.** A
`heat_exchanger`'s `power` property is registered as `Sized`, but a user who wrote `power=30 kW` can
read `HE1.power` at once. Availability describes where a value comes from when nobody stated one.

**`Print` reconstructs from tokens and must never slice the source.** A slice over `FullSpan` would
reproduce the file no matter what the tree had lost, which makes the round-trip test pass while
proving nothing.

**A count is the cheapest specification there is.** `01` fixes the syntax reference's diagnostic set at
nine, and asserting it found three separate defects in one run: the `FS1519` on `coolingLoop` (L-31),
the `FS2107` on every I3 node (L-30), and the two ways `FS1507` and `FS1511` could double-report
(L-29). None of them would have been visible from a test that checked "an FS1507 is produced".

**Steps 6–11 needed the component list to have one home.** The binder kept a `ComponentSymbol` in a
name→slot dictionary *and* in an ordered list; step 6 rewrites a component's ports and step 11 its
tag, both by `with`, so the dictionary's copy went stale the moment a port was materialized. The slot
now holds an index. A record's structural equality makes this class of bug silent: the stale copy
compares equal to nothing, and reads perfectly.

**A drift check that parses a document must assert it parsed something.** `CodeRangeOwnershipTests`
reads `16`'s range table with a regex; a pattern that matched nothing would make both of its
assertions pass while checking nothing at all. It asserts a floor on the row count and one known row
first. Every check in this repository that reads the plan has this failure mode, and this is the only
one that currently guards against it.

**A curve is a node of the dependency graph, and that is the whole of step 0b's cost.** Making
`ValueId.Curve` an ordinary id bought the topological order for free, so a curve that drives another
is already evaluated when the second is reached, and a cycle among curves is the same `FS1402` and the
same depth-first sort that already reported one among `let` bindings. The alternative — a second
ordering pass over curves alone — is the shape that drifts.

**The design point is tried before the driver chain, and the order is the feature.** `D-58`'s worked
example only works that way: with `design tout=-26`, `time → outdoor → heating` is not walked at all.
Reversing it would make a file carrying a year of weather data unsolvable statically, which is the
case the design point exists for.

**A driver's role is resolved even when the driver is a curve.** `curve heating outdoor` is driven by
the curve `outdoor`, and `design tout=-26` still reaches it, because the role is the design point's
*key* rather than the driver's kind. Resolving the role only in the `Role` branch is the obvious
implementation and it breaks `D-58`'s example silently — the curve chain is walked instead, and the
number that comes out is plausible.

**An observer needed no exemption from `FS1507`, `FS1511` or `FS2107`.** All three are already
conditioned on ports or on appearing in a connection, and an instrument has neither. The registry's
`IsObserver` earns its place elsewhere — it is what makes `at` on a pump `FS1532` — but the validation
half came out free, which is worth knowing before someone adds a fourth check that hard-codes a list
of instrument kinds.

**`design` values evaluate through the same pending/graph machinery as everything else**, with two
extra fields on `PendingValue` rather than a parallel path. That is what makes `design tout=baseTemp`
work without anything being written for it, and what makes the dimension check reuse `FS1304`.

**Four of the six closed in this sweep needed a document, not a change.** `L-20`, `L-39`, `L-32` and
`L-45` were all the same shape: a rule that existed in code, was correct there, was commented there ---
and lived in no `plan/` document, so every session that met it had to re-derive whether it was
deliberate. `ComponentRegistry.Verify` explains in eight lines of comment why a reserved word may be a
keyword and not an alias; `CircuitRoleRegistry` says in as many words that its twelve roles are
implementation-defined. None of that was reachable from the tier that owns the language. **A defect
whose fix is "write down what the code already knows" is the cheapest kind there is and the easiest to
leave open**, because nothing is broken and no test fails --- which is exactly why the list grew.

**`L-44` closed by deleting code and running the suite.** The entry said the attachment exemption in
`ReportDeadEnds` was "now dead code"; that had been believed since `P3.4b` without being tested. Removing
it left 1408 tests passing, which converts a claim into a measurement for the cost of one build.

**A registry row has three spellings, and the dictionary has to be keyed by the one nothing prints** (P5.13a, `D-120`, 2026-09-20). Before `D-120` a parameter's name was its identity: the registry dictionary, the bound `ComponentSymbol.Parameters`, the sizing decisions, `FS2101`'s group lists and the wire all held the one string. `in[2].t` broke that in a way the first cut got wrong twice. Keying `ComponentKindInfo.Parameters` by script name while `component.Parameters` held keys made `ReviewRequiredParameters` and the factory's `kind.Parameters.ContainsKey(name)` silently miss every keyed row -- a side-2 size (`flow2`) would have been dropped on the floor with no diagnostic, because the check that a sized name is a real parameter looked the key up in a dictionary of names. The rule that came out: **the dictionary is keyed by `Key`, every message that quotes a parameter goes through `kind.ParameterName(key)`, and the only place a written spelling is ever compared is `ResolveParameter`.** The doc-matching test (`RegistryMatchesTheComponentModelTests`) reads names, the components' `Port { Name }` lists are compared against `Key`, and a test that asserts a message quotes `in, out` is a test that quotes keys. The fold order mattered too: `in[1].level` folded to `in.level` must be tried *before* the written form, or it matches the `in[{index}].level` family with an index below its minimum and reports `FS1516` for a legal spelling; `layer[1].t` folded to `layer.t` matches nothing and falls through to the written form, which is the family. The lexer's rule-5 clause also had to grow -- `50 in.t=` and `50 in[2]` were fifty inches -- and it grew by one character of lookahead in each direction (`[`, and `.` followed by a word start), not by a new rule.
