---
id: 30-solver-defects
title: What implementing against the solver tier found
tier: 30-solver
owns: [defect and observation record for documents 31-36]
---

# What implementing against the solver tier found

Defects, deferrals and observations from implementing against `31`–`36`. The rule and its reasoning
are in [`08-implementation-sequence`](../08-implementation-sequence.md).

**No solver has been built.** What has happened is that four packages in tier 20 — `P3.0` through
`P3.4a` — were written *against* this tier's contracts without implementing any of them: the component
interface waits on `31` for the shape of `SolveContext`, the residual functions implement `36`'s
smoothing constants, and the graph exists to be assembled by `32`. Everything below was found from
that side. **The absence of an entry about `33`, `34` or `35` means nothing has looked**, not that
nothing is wrong.

This file was started late. `P3.0`–`P3.4a` should each have appended to it as they went, and instead
the findings sat in [`20-core-domain/defects.md`](../20-core-domain/defects.md) where the tier that
owns them would not have seen them. The entries below are that backlog, written up in one pass.

**Next id: `S-89`.** The columns, their vocabularies, and the rule for filing, reopening and closing
are in [`08`](../08-implementation-sequence.md) under *Every package writes down what it found*.

## Open

| # | Filed | Effort | Risk | Basis | Document | What | Why it is still open |
|---|---|---|---|---|---|---|---|
| S-88 | 2026-09-25 | small | med | measured | [`32`](32-steady-state-newton.md), [`24`](../20-core-domain/24-auto-sizing.md) | **`CommonReturn` rates a mixing ring's source from the one load that states a duty, so a ring whose other loads state only temperatures seeds at 1.5× its flow** | Found re-measuring `S-69` in the bookkeeping review (2026-09-24), on the plain `step-08c-header-series-four.fluid`. What the seed meant: a source whose return is not stated is rated over its supply and the return its loads hand back (`BranchFlows.CommonReturn`). What it does: it gathers returns only from loads with a negative `Power`; the AHU, floor and DHW coils state `in`/`out` and no duty (theirs are promoted, so they live in the solution), so only `HE_RAD`'s stated 40 °C is found and `HS1` is rated over 60/40. The real return is 30 °C, after the DHW block. Physically the radiators take 20 kW at 50/40 mixed half and half, so the ring carries 0.239 kg/s and each coil 0.478; the solved iterate agrees (`TV_RAD.a` −0.2396, coils 0.4796/0.4795/0.4795). The seed gives the ring 0.3589 (`Duty(HS1)`) and each coil 0.7177. This is also why `S-69`'s 2026-09-21 note called 0.717 kg/s per coil "the first pass's honest problem": the flow was the seed's. | Open: `CommonReturn` should decline when any load its water reaches has no rating, leaving the ring to the next rule. Not yet tested in isolation: stating `HS1 in.t=30` seeds exactly (ring 0.2393, coils 0.4785) but still fails `NonFinite` after 5 iterations, and the statement also moves the promotions (`HS1.out` takes `PU_RAD.head`, `HE_RAD.out` takes `PU_AHU.head`), so it does not isolate the seed. Whether 8c then settles is `S-69`'s question. |
| S-86 | 2026-09-23 | medium | med | measured | [`32`](32-steady-state-newton.md), [`34`](34-controllers.md), [`24`](../20-core-domain/24-auto-sizing.md) | **The flow seed never reads a controller's setpoint, so a loop whose only design temperature is a setpoint seeds at the nominal 0.1 kg/s -- and a ring with one node, backwards** | Found writing the ladder's controlled steps (P6.10, 2026-09-23). What `34` meant (P6.0): a `control` line's setpoint is the node's design temperature -- lowering writes it into the node's stated parameters and the design solve holds it with the named actuator, *in place of* the same temperature stated on an exchanger. What the seed does: `BranchFlows.Duty` rates a branch from its exchanger's own stated terminals and `MixingFraction` partitions a three-way valve from its coil's stated inlet; neither looks at a setpoint on the node next to it. Step 5 with its controls (`HE1` states `power=30 in.t=20`, the secondary valve `CV1` holds `NS.t = 50` in place of `out.t=50`): the constraint is well formed (`NodeTemperature on NS.t -> solved for as CV1.position`), but the secondary ring -- one branch from `NS` back to `NS` -- seeds `Nominal()` at **-0.1 kg/s**, against its pump, and 30 kW taken from 0.1 kg/s put `LOAD__CV1` out of the property domain before the first step (`FS3007` after 0 steps). Physically the ring carries 30 kW over 20 -> 50, 0.239 kg/s forward. The same in step 11a with its controls (`CV_H` holds the heating supply in place of `HS_H`'s `out.t`: `LOAD__CV_H`, 0 steps) and in step 8c (the four coils' inlets held by their valves: the AHU, floor and DHW coils, which state no duty, lose the three-way partition their stated inlets gave -- all six of their legs seed `Nominal()` where the plain step's are `Partitioned` -- and the solve leaves the domain at `NM_RAD` after 1 step with `TV_DHW` on its stop; the plain 8c already stalls on `S-69`, which this step would meet next). The same scripts with the temperature stated on the exchanger solve, the controller then noted as `FS3210`. | Open: the seed should read a controlled node's setpoint wherever it reads a stated terminal -- `Duty`'s inlet or outlet, `UpstreamOutlet`, `MixingFraction` -- and orient a one-node ring by its pump. Steps 5, 8c and 11a with their controls carry `# does not settle: S-86`. **2026-09-24:** step 12b (the zones with their controllers, `S-85`'s second script) now fails here, not at `S-85`: its three returns are held at 40 C by the zone valves and `HE1` states no inlet, so every branch seeds `Nominal()`, the sizing sizes `PU1` to 0.08 m at 0.3 kg/s, and Newton hits its cap at 50 iterations. Its marker names `S-86`. With the setpoint read as the return, `BranchFlows.DesignFlow` would rate the zones as it does step 12's. **Re-checked 2026-09-24 (the bookkeeping review):** valid as written, with one count corrected: in step 8c with its controls the AHU, floor and DHW blocks seed **nine** `Nominal()` legs, three each, not six. The same review saw the report's seed-basis column label a leg `Nominal()` when `BranchFlows.Refine` gave its value, which makes a refined seed read as an unrated one; worth fixing when the seed learns setpoints, since both are about what a leg's basis says. **2026-09-25 (P6.11 slice 3d):** `19`'s language 2 reference script meets it too, now that it binds clean: `TC1` holds `TE1_node` at 60 C (38.9 C in the mild case) with `TV1`, the heating loop's branch through `SP` seeds `Nominal()` at -0.2 kg/s against its pump, and both cases end `NonFinite` after 2 iterations (`FS3007` at `NPR` in winter, at `NR__HX1__out` in mild; `FS2301`). Not a language 2 fault: the translation hands the binder language 1's `control` line, and the setpoint reaches lowering per case as it should. **2026-09-25 (P6.11 package 4):** `samples/v2-syntax-tour.fluid`, the language 2 tour, is the same plant and carries `# does not settle: S-86`; measured through the compile endpoint once package 4 let the curves reach the solve, it ends as the reference script does (`FS3007` at `NPR`, the heating ring seeded at -0.2 kg/s against its pump). |
| S-85 | 2026-09-23 | medium | low | measured | [`32`](32-steady-state-newton.md), [`24`](../20-core-domain/24-auto-sizing.md) | **At a plain split, the seed hands every undetermined branch the whole neighbouring flow, so mass balance runs one branch backwards and a parallel zone plant never solves** | Found holding the pending three-zone script (`Layout/Parity/step-12-zones.fluid`, P6.10) to a ladder step's solve gate (2026-09-23). What the seed meant: a branch with no duty of its own takes the largest known flow at a junction it meets (`BranchFlows.Propagate`), and `SolutionSeed.Field` then solves the tree by mass balance, so every branch starts near its answer and in its direction. What it did: at `NA2` zones 2 and 3 were each seeded at 0.478 kg/s -- the whole plant's flow, from `HE1`'s 60 kW over 40/70 -- and so was the header link `NA1 -> NA2`; kept as chords, they left `NA1` short by 0.478 kg/s, and the tree took it from zone 1, seeded **backwards** at -0.478 kg/s (return to supply, through its valve and its 20 kW load). Physically three equal zones on equal Kv 4 valves carry about 0.159 kg/s each, all forward. Newton kept zone 1 near -0.02 kg/s, `LD1` pulled `CV1__LD1` to 4 C, and the solve ended `NonFinite` after 2 iterations. It is `S-53`'s mechanism: that row closed the copy on a three-way valve's legs (`PropagateThreeWay`), and a plain junction still copies. **Measured:** handing each undetermined branch an even share instead (`best / open`) converges the zone plant in 3 iterations with an exact heat balance (zones 0.184, 0.147, 0.147 kg/s, all forward) -- but the sizing loop then does not settle in 10 passes (`FS2301` on `CV1-3.authority` and `LD1-3.flow`), and the same change breaks `OuterLoopTests.TwoPumpedSourcesShareALoad...`, whose `N7` seeds out of the property domain before the first iteration (`FS3007` after 0 steps). So an even share is not the rule either. | Open: a split's undetermined branches need a mass-consistent share -- what enters the junction on its determined branches, less what leaves on them, over the open ones, oriented by the flow -- tested against the two-source plant and the zones together; then the zones' sizing loop, which may be a second defect behind this one. Both zone scripts carry `# does not settle: S-85` until then. **Worked 2026-09-24; the row's script now settles, the mechanism stays open.** (1) A mass-consistent share *with* direction -- each determined branch's direction read from its pump or a stated exchanger terminal, the open legs sharing the junction's net and oriented to close it -- seeded the zones 0.239 / 0.120 / 0.120 kg/s forward and Newton converged in 3 iterations, but broke the two-source plant: the copy rule still ran at the junctions the share could not read, the two handed out inconsistent magnitudes, and the tree ran the DHW branch backwards against `PU_DHW` (-0.090 kg/s, `N7.h` 0, `FS3007` after 0 steps). Reverted. (2) The zones' sizing loop, measured: a load with only a duty has its design flow sized from the flow the last pass found, and the three zones approach their equal split by 20 / (20 + 2.06 kPa valve) = 0.91 per pass -- from the share's 0.184/0.147/0.147 not settled in 10 passes, about fifty needed. So the start matters, and the right start is the answer: (3) **built** -- a load stating its duty and no temperature is rated at its sources' design temperatures when every source its water reaches states both and they agree (`BranchFlows.DesignFlow`, `32`), yielding to any rating from temperatures stated on its branch. Step 12: all zones seeded 0.1594 kg/s, `Duty(LD1-3)`, converged in 0 iterations, settled in 2 passes; its marker is gone. Step 12b's returns are setpoints and no source states an inlet, so nothing rates there -- that is `S-86`, and its marker now names it. No other script, sample or golden moved (Core 2430/0/3, Api 60/0). **Still open:** the copy at a split whose legs have nothing to rate -- no script in the tree hits it now, and a fix must pass the two-source plant and the zones together. **Regression found and fixed the same day** (the bookkeeping review, 2026-09-24): `DesignFlow` also rated the propane heat pump's evaporator (`HPE chiller power=91.84`, no temperatures) at its cooling coil's 7/12, on a circuit fed by the bores' 10 °C water; `hp-propane-heating.fluid` went from converged in 1 iteration to `IterationCap` at 50 with `TVC.position` on its stop. The rule now applies only to a closed circuit (no boundary node on the load's water). Measured: both propane cases converge in 1 iteration again, step 12 still settles in 2 passes, and `SolutionSeedTests` asserts an open circuit's load is not rated. `heat-pump-recovery.fluid` is `Singular` with or without the rule -- not this. **Re-checked 2026-09-24 (the bookkeeping review):** the headline no longer describes the tree: the zone plant (step 12) solves in 0 iterations and 2 passes. What stays open is only the copy in `BranchFlows.Propagate` at a split whose legs have nothing to rate, and no ladder report shows a `Propagated(...)` seed that is not mass-consistent or does not converge. The residue moves to *Observations* if no script turns it up by P6.3's close. |
| S-82 | 2026-09-23 | medium | med | measured | [`32`](32-steady-state-newton.md), [`34`](34-controllers.md), `D-02` | **A promoted parameter held at its bound keeps the stated target that promoted it, so a plant whose valve really does saturate in one case is singular instead of solved with the target unmet** | Found building the user's simultaneous heating-and-cooling heat pump (2026-09-23): a condenser loop and an evaporator loop, each with a three-way valve that diverts to a ground-bore boundary at 10 °C, two scenarios. In the heating-priority case the machine's 120 kW condenser duty equals the 120 kW heating load, so the heating side needs **no** flow to the bores and its valve belongs on its stop — the physically right answer. A three-port body is never tight (`D-135`): at the Kv 160 it was sized to, 2 % leakage passes about 0.49 kg/s, which carries some 70 kW to the bores. So the stated supply `in.t=45` cannot be held even fully shut. `EquationSystem.Project` holds `TVH.position` at 1 and reports `FS3008`, but the stated temperature stays in the system with nothing left to answer it, and Newton ends `Singular` (rank 39 of 40; `FS3009` on the bore inlet's enthalpy once the leak runs backwards through it). The cooling-priority case is the mirror on the evaporator side. **Measured** that saturation is the only blocker: with duties that make both sides trade with the bores in both cases (heating 100/40 kW, cooling 40/70 kW), the cooling-priority case converges in 8 iterations and balances to 0 kW — 80 kW to the bores at 0.547 kg/s, 45 → 10 °C — while the heating-priority case, whose heating side then needs 0.137 kg/s against a leak of about 0.25 kg/s, is singular the same way. **What a real plant does is what the model cannot say**: the valve sits on its stop and the supply settles a little under 45 °C. D-02 makes a stated value a constraint, and a constraint has no "unless it cannot be met"; `34`'s steady controllers are post-v1. | Open: needs a decision, since it changes what a stated value means at a bound. One reading is that a saturated promotion releases its target, which becomes an unmet setpoint in the report (the value reached against the value stated) instead of an equation — the complementarity a controller's anti-windup expresses in the transient. That is a solver change on the Newton path and a new `D-`, not a patch. Until then a plant whose control valve saturates in any case cannot be written as a scenario file. **The user's verdict (2026-09-23), recorded in `09` that day and brought here 2026-09-25:** the failure is the right answer -- the file stated the machine's duty and the load equal, and a shut three-port body leaks, so no steady state holds the supply. Release on saturation is not wanted; the prototype is shelved outside the repository (`diagnostics/scratch/s82-saturation-wip.patch`, gitignored). The "needs a decision" above is therefore answered. **Re-checked 2026-09-24 (the bookkeeping review):** the heating case at equal duties (`HPC power=120`, `HL power=120 in.t=45`) ends `Singular` after 11 iterations at rank 39 of 40 with `FS3008` holding `TVH.position` at 1, and also emits `FS3002`, `FS3009` and `FS3010`, which send the user looking for a missing datum and an undetermined enthalpy. What is open: one diagnostic that says the stated `in.t` cannot be held with the valve on its stop, in place of the rank messages. |
| S-81 | 2026-09-22 | medium | med | measured | [`62`](../60-docs-and-devex/62-testing-strategy.md) V17 | **V17's refinement half compares the solver against itself, because the independent reference table `62` promises does not exist** | `62` says the V17 reference is "generated by an independently reviewed control-volume calculation, not by the production solver". P6.2 shipped the convergence assertion as *the drawn temperature moves less at each doubling of `layers`*, which catches a scheme that diverges and does not catch one that converges to the wrong answer — the exact failure an independent table exists to catch. Writing the table is a plug-displacement calculation over a stated charge and draw, reviewed and committed as data; until then V17's first half (the remix) is real evidence and its second half is a smoke test |
| S-77 | 2026-09-22 | medium | med | measured | [`33`](33-transient-time-domain.md) §Disturbances | **A schedule cannot move a boundary state, and the district-supply scenario `33` names as the only weather route needs exactly that** | Found building P6.1: `EquationSystem.Schedule` writes the table a component's residual reads at solve time (`Resolvable`: an exchanger's `power`, a valve's `position`/`kv`, a pump's `head`); a boundary's `t`, `p` or `flow` is folded into the flux tuple at assembly and a node resolves nothing, so `at 60 s N1.t = 10` bound, lowered, and would have been silently ignored. `FS3105` now refuses it, naming what the kind can move. Closing it is a boundary flux whose stated enthalpy and magnitude are re-evaluated per step — a state fix per scheduled boundary per evaluation, outside the Newton loop — and the same seam a scheduled ambient (`S-18`) will need |
| S-78 | 2026-09-22 | small | low | measured | [`33`](33-transient-time-domain.md) §Error cases | **`FS3102`, `FS3106` and `FS3107` are built and no test provokes them** | The run's exits: the step under its floor, drift past 1 %, an invariant failure at 5 % or a non-finite state. `FS3103` is provoked (a gigawatt scheduled into the loop leaves the property domain), and the descriptor test names the rest; a provoking test needs a run that integrates wrongly on purpose — a deliberately mis-scaled derivative behind a test seam, or a state driven non-finite — which is the kind of seam worth adding with the controllers, whose saturation and windup are the likelier real triggers |
| S-76 | 2026-09-22 | big | high | hunch | [`33`](33-transient-time-domain.md), `D-141` | **A cold start — every volume at a stated temperature, pumps off, a schedule ramping them — is the user's commissioning scenario and has no statement** | The user's hunch (2026-09-22), accepted as an opt-in by `D-141`, which names the blockers: no ambient (`S-18`); at zero flow every zero-volume node's balance `ṁ(h_up − h) = 0` leaves `h` undetermined and the algebraic system singular, so a stopped pump in a closed loop needs a defined zero-flow state; a pump-off model with a check valve, else the primary boundaries drive reverse flow through it (`FS3015`'s territory); and a demo whose step waits for the ramp to settle. Not before P6.7 |
| S-27 | 2026-09-07 | big | high | hunch | [`32`](32-steady-state-newton.md), `D-78` | **A two-phase node's enthalpy column is carried by one equation, and nothing has measured whether it is enough** | `(P, h)` fixes a two-phase state exactly, which is why enthalpy rather than temperature was the right node unknown -- but inside the dome `dT/dh = 0` **exactly**, so every residual that reads a temperature at that node contributes nothing to its enthalpy column. The column is carried by the evaporator's own energy balance alone, whose coefficient in `h` is 1. That is almost certainly sufficient and it is exactly the `S-21`/`S-25` shape this project has now hit three times, each time after arguing it away first. Open until a Jacobian probe on a real refrigerant loop says so; the argument is not the evidence. Note also that a `sqrt(eps)` perturbation (~6 mJ/kg on water at 100 C) cannot itself cross a phase boundary from a state well inside a region -- the hazard is the vanishing derivative, not a chord across the discontinuity. |
| S-4 | 2026-09-03 | medium | med | measured | [`36`](36-numerics-and-convergence.md), [`07`](../00-foundation/07-quality-attributes.md) | **A property-backend call can never return, and nothing has a cut-off** | Measured, not theorised: `Water + Ethanol 60/40` flashed at `(h, s)` ran past a 600 s timeout with no output and no progress. `36` covers a *solver* that does not converge and says nothing about a *backend* that does not — and a flash is itself an iteration, inside the residual, inside the Newton step. `D-22`'s stop requirement ("any isolation breach stops the run at its last verified frame") has no mechanism here: a hung flash is not a breach, it is a call that never comes back, and cancellation cannot reach a native frame. The performance harness works around it with a background thread and a five-second join, which is a diagnostic's answer and not a solver's. **Re-checked 2026-09-24 (the bookkeeping review):** the measured hang -- a water-ethanol HEOS mixture at `(h, s)` -- is not reachable from a script: production calls water on IF97 (`D-137`), humid air through `HAPropsSI`, and pure refrigerants on `(p, ·)`, and `BackendPairDiagnostics` documents the mixture as a test-only probe. The gap stands: no native call has a cut-off, and nothing has measured whether a production pair can hang, for example a refrigerant near its critical point. |
| S-17 | 2026-09-04 | small | low | hunch | [`33`](33-transient-time-domain.md) | **Quasi-static pressure is justified by the wrong quantity, and the right one nearly fails for air** | `33` argues the split from acoustic transit: 1500 m/s in water, milliseconds across a plant. Sound speed in air is **343 m/s** — only 4.4× slower — so a 50 m duct is 146 ms and transit time would clear air comfortably. It is not the criterion. What decides whether pressure can be treated as instantaneous is **pneumatic capacitance**: from `dp = a²dρ`, `C = V/a²` and `τ = (V/a²)(2Δp/ṁ)` across a square-law resistance. A 1 m³ hydronic loop at 200 kPa and 5 kg/s gives **0.036 s** and the conclusion holds by one to two orders; a 100 m³ AHU and duct system gives **0.34 s** at design flow and **1.7 s** throttled to 0.5 kg/s, which is the same order as the timestep and the frame interval. The physical point is not that pressure signals travel slower in air — it is that **air stores mass and water essentially does not**. The split is still right for v1; the argument for it is not, and the air-side limit is currently unstated and unguarded. **2026-09-24:** the argument is corrected and the air-side limit stated in `33` (the capacitance table, *air stores mass and water essentially does not*); what is left is the guard, which `33` says needs a node volume and a port's speed of sound first -- neither exists, so this is no longer a small documentation row but one waiting on those two. |
| S-18 | 2026-09-04 | big | med | hunch | [`33`](33-transient-time-domain.md), [`12`](../10-language/12-grammar.md) | **A schedule has no ambient to drive, so an outdoor-temperature scenario cannot be written at all** | `schedule` sets `component.parameter`, and there is no ambient temperature in the model to be a target. `33` also states that v1 has no ambient loss and no wall conduction, so even given one, no component would read it. The only path an outdoor condition has into a model today is a boundary node's stated temperature — which then transports downstream correctly, but is a district-supply scenario rather than a weather one. The gap is small and structural rather than deep: an ambient scalar the schedule can reach, plus `−UA(T̄ − T_amb)` contributed by the pipe. That term already has its home — it is `D-69`'s flux member, which is on `IFlowComponent` rather than on the exchanger for exactly this reason — so the component half costs nothing. Recorded because "outdoor temperature drops and the front reaches the air handling unit late" is the first transient a user will try to write, and nothing today says why they cannot. **Re-checked 2026-09-24 (the bookkeeping review):** half closed by `D-149`: an outdoor curve of time now drives any parameter a run can move, so "the outdoor temperature drops and the load rises" can be written (`CurveClockTests`: `HE1`'s duty 30 → 15 kW as the outdoor air goes −26 → −6 °C). Still missing is the ambient as a heat sink -- no component reads it, so "the front reaches the AHU late because the pipes lose heat" cannot be written -- and a scheduled boundary state (`S-77`). `33`'s paragraph now says the same. |
| S-19 | 2026-09-04 | small | low | hunch | [`33`](33-transient-time-domain.md) | **The `nodes=` guidance inverts on the air side, and the CFL limit gets 24× tighter for a shorter delay** | The step limit is the residence time `Vρ/ṁ`, and air is ~800× less dense than water. `33`'s DN20 water cell — 0.0925 l, 988 kg/m³, 0.0763 kg/s — gives τ = 1.20 s and a step of 1.08 s. A Ø315 duct cell of the same 0.25 m length holds 19.5 l at 1.2 kg/m³ and 0.467 kg/s: τ = **0.050 s**, a step of **0.045 s**. Same `nodes=`, **24× more steps** — roughly 13 000 over a 600 s horizon against 1 400, and two Newton solves each. The payoff moves the other way: a front crosses 8 m of duct at 5 m/s in **1.6 s**, so duct transport is barely visible while the air handling unit's thermal mass, which v1 does not model, dominates the response. So the document's advice — that `nodes=` is the single most important modelling decision in a transient run — is true on the water side and close to backwards on the air side, and `FS3101` will fire on every air-side model. **2026-09-24:** the analysis is in `33` (*On the air side that advice inverts*) and the users' half is written (`docs/advanced/discretized-pipes.md`: *Keep ducts coarse*). What is left is `FS3101` firing on every air-side model, which waits on an air-side flow component -- none is registered yet. |
| S-69 | 2026-09-19 | medium | high | measured | [`32`](32-steady-state-newton.md), [`24`](../20-core-domain/24-auto-sizing.md), [`23`](../20-core-domain/23-topology-and-graph.md), [`29`](../20-core-domain/29-layout-ladder.md) | **A ring of injection blocks whose loads state only `in`/`out` puts every block's coil on the one promoted pump, and the first pass leaves the domain in a step** | What is left of `S-68` once its seed and sizing defects closed (2026-09-19). The ladder's `step-08c-header-series-four.fluid`: four blocks in series on one ring, only the radiators rated, the AHU, floor and DHW written as `in`/`out` with no `power`, every valve's Kv the band rule's 6.3 (`D-122`) and every mixing point real (`in` half way between the feed and the return; the script's first form had `in` *equal* to the feed, which asks each valve to sit exactly on its open bound where its `in` constraint has no sensitivity, and that was the script's error, corrected with the marker). What the count does with three unrated loads: `CloseEnergyBalance` can size one omitted duty from the total and not three, so each `out` promotes its coil's `power` (the thermal answer) and every pump but the radiators' stays a rule's choice. What the physics then says: with `in` half way, each coil carries twice the ring's stream, 0.478 kg/s against a coil rated 20 kPa at the 0.259 the seed gave it, so 68 kPa; the block's own pump was sized to 2.05 m against its 20 kPa, and the only free head is `PU_RAD`'s, which must therefore push the whole ring through four coils -- the first pass's honest answer is a radiator pump near 15 m and three pumps that are then re-sized on the second pass. What Newton does from the seed: the first step sends every position to 0, `PU_RAD.head` to 0 and the powers to tens of watts, and the second leaves the domain at `NM_RAD`; the Jacobian's pivot ratio at that iterate is 2.2e-7. The seed's own rows are not innocent either: on this ring as on 8b and 8e the temperature walk puts every coil's outlet 4 K below its stated `out` and the junction after it 2 K above (`NM_RAD` 36 for 40, `N4` 42), the datum's reset in the pressure walk leaves 33 kPa across `HS1` (the walk starts at the first branch's valve, not at the anchor it found), and a promoted `power` seeds at 0 W against a temperature field that already assumes its duty. Each is a few tenths of scaled residual that 8b and 8e absorb; here they sit on a first-pass graph whose answer is far from the seed. What to try, in order: promote the pump head rather than the power where the block's coil flow is fixed by its neighbours (a series ring fixes every block's `a` flow), and seed a promoted power from the stated `in`/`out` at the seeded flow. Starting the pressure walk at the anchor was tried under `D-122` and withdrawn: it moved the closure error onto whichever valve leg the tree closed through and the headers stopped converging; the walk now crosses a promoted pump's branch last so the error falls across the pump, which left this ring where it was (`NonFinite` after one step, residual 2.6). The script carries `# does not settle: S-69` and the solve gate expects the stall; the corpus and every other ladder step settle. **Re-measured 2026-09-21 after `D-133` and the seed rules `S-73` brought** (release-equivalent debug build): the promotion is unchanged -- each unrated coil's `out` still takes its own `power`, which is right, since the flow is what the mixing fixes and the duty is what the outlet then determines. The seed is now what the row asked for: `BranchFlows.Refine` hands each block's feed leg the ring flow the closed field found (0.3587 kg/s), the stated `in`/`out` partition the coil at 0.717 and the recirculating leg at 0.359, and the promoted powers seed at -6.0/-4.5/-4.5 kW instead of 0; the seed's scaled residual fell from 1.52 to 0.185. The solve still fails, and differently: pass 1 now runs 13 iterations with `PU_RAD.head` walking to 0 and every position to a bound, pass 2 sizes the three block pumps from that iterate at 0.91 m shut-off (their coils' `flow` at 0.478, `loop drop 8.9 kPa`) and goes non-finite after 16. The physics the seed now states -- each block's coil at 0.717 kg/s through a coil rated 20 kPa at 0.2587, so 150 kPa per block for a pump sized to 2.05 m -- is the first pass's honest first-pass problem: the block pumps are rule-sized for a flow the mixing does not give them, and only `PU_RAD` is free. What is left is the sizing passes on a ring rather than the seed: the coil's `flow` rule should take the partitioned flow (it took `Nominal` 0.2587 in pass 1 and the failed iterate's 0.478 in pass 2), and a pass that fails should not size the next from its last iterate. **Re-checked 2026-09-24 (the bookkeeping review):** the plain 8c now fails `NonFinite` after 14 iterations of pass 2 (13 + 14, residual 0.2477, `FS3007` at `NM_RAD`), not 16; pass 2 still sizes `PU_AHU`, `PU_FLR` and `PU_DHW` at 0.91 m with an 8.9 kPa loop drop. The seed's ring flow is 1.5× the physical one, and the 2026-09-21 note above is wrong to call 0.717 kg/s per coil the first pass's honest problem: that is `S-88`, filed separately because its mechanism is `CommonReturn`'s. This row keeps the sizing-pass half -- the coil's `flow` rule reading the partitioned flow, and a failed pass not sizing the next. |

## Traps

What a session working against these documents gets wrong first. Promoted from *Observations*
or from a closed entry when a session hits it a second time; read before working in this tier.

- The stagnant rule (`Stagnant()`) anchors enthalpies only inside a branch pinned at zero mass flow. A `vflow` row has a zero *mass* target too and is not a pin — guard on `VolumeNode < 0`, or the whole loop anchors at 20 °C and the duty vanishes (S-72).
- A stated pump `flow` with a stated `head` is a curve duty point, not a constraint; without `head` it is a `FixedFlow` row answered by the pump's own head (S-72).

## Closed

| # | Filed | Closed | Effort | Document | What was wrong | What changed |
|---|---|---|---|---|---|---|
| S-64 | 2026-09-17 | 2026-09-17 | small | [`32`](32-steady-state-newton.md), [`36`](36-numerics-and-convergence.md), [`29`](../20-core-domain/29-layout-ladder.md) | **A free boundary's seeded flux was the fragment's largest estimate, so the branch that closed its balance ran the syntax tour's rated `HE2` backwards** | Closed by `D-115`: a boundary has one connection, so the tour writes `NB1 - NJ1` and `NJ2 - NB2`, the seed runs `HE2` forwards, the marker is off and `SolutionSeedTests.TheSeedRunsARatedExchangerFromItsInletTowardsItsOutlet` holds it over every sample. The row said so on 2026-09-17 and was left in the Open table; moved by the bookkeeping review (2026-09-24), which found no sample, step or variant where the closure runs a rated exchanger backwards. The flux rule itself is unchanged -- see *Observations*. |
| S-87 | 2026-09-23 | 2026-09-24 | small | [`34`](34-controllers.md), [`32`](32-steady-state-newton.md) | **A pump named to hold a temperature in the design solve made the circuit singular**: `PU1.speed` was promoted for `NR.t = 20`, and at the design point speed moves nothing (`FS3009`, `FS3010`) | `D-163` (the user's option (a) of two): the binding promotes the pump's `head` in the design solve and leaves `speed` to the run, starting at 1; a pump stating `head`, `dp`, `flow` or `vflow` has no head left and the setpoint is `FS3210` with that reason. The row's own proposal, `FS3210` alone, was built and reverted first: the note was right and the loop had no design return, `NR` out of the domain after 0 steps. Measured on the row's script, step 3 with its controls: converged in 1 iteration, settled in 2 passes, `NR` 20 °C, 0.2392 kg/s, `PU1.head` 4.09 m; its marker is gone. `SetpointTests` pins both halves. |
| S-84 | 2026-09-23 | 2026-09-24 | small | [`32`](32-steady-state-newton.md), [`62`](../60-docs-and-devex/62-testing-strategy.md) | **The solve report's heat balance left out a promoted duty**, so `HPC heater out.t=45`, converged at 149.1 kW with every energy row at 1e-11 W, read `sources +0 kW … net -149.105 kW` | `HeatBalance` reads a promoted `power` from the solution (`SolvedStates.Parameter`) and marks the entry `solved`. Measured on the row's own script (`AMachineHoldingItsLeavingTemperatureMakesUpWhatItsShutValveLeaks`): `HPC +149.105 solved`, net `+0 kW`, asserted. |
| S-80 | 2026-09-22 | 2026-09-24 | small | [`33`](33-transient-time-domain.md) §Stratified tank | **The interface-flow formula was built and only its zero branch exercised** -- the storage header matches every layer's inflow with an outflow | `PinnedViewTests.EachInterfaceCarriesTheLayerItsFlowLeavesIntoTheNext`: a three-layer tank at 30/40/50 C crossed by 0.1 kg/s of 60 C water, entering the bottom by `in` (each interface +0.1 kg/s, each layer gaining 0.1 x (h below - h own)) and entering the top by the `out` port run backwards (-0.1 kg/s, each layer gaining 0.1 x (h above - h own)). Both branches match the hand products to 0.5 W; no product code changed. |
| S-79 | 2026-09-22 | 2026-09-23 | medium | [`33`](33-transient-time-domain.md), [`15`](../10-language/15-semantic-model.md), `D-58`, `D-94`, `D-149` | **The live-curve half P3.8 moved to P6.1 had no statement of where t = 0 sits on the timestamp axis, and P6.1 shipped without it** | What it meant: a dynamic circuit reads a curve of time "at the current time" (`15`). What it did: the binder deferred the reference and nothing ever read it again, so a run held every curve-driven parameter at its design value and a time curve's rows were decoration. The user chose the reading (2026-09-23): t = 0 is an instant the script states, not the earliest row of the earliest curve, because the second moves every curve when one is added. **`D-149`:** `project dynamic plant_01 start="2026-01-15T06:00:00"`, quoted because a date unquoted is a subtraction, read by the curve rows' own reader. `CurveClock` on the graph holds each parameter whose curve chain reaches `time` and evaluates its expression at `start + t` with the binder's evaluator (`D-14`'s bare number included -- the first build read `heating`'s 30 as dimensionless and refused it); `TransientSolver.Follow` writes it through `EquationSystem.Schedule`, so `FS3105`/`FS3109` apply; steps land on curve rows. `FS1545` (unreadable start, error), `FS1546` (clock read with no start, warning on each reader), `FS1547` (start with nothing dynamic). **Measured** on the demand-step loop with outdoor −26 → −6 °C over five minutes and `heating` 30 → 15 kW: the rise across HE1 at 0.2393 kg/s is 30.0 K at t = 0, 22.5 K at 150 s and 15.0 K at the 1 800 s horizon, and the last frame matches `SteadyAt` there. **Found and fixed on the way:** a `schedule` line on a role-word duty was written unsigned, so `HL.power = 45` on a `load` handed the run +45 kW, a heater; `ExchangerRoles.Duty` is now the one home for `D-91`'s sign, used by lowering, the schedule and the clock. Left open in `33`: whether t = 0 should be the steady plant at the start's own conditions rather than the design state (`D-141`). |
| S-83 | 2026-09-23 | 2026-09-23 | small | [`32`](32-steady-state-newton.md), [`24`](../20-core-domain/24-auto-sizing.md) | **A machine that holds its own leaving temperature, duty free, left the load it feeds with no inlet to rate against, so the loop was seeded at the nominal 0.1 kg/s and Newton failed before it started** | What the seed meant to do: start every branch at the flow its stated duty and temperatures imply (`24` step 1). A heat pump, chiller or boiler with leaving-water control is written `HPC heater out.t=45`, duty unstated, feeding `HL load power=120 out.t=40`; `BranchFlows.Duty` rated HL only from HL's own `in`, or for a source from the common return of opposing loads, so HL got nothing and every heating branch started at 0.1 kg/s against 5.74. The first step took `HPC.power` to −6.07 kW and the solve was `Singular` at iteration 1. **Changed:** `BranchFlows.UpstreamOutlet` walks from an exchanger's inlet against the written direction across what leaves the temperature alone — two-port elements that are not exchangers, two-way nodes, a three-way valve entered by `a` or `b` — to the outlet stated upstream, and `Duty` reads it before the common return. The common return itself now reads only loads in the source's own hydraulic partition: a chilled coil warms its water, so it reads as a source, and it had taken the heating loop's 40 °C as the inlet of a 7/12 stream (0.85 kg/s where 100 kW needs 4.77). Circuit is the wrong filter — the distribution header's loads are subcircuits of another name on the same water, and three header tests failed on it. **Measured:** both priority cases of the propane heat pump (`diagnostics/scratch/hp-propane-*.fluid`) converge in 1 iteration on their final pass, 3.5e-12 and 3.6e-13; without the walk they return to `Singular` and `NonFinite`. The row's second proposal, seeding a promoted duty from the seeded inlet temperature instead of 0, was built and **measured to do nothing**: the same iteration counts on every pass, from a start of 240 kW against a 162 kW answer, so it was removed. Three tests, one of which fails without the partition filter. |
| S-75 | 2026-09-22 | 2026-09-22 | medium | [`34`](34-controllers.md), [`23`](../20-core-domain/23-topology-and-graph.md) | **The demand-step loop as `01` writes it does not solve at t = 0.** The control line's setpoint played no part, the valve defaulted to fully open, and `HE1.out.t`'s promotion drove `PU1.head` to 62 m and Newton non-finite in three iterations | P6.0, `D-141`: lowering writes a setpoint on an unstated actuator into the measured node's stated `t` and records every binding on `CircuitGraph.Setpoints`; `WellPosedness` answers that node-temperature constraint with the named actuator alone and raises `FS3210` (actuator stated, node states its own `t`, or a neighbouring terminal already fixes it -- the ladder's step 10, where the sensor sits on `HE1`'s outlet and applying the setpoint went non-finite) and `FS3211` (not a node temperature). The sample converges in one iteration on `01`'s figures -- `N2` 20.00 °C, 0.2393 kg/s secondary, 0.0763 recirculating, the valve at 0.501, 2.55 m -- and is in `CorpusStatusTests`. The syntax tour counts over-specified by 5 rather than 4: its setpoint's actuator is already `HE3.in.t`'s, which the tour states too. Not held yet: a measurement that is not a node temperature (`FS3211` says so) |
| S-37 | 2026-09-08 | 2026-09-22 | medium | [`32`](32-steady-state-newton.md), [`36`](36-numerics-and-convergence.md), [`23`](../20-core-domain/23-topology-and-graph.md), [`24`](../20-core-domain/24-auto-sizing.md) | **Two pumps in series have one head between them and nothing says how it divides.** A ring with no pump of its own, its two consumer blocks in series, passes one stream through both pumps; the ring's loop equation fixes only the sum of what each block adds, and the split -- with the valve positions that follow -- moved a metre under a 1e-4 change in the water formulation with every flow and Kv unchanged | `D-142`: no new language, a diagnostic. Two outside analyses the user brought answered the parallel manifold, where nothing is shared and the built code is full rank at 44; the ladder's series ring is the shape both name as undetermined. Measured: the weakest direction at the series header's answer is `PU_RAD.head +1, PU_AHU.head −0.73, TV_AHU.position −0.36`; unique answers run a pivot ratio of 8e-3 and above, the series headers 3e-5 and below; stating one head over-specifies its block (`FS2210`) and a balancing valve added to take the flow joins the valley at 9e-6. `FS3016` (warning) names the sizes sharing the direction under `jacobian.valley_tol` = 1e-3, reading the direction rather than the pivot so the ladder's step 5 (6e-6, one flow through a tiny valve) stays silent; the report prints the direction. The booster on a manifold is the same defect and the same diagnostic once it converges; `S-36` (the exact-singular case names the wrong pump) stays open. Whether a ring pump closes the valley is untested |
| S-42 | 2026-09-08 | 2026-09-22 | small | [`32`](32-steady-state-newton.md), [`62`](../60-docs-and-devex/62-testing-strategy.md) | **`FS3009` and `FS3010` have no end-to-end subject left in the corpus, and the gap has widened** | The row wanted a circuit that is square and singular on purpose and thought `S-37`'s construction -- two pumps in series, both heads promoted -- needed a constraint pair a series loop could not supply, because two duty pairs over-specify. Measured 2026-09-22 through the circuit harness on the simple loop with a second pump: two duty pairs do over-specify, but by their *inlets* (`FS2210` naming `HE1.in.t`/`LOAD.in.t`: the second `MixedInlet` promotes nothing and the level pays for one), and a duty pair with no inlet is an `EnthalpyLevel`, not a flow. What fixes a flow without an inlet is `flow=` itself: `PU2 pump flow=0.239` (or `LOAD ... flow=0.239`) beside `HE1 power=30 in.t=20 out.t=50` promotes `PU2.head` beside `PU1.head`, counts square at 15, and is rank 14 at the seed with a zero pivot. Both codes fire from the script: `FS3009` *Nothing in the circuit determines PU2.head - PU1.head - 0.22 x N1b.p* -- the two heads and the pressure between them, which is the physics exactly -- and `FS3010` *PU2.flow stated - 0.33 x HE1.out.t stated are not independent*, the redundancy itself. The fixture is inline in `NullDirectionTests` with the counting, the rank and both messages asserted; it is not a sample, as the row asked. Estimated medium, took small: one harness run answered the construction. A by-product went to `S-37`: the same ring with *one* flow statement, one head promoted and the other left to sizing, never settles. |
| S-47 | 2026-09-09 | 2026-09-21 | medium | [`36`](36-numerics-and-convergence.md), [`32`](32-steady-state-newton.md), [`24`](../20-core-domain/24-auto-sizing.md) | **A seed cap of 10 kPa per component is a number chosen by measurement, and the corpus disagrees with itself about it** | The cap lived as a literal in `BranchResistance.Across`, the small Newton that turns a valve's Kv law into port offsets for the seed's pressure walk; nothing named it, `36` had no row for it, `32` still described the ladder `S-46` retired, and `SolutionSeed.NominalDrop` still declared 10 kPa with no reader. Two things were wrong beyond the naming. First, the clamp bounded every caller, and `OuterLoop.Resistance` is one: sizing's branch-drop totals, read at *solved* flows for a two-way valve's authority and a three-way valve's variable leg, had every other valve on the branch capped at 10 kPa --- a balancing valve set to 18 kPa entered the total as 10, and a number justified for a guessed flow was applied to a solved one. Second, the bound the row proposed instead, one derived from the circuit, had never been measured. Swept on 2026-09-21 under the current solver (after `S-74`), whole suite plus the circuit harness at each value: 10 and 15 kPa green everywhere; 40 kPa stalls `s55-main-valve` at the cap (residual 0.635) and fails the pump-free header test; 100 kPa converges `s55` but sends the three `s56` switched-off consumers non-finite or to the cap; 300 kPa fails all four; the largest pump's shut-off head --- the circuit-derived bound --- stalls `s56-ahu-off-main` at 0.353. The non-monotonicity the row reported is real and survives the Jacobian fix. On healthy loops the honest law is the better seed: `m2-simple-loop`, `cells` and `flow-*` take 2 first-pass iterations unbounded and 4 at 10 kPa, which is the cap's cost. | The clamp is `seed.valve_excursion`, a `Tolerances` row at **15 kPa**, the top of ESBE's published 3--15 kPa selection band (`three_way.dp_max`), and `BranchResistanceTests` holds the two equal so that moving either alone is a visible re-measurement. The physical claim is that a valve wanting more than it would ever be selected for, at a flow the seed invented, is a valve whose seeded flow is wrong, and what the seed wants from the law is its direction and its port ordering, not that number. Only a valve reaches the clamp: every other kind writes a pressure row and is read off it. `Across` and `Of` take the bound as a parameter, the seed passes it at its three reads, and sizing passes none, so a balancing valve's drop enters an authority total in full; no sized value moved anywhere in the corpus, and the Api goldens changed only by one iteration on the cooling loop (9 to 8) and by residual noise at 1e-10. `NominalDrop` is deleted. Not a circuit-derived rule, by measurement: the one the row proposed sits inside the band that fails. |
| S-74 | 2026-09-21 | 2026-09-21 | medium | [`36`](36-numerics-and-convergence.md), [`32`](32-steady-state-newton.md) | **The series ring's first solve from the cold seed is erratic in its bypass leg's resistance: 5 iterations at one Kv, the cap at the next** | Found building `C-111`'s balancing rule. `step-08b` with `BV_RAD valve kv=K` on the radiators' bypass, first pass from the cold seed: K = 2.95 took 5 iterations, 2.96 took 6, 1.53 took 11 -- and 2.9 took 34, 2.94 took 46, 2.9467 took 44, 4.99 the cap. The signature every time: `TV_AHU`'s Kv law leading, `PU_RAD.head` moving most, the line search cutting to 0.125 and 0.063 while the residual fell by ten in ten iterations. A replay of the trajectory with a central difference beside the solver's forward one found the temperature rows' pressure entries wrong by 260 %, and `FlashNoiseDiagnostics` then measured why: at the Jacobian's 4 mPa pressure step water's (p,h) flash returns dT/dp of -7.2e-7 K/Pa against the -2.06e-7 it settles at from 1 Pa up (a hundred times too large at 0.1 mPa) and dρ/dp 44 % high, because the flash's own iteration noise exceeds the change a milli-pascal step makes. √ε is the optimum for a function exact to round-off, and a flash is not one. The enthalpy columns were exact at √ε: dT/dh and dρ/dh agree to three digits at seven states. | `newton.fd_step_state` = 1e-5 on the node pressure columns, and a pressure column is read twice: at that step for every row, and at √ε for the rows a valve declares `SteepInPressure` -- its Kv laws, whose `C/(2√Δp)` may sit at a sub-pascal drop (the dead-leg script's bootstrap valve at 0.19 Pa) where a step of pascals is a secant across the whole operating range: read at 1e-5 alone, `s23-dead-leg` and the three `s56` stopped-consumer scripts went from 4 iterations to the cap, with the first full step's residual rising from 1.6 to 87 on the Kv law. Enthalpy-only at 1e-5 was tried first and was luck: Kv 2.94 converged, 1.53 took 41, 4.99 capped. Measured after: every Kv in the sweep 5-6 iterations; the sized balancing valve on the series header settles in three passes (6 + 6 + 2) at Kv 1.53 with `TV_RAD` at 0.501; the stopped-branch scripts unchanged; no corpus script slower and the substation and `vflow-he-hot` an iteration faster per pass. The noise is the flash's and IF97's closed-form backward `T(p,h)` would not carry it (`C-68`, `F-19`); the fix holds whichever backend is under it. |
| S-73 | 2026-09-21 | 2026-09-21 | small | [`32`](32-steady-state-newton.md), [`24`](../20-core-domain/24-auto-sizing.md) | **A `kv` promoted on a parallel branch does not converge from the bootstrap seed** | Closed the day it was filed. Two things, and the first was not the seed. `EquationSystem.Constraints` read a `dt` row's sign from `HydraulicPartition.Stated(component, "power")`, the value as written, while `ComponentFactory` lowers a `load`'s power to `-|power|` (`22`): `load power=20 dt=20` therefore demanded that the load *heat* its stream by 20 K, the seed's 70/50 read as 40 K wrong, and the first Newton step went the way the row pointed. Only `m2-substation`'s `heat_exchanger power=-150 dt=20` had ever met the row, and its sign was right by accident of the spelling. The row now reads the component's own `Power`. Then the seed: `SolutionSeed.PromotedKv` knew two sources for what a promoted valve has to take, stated end pressures and a stated pump head on its own branch, and a balancing valve on a parallel branch has neither; `SiblingKv` adds the branch that shares both ends, its drop at its seeded flow less the valve's branch's other drops, and the valve takes the whole remainder. **Measured** on the filed script with `RAD1 dp=10 kPa`: seeded Kv 4.08, converged in 2 iterations over 2 passes to Kv 4.10, 10.00 kPa across the valve, `PU1.head` 4.13 m, supply 70 °C and return 50 °C, where before it went non-finite in 6 with Kv at 0. The default-drop variant, both loads at 20 kPa, is a valve with nothing to take and stays a Kv nothing determines; not re-measured, since it is not a seed question. Corpus and ladder byte-identical on every solve, promotion and `FS22xx` line. Pinned by `OuterLoopTests.ABalancingValveOnAParallelBranchIsSeededFromItsSiblingAndThePairSolves`. |
| S-72 | 2026-09-20 | 2026-09-20 | medium | [`23`](../20-core-domain/23-topology-and-graph.md), [`22`](../20-core-domain/22-component-model.md), [`13`](../10-language/13-type-and-unit-system.md) | **A stated `flow` on an exchanger or a pump was not a constraint** | Found 2026-09-20 measuring P5.13b's `vflow` question on the simple loop: `HE1 flow=0.3` and `PU1 flow=0.3` both counted 11/11 with no `FixedFlow` row, solved to 0.086 kg/s -- the flow the loop drop and the bootstrap sizes happened to give -- and ended `NonFinite` when 30 kW at that flow put `N3` out of the fluid's range. The exchanger's `flow` had only ever been the design flow of its 20 kPa law, the pump's only its curve's duty point, while `22` called the exchanger's "Flow constraint, per side" and `CLAUDE.md` states the invariant a stated parameter is always a constraint (`D-02`, `D-32`). Closed the same day with the package: a stated `flow`/`in[2].flow` on an exchanger, or `flow` on a pump whose `head` is unstated, is a `FixedFlow` row on that branch, scaled so a 100 % flow error reads as the temperature scale; the candidate rule skips the exchanger's `power` for such a row (a column a flow residual never enters); a pump with `head` too is a curve point and pins nothing. `vflow`, the question the package was opened on, is the same row through the inlet node's solved density -- `ṁ − ρ(p,h)·V̇ = 0` -- and the first cut anchored every node on the branch to the header (the stopped-branch rule read a volume row's zero mass target as a pin at zero): 30 kW injected and the loop uniform at 20 °C, caught by reading the report. Measured: `flow=0.3` → 0.3 kg/s, `PU1.head` 7.09 m; `vflow=0.3 l/s` at a 20 °C inlet → 0.29946 kg/s, at a 60 °C inlet → 0.29497. Pinned in `StatedFlowTests`. The bare-unit trap `vflow` exposes is `L-57`. |
| S-71 | 2026-09-20 | 2026-09-20 | medium | [`32`](32-steady-state-newton.md), [`62`](../60-docs-and-devex/62-testing-strategy.md) | **The solve report stopped where the diagnosis starts: no trajectory, no state an engineer can read, no balance, no seed basis, no operating points** | Filed and closed 2026-09-20 at the user's request, after `S-55` and `C-105` were both diagnosed by reading the engine because the report did not carry the answer. What it lacked, by section: **iterations** -- `SolveResult` kept the final norm and the count, so "NonFinite after 8" was all a failed solve said; `NewtonSolver` now records each step (the residual it set out to remove, the line-search factor, the leading equation, the unknown that moved most and by how much in its own scale) in `SolveResult.History`, rendered as a table. **State** -- unknowns were `N3.h = 251439 J/kg`, `N1.p = 250000 Pa`; every node now also reads in °C and kPa and every branch in kg/s as *forward*, *reversed* or *still* against its written order, with its path. **Heat balance** -- `FS2203` checked it and said nothing; per hydraulic the report now sums sources, loads and boundary streams (mass × enthalpy at the crossing node) and closes them, a coupled exchanger contributing `+Power` to the hydraulic holding its side 1 and `−Power` to the one holding side 2 (the first draft counted the substation's primary as a 150 kW source and closed at +300). **Seed bases** -- `S-68` gave every branch flow a basis and the table showed the number only; `Duty(HE_RAD)`, `Partitioned(HE_RAD)`, `Propagated(HS1)`, `Nominal()` now sit beside each seed. **Operating points** -- pumps and valves appeared only as promotions and sizing lines; each pump now reads flow, head (solved or on its curve) and rise, each valve Kv, position, flow and drop, each three-way its Kv, position and every leg's flow and drop to the common port. The unknowns table also printed twice (a duplicated call). Pinned on the substation in `SolveExplanationTests`; `62`'s experiment loop and `diagnostics/README.md` list the sections. |
| S-56 | 2026-09-14 | 2026-09-20 | medium | [`32`](32-steady-state-newton.md), [`24`](../20-core-domain/24-auto-sizing.md), [`23`](../20-core-domain/23-topology-and-graph.md) | **A zero-duty consumer is seeded at another consumer's design flow, so an inactive parallel branch is treated as an open cross-flow path** | Found by changing only `HE_AHU load ... power=24 kW` to `power=0 kW` on the pump-free injection header. Half of it had gone by the time the entry was worked -- `S-68`'s forest stopped the off coil inheriting its sibling's 0.3589 kg/s -- and what remained was that nobody had decided what `power=0` *means*. Measured 2026-09-20 with the sibling fix in: the count was 39/39 and the solve ended `NonFinite` after 18 iterations, `TV_AHU.position` held at 0 (`FS3008`), because the off coil's `in=50` still promoted its valve to hold an inlet nothing flows to (0/0), and its `out=30` with a rated flow of zero had fallen back to the temperature form -- the outlet node *at* 30 °C, a statement about a node the return header happens to leave at 30 °C, so the pump head it promoted had nothing to push against. Decided as (b) of the three options put to the user (refuse; off means off; a `check_valve` kind), the user choosing (b): a zero duty is an operating state. Its `in` adds no `MixedInlet` row and promotes nothing; its `out` with `in` pins the branch at exactly zero flow, written in kelvin through the nominal seed flow; the pump promoted to hold it may solve to a negative head -- the bound is lifted for that one column -- and the answer is reported either way, `FS3015` when the pump dead-heads against the running consumers' backward push (and a pump that is *off* then needs a check valve, which is the cross-flow the entry was filed about) and `FS3014` when a main pump pushes forward and no pump can resist it. Three more things the measurement found and the same change fixed. Every node inside the stopped branch was the dead leg of `S-23` -- rank 36 of 38, the coil's outlet enthalpy and the pump head named as the pair nothing separates -- so a stopped branch's nodes take the temperature of the header node the branch hangs from, by an explicit closure row (`32` has the rule and why it is this project's reasoning). The exchanger sizer took 1.8e-27 kg/s as the flow its 20 kPa was measured at (a resistance of 1e44, pivots to 1e44); an off coil is not sized at all, and says so. And `MixedInlet`'s candidates were the hydraulic's three-way valves in list order, so with the off coil's valve free the *radiator's* inlet was handed the AHU's valve; the split on the coil's own branch comes first now. Pinned in `HeaderSeedTests`: AHU off and RAD off both converge in two iterations at full rank, the off coil has no `flow` size, the running coil keeps 0.3589 / 0.2871 kg/s, the holding pump dead-heads at 2.05 m, and the only flow on the stopped side is the valve stop's 2 % leakage crossing the valve body (6 g/s), a path the script wrote; with `PU_MAIN head=8` on the return the same pump solves to −6.0 m with `FS3014`. Acceptance met: neither inactive exchanger is sized from the active branch, both variants reach the solver, and the non-zero inactive-branch flow is traceable. Option (c), a `check_valve` kind, is the follow-up the `FS3015` wording asks for and is not filed until a script needs it. |
| S-45 | 2026-09-09 | 2026-09-21 | medium | [`23`](../20-core-domain/23-topology-and-graph.md) | **A flow constraint can be answered by a pump in a different consumer's loop, which no pressure it develops can reach** | Closed 2026-09-21 by `D-133`, the user's call between the options laid out that day (B, define *reaches* and filter the lists; D, a matching over all constraints). What was wrong, in the end, was two things the entry's history circled: the candidate lists admitted actuators that cannot move the quantity, and first come pushed a deficit onto whichever statement was last. `Reach` now answers three physical questions once: a split's `position` holds the temperatures on the stream it mixes, walked along nominal flow from its common port to the next exchanger (a diverting valve, its common port fed by an oriented neighbour, is walked from both legs); a pump's `head` moves the flows on a cycle through it, which is `HydraulicBlocks`' block with the boundaries grounded; a leg split's `position` is its leg's flow. `Assignment` keeps `D-130`'s greedy pass and augments it (Kuhn) for each constraint it left, so nothing moves unless that rescues a constraint, and reports what stays unmatched as its Hall group. **Measured** on the injection header with `HS1 in.t=40 out.t=80`: still over-specified by 1, now `HS1.out.t, N3.t, HE_AHU.out.t, HE_RAD.out.t share PU_RAD.head, PU_AHU.head, TV_MAIN.position` with `N3.t` on `TV_MAIN` and each coil's inlet on its own valve, where it blamed `HE_RAD` and handed the setpoint to `TV_AHU`. Two parallel loads below one pump with a valve on the first-declared branch: square at 15/15, the bare branch on the pump, where it refused asking for a valve on the branch meant to have none. Every other promotion and `FS22xx` line across the corpus and the ladder unchanged, except `m1-syntax-tour` (over by 4, not 3: `TV2` never held `HE2`'s inlet). The augmented parallel case is square and does not solve: `S-73`. Five tests in `PromotionLocalityTests`; `FS2218` is information. |
| S-66 | 2026-09-19 | 2026-09-20 | medium | [`32`](32-steady-state-newton.md), [`36`](36-numerics-and-convergence.md), [`24`](../20-core-domain/24-auto-sizing.md) | **The seed's pressure walk did not see a promoted Kv, so a promoted valve was seeded with a few pascals across it and the Jacobian at the seed was nearly singular in the Kv column** | Closed 2026-09-20, measured before and after on the whole corpus with two instruments built for it: the solve report now prints the Jacobian's pivots *at the seed* beside the solution's, and the iterations of each sizing pass instead of their sum. Before: substation seed pivots largest 38.6, smallest 1.19e-4, ratio 3.1e-6 (the row's 1.2e-4, confirmed); `head15` ratio 5.4e-7. Fix as the row proposed: `Promoted` runs before `Integrate`, and the walk passes each component its promoted `kv` and `head` through a `parameters` buffer `BranchResistance.Of`/`Across` now accept (`SolveContext.Parameters`, the same slot the solver fills). After: substation ratio 1.36e-2, `head15` 6.7e-2 and 6 → 4 iterations; every other script's pass counts unchanged; goldens moved only in the substation's residual norm (5.4e-11 → 8.2e-11). Two things the row did not predict. (1) Letting the walk read a promoted *position* as well (mid-travel, `S-50`) cost 1–2 first-pass iterations on every three-way-valve circuit — cooling loop 5 → 7, header 5 → 6, `s55` 9 → 11, `s56-ahu-off` 8 → 9 — while improving `s55`'s seed pivot 0.085 → 0.21 and `rad-off` 10 → 8; position is left to the component's own value and the trade is recorded in code and in `32`. (2) The row's second remedy, re-seeding a picked datum at 0 once the seed was well conditioned, was measured and fails for a different reason than `D-121`'s attempt did: the walk from a datum at 0 leaves `NSUP` at −70 kPa *absolute* before Newton starts (`FS3007` at 0 steps), because a seeded 2.2 m head cannot lift what the ring's drops take at the seeded flow. The 100 kPa offset stays, and the seed's remarks say why twice over. Effort medium, against a small estimate: the instruments were most of it. |
| S-23 | 2026-09-04 | 2026-09-20 | small | [`31`](31-solver-architecture.md), [`32`](32-steady-state-newton.md), [`36`](36-numerics-and-convergence.md) | **A dead leg's node enthalpy was a zero column, and the whole loop failed for it** | Measured 2026-09-20 on `diagnostics/scratch/s23-dead-leg.fluid` -- the simple loop with `N3 - N9 length=5 dn=25`, a stub to a node nothing else touches, which is what a script looks like between two keystrokes. Before: `Singular after 0 iterations`, `FS3002` around `N9`, `FS3009` naming `N9.h`, `FS3010` pairing `N9`'s mass and energy balances as dependent, rank 14 of 15, and sizing not settling (`FS2301`) because no pass converged. The mechanism was as filed: `N9`'s mass balance forces the stub's flow to exactly zero, and its energy balance is then `ṁ·h` with `ṁ = 0` -- a row that is `h_N9` times the mass row, on a column of zeros. What closes it is `S-56`'s closure without a constraint behind it: `EquationSystem.Stagnant` now also walks every branch with one end at a terminal interior node (one port, no boundary role -- a `supply` or `return` with one connection is a boundary whose flux is unknown, `D-64`, not a dead end) and closes that node and every node inside the branch against the node at the live end; a stub off a junction element has no anchor and keeps its rows. After: converged in 4 iterations, `N9` at `N3`'s 50 °C, rank 15 of 15, no diagnostics beyond the datum. `32` says so; `SolutionSeedTests`' exemptions stand (they are about the seed's flow, which was never the question) and lost a claim about an `FS4010` that does not exist. Pinned by `HeaderSeedTests.ADeadLegTakesTheTemperatureOfTheNodeItHangsFromAndTheLoopStillSolves`. Effort as estimated. |
| S-70 | 2026-09-20 | 2026-09-20 | small | [`23`](../20-core-domain/23-topology-and-graph.md), [`36`](36-numerics-and-convergence.md) | **A coupled exchanger between two closed circuits reported `FS2203` under one circuit's name, both stated flows as answered by side 1's pump, and both rings as running "reversed"** | Measured 2026-09-20 on `diagnostics/scratch/s70-two-rings.fluid` (`first`: `PU1`, `HX1`; `second`: `HX1.out[2] - PU2 - N3 - HX1.in[2]`), promoted heads. Three attributions, each a lookup that stopped one step early. (1) `WellPosedness.Name` took a hydraulic's circuit from its *first element*; a coupled exchanger is an element of both hydraulics it separates, so whichever ring's walk happened to start at `HX1` was named for the exchanger's circuit. Now the circuit most of the hydraulic's *own* elements belong to, an element in two hydraulics excluded; ties to graph order. (2) The report matched a promotion to a constraint by kind and component; `HX1` states a flow on each side, so `FixedFlow on HX1` twice both took the first promotion, `PU1.head`. Now the whole `ComponentConstraint` record, and the line names the parameter: `FixedFlow on HX1.out -> PU1.head`, `on HX1.out2 -> PU2.head`. (3) The state section's direction was the sign of the branch flow against the branch's own walk, under a header that said *written order*; a ring's walk starts wherever the decomposition reached it first, and both rings read `reversed` while every pump pushed the way it was written. Now read off the port map as `FS3013` already does: the flow entering the first ported element's `in`; a direct link with a named port says `into TV_AHU.b` / `out of 3WV.a`, which a reader can check; a bare node-to-node link says along or against the printed arrow. Across the corpus the label changed on eleven rows, every one of them a ring or a valve leg, and no number moved. Pinned by `SolveExplanationTests.ATwoSidedExchangerBetweenTwoRingsIsReportedSideBySide`. Effort as estimated. |
| S-55 | 2026-09-14 | 2026-09-20 | medium | [`32`](32-steady-state-newton.md), [`24`](../20-core-domain/24-auto-sizing.md), [`23`](../20-core-domain/23-topology-and-graph.md) | **Loop-driver and main-valve sizing look only for a pump on the local source/bypass cycle and miss distribution pumps that drive the complete circuit** | Measured on the exact pump-free mixing topology `N1 - HS1 - TV_MAIN.a`, `N1 - TV_MAIN.b`, `TV_MAIN.ab - N3`, with `N3 t=60` and pumps only in the AHU and radiator circuits. Automatic `TV_MAIN.kv` leaves the bootstrap **Kv 630** and emits `FS2214` for the small `TV_MAIN.b -> TV_MAIN.a -> HS1` cycle: *nothing drives flow around it*. But the intended flow is not an isolated circulation around that small cycle. Each distribution pump draws from the shared supply, discharges through its load to the shared return, and therefore establishes the pressure difference that drives both source-valve legs. Holding only `TV_MAIN.kv=25` changes the report from rank **44/45**, with `TV_MAIN.position` undetermined, to full rank **45/45** without adding a source pump. In the still simpler no-bypass topology `N1 - HS1 - N3`, the report is full rank **39/39**, emits no driver warning, runs two iterations, and solves positive heads of **0.643 m for `PU_AHU`** and **1.796 m for `PU_RAD`** before a thermal seed failure. The counting table also states the intended ownership directly: each exchanger's `FixedFlow` is answered by its distribution pump head. This is evidence that a source pump is not required by the equations; the driver diagnostic and valve sizer are using a cycle-local criterion that is too narrow for a multi-pump graph. | **Closed 2026-09-20.** What the code meant: a loop nothing drives carries no flow, and a valve whose circuit has no free pump takes what the boundaries leave. What it did: asked both questions of one fundamental cycle -- the small `TV_MAIN.b → TV_MAIN.a → HS1 → N1` cycle has no pump on it -- when the answer belongs to the cycle's biconnected block, where any two branches share a cycle and a pump anywhere reaches every branch. Now `HydraulicBlocks` (Tarjan over branch endpoints, junction elements included) labels each branch with its block; `ReportDriverlessLoops` reports a loop only when its block holds no `DrivesFlow` kind and no boundary pair (a virtual ground joins the boundaries, so an inlet-to-outlet path is driven), and `OuterLoop.Driven`/`ThreeWay` read the block *without* the ground, so a bounded primary beside a pumped secondary still rounds up (`D-89`). Measured on the entry's own script: 45/45, converged in one Newton iteration, no `FS2214`, `TV_MAIN` Kv 6.3 by `D-122`'s band at position 0.81 (was the bootstrap 630 with the position undetermined), `HS1` 0.2581 kg/s plus 0.1725 through the bypass making the header's 0.4307, coils 0.2871/0.3589, heads `PU_AHU` 5.05 m and `PU_RAD` 5.51 m -- each its own loss plus the shared source path. `OuterLoopTests.TheDistributedHeaderFindsItsFortyKilowattSourceDutyAndConverges`, skipped since it was written, runs and passes; `HeaderSeedTests.ThePumpFreeMixingHeaderIsDrivenByItsConsumerPumpsAndSizesItsMainValve` pins the figures. No corpus value moved. Left as a note, not a defect: the report says `TV_MAIN` has authority 0.1 against a variable circuit dominated by `HS1`'s 20 kPa, which is what a mixing valve on a source branch looks like and `D-122` sizes it to the band regardless. |
| S-53 | 2026-09-14 | 2026-09-20 | small | [`32`](32-steady-state-newton.md), [`24`](../20-core-domain/24-auto-sizing.md) | **The header seed confuses local consumer circulation with net header draw, copies full circulation onto both three-way-valve inlet legs, and can orient pumped branches backwards** | Measured by removing the supply bypass entirely: `N1 - HS1 - N3`, no source pump, `HS1 out=80`, with 24 kW and 30 kW consumers each mixing 80 C supply with 30 C return to reach a 50 C coil inlet. This graph is square and full rank (**39/39**) and runs two Newton iterations, so the topology is not deficient. Its correct duty-derived flows are **0.2871 kg/s** through the AHU coil, **0.3589 kg/s** through the radiator coil, and only **about 0.2581 kg/s net through HS1**: the consumers draw about 0.115 and 0.143 kg/s from the 80 C header and recirculate the rest locally. The complete report with `HS1.out` omitted makes the exact factor of two visible before Newton: `TV_AHU.a = 0.286807`, `TV_AHU.b = 0.286807`, so its common leg becomes `-0.573614 kg/s`; `TV_RAD.a = 0.358509`, `TV_RAD.b = 0.358509`, so its common leg becomes `-0.717017 kg/s`. `BranchFlows.Duty` has already calculated the correct positive magnitude from `abs(power) / abs(delta-h)`. The corruption occurs when `BranchFlows.Propagate` copies the best known magnitude to every nominal branch meeting a junction: both inlet legs receive the *whole* load circulation, and the later mass-consistent field correctly sums those two wrong guesses on the common leg. Physically those legs partition one circulation, so at an 80 C header they should be about `0.1148 + 0.1723 = 0.2871 kg/s` for AHU and `0.1436 + 0.2153 = 0.3589 kg/s` for RAD. The explicit-`out=80` report consequently sizes `HE_AHU.flow` at **0.5742 kg/s** and `HE_RAD.flow` at **0.7177 kg/s**, exactly twice their physical coil flows, and `HS1.flow` at **0.6460 kg/s**, the sum of the undoubled coil flows rather than the net header draw. Repeating the explicit-source experiment with neutral `heat_exchanger power=-24/-30` removes `S-54`'s false 108 kW diagnostic but leaves the radiator seed and size at **0.7177 kg/s**, proving that duty-sign addition does not cause this doubling. The seeded/solved directions also disagree between parallel circuits: the radiator supply and return paths run backwards and both common-port pump/exchanger branches are negative. Newton responds by driving a valve to its limit, then `N1` leaves the property domain and the solve terminates `NonFinite`. On the mixing-supply variant, automatic closure is similarly unavailable when the first seed is built: `HS1.flow` becomes **1.6508 kg/s**, fixing only Kv restores full rank but not a finite first step, and stating the physically expected 0.258 kg/s contaminates the consumer sizing instead of correcting the field. | **Closed 2026-09-20.** The four fixes landed under other names -- the source's omitted duty is closed before the seed (`S-63`), a coil's design flow comes from its own duty and terminal difference and the forest ranks its chords by basis (`S-68`), the three-way valve is a partition seeded from its temperatures (`S-58`, `S-63`), and direction rides with the pump (`S-63`) -- and the acceptance is now measured and pinned on the entry's own script, `HeaderSeedTests`: the no-bypass header at `out.t=80` converges in two Newton iterations with `HE_AHU` 0.2871 kg/s, `HE_RAD` 0.3589 kg/s and `HS1` 0.2581 kg/s, both as solved branch flows and as sizes; each valve's legs sum to its common flow with the hot leg at 0.4 of it and neither leg the whole; and with `out.t` omitted the graph is 39/38 and `FS2211` leads with a temperature, not a pressure (`S-52`). The mixing-supply variant is `S-55`'s and stays there. |
| S-68 | 2026-09-19 | 2026-09-19 | — | [`32`](32-steady-state-newton.md), [`24`](../20-core-domain/24-auto-sizing.md), [`29`](../20-core-domain/29-layout-ladder.md) | **On a header with unrated blocks the seed's flow field overrides the rated coils, and the first sizing pass sizes pumps and valves against exchangers that have no drop yet** | What the seed meant: every branch takes its duty estimate where a rated exchanger gives one (`BranchFlows`), and the spanning forest reconciles them by mass balance, chords keeping their estimates and tree branches taking what continuity leaves. What it did: the forest was grown without looking at what each estimate rested on, so on the ladder's 8c -- four blocks in series, only the radiators rated -- the three unrated coils' 0.1 kg/s nominals were kept as chords and the rated radiator coil became a tree edge that came out at **0.0197 kg/s** against its 0.478 duty; the ring's stream was that 0.02, `HS1`'s 30 kW at it was a 364 K rise in the temperature walk, and `N1` seeded at `h = 0` (`FS3007` after 0 steps, residual `Infinity`). On 8e the AHU's rated coil, 0.287 kg/s from 24 kW at 50/30, seeded at **0.0477** because its `a` leg took a header junction's propagated 0.239 and the valve's remainder rule set the coil to the difference. Underneath that, a second defect in the same runs: `OuterLoop.Prepare` sizes once on the bootstrap graph, where an exchanger with no stated flow and no duty is built ideal (`ComponentFactory`: no design point, no resistance), so a pump whose head no constraint had claimed was sized to zero -- "no modelled resistance" -- on a loop whose coil drops 20 kPa, and the first solve ran on a graph with no answer near its seed. | **Closed 2026-09-19.** Two changes. `BranchFlows` ranks its estimates -- `Nominal` < `Propagated` < `Partitioned` (a valve leg split from a rated coil's flow, new) < `Duty` < `Stated` -- and `SolutionSeed.Field.Span` grows its forest as a minimum spanning tree over that rank (Prim's, the switched legs of a three-way valve and barred branches penalised, the penalty direction-free because a directed one had the greedy walk take a `b` leg outward and then sacrifice a rated `a` leg), so the best-founded estimates are the chords and the tree is built from the nominal ones. `Prepare` applies the sizing rules twice on the bootstrap, the second time on a graph lowered from the first application's sizes, so a pump's head and a valve's Kv are read against the exchangers' drops. Measured: 8c's radiator coil seeds at 0.478 and the ring at 0.359 (`HS1`'s duty at the 20 K the walk sees), 8e's AHU at 0.287, its radiators at 0.479 and its series stream at 0.239 -- the values Newton finishes at; the simple loop settles in two passes instead of three, and nothing else in the corpus moved (the header still at one iteration). 8e's script had a second fault of its own: the floor's `in` equalled the 40 it is fed, so its valve had to sit on its open bound; given a mixing point (`in=35`) and its valves' Kv, like 8b (`C-104`), it settles in two passes and two iterations. 8c settles neither, for reasons that are not the seed's flow field: `S-69`. `SolutionSeedTests.AThreeWayValvePartitionsTheCommonDutyFlowAcrossItsInletLegs` (the `Partitioned` basis), `.ARatedCoilAmongUnratedBlocksKeepsItsDutyAsAChord`, `OuterLoopTests.APumpOnAnUnratedCoilIsSizedAgainstTheCoilsDropBeforeTheFirstSolve`, `.AMixedHeaderWithASeriesPairConvergesAndSettles`. |
| S-63 | 2026-09-17 | 2026-09-19 | — | [`32`](32-steady-state-newton.md), [`36`](36-numerics-and-convergence.md), [`24`](../20-core-domain/24-auto-sizing.md), [`29`](../20-core-domain/29-layout-ladder.md) | **Two injection branches in series do not converge, whatever the script states** | What the entry believed: that the series *shape* -- a stream passing through one mixing block into the next -- was what the seed and the step control lost, with the seed's orientation, the mixing enthalpy step or the reduced-step rule as suspects. What was actually wrong were two seed defects that only the series shape exposes. First, `BranchFlows.MixingFraction` took the hottest source outlet in the whole graph as the temperature feeding every mixing valve's `a` port: right for a parallel header, where every valve sees the boiler's 60 °C, and wrong for the AHU behind the radiators, which is fed by their 40 °C return. Mixing 40 and 30 to 35 is half and half; read as 60/30 to 35 it is a sixth, so the AHU's stream estimate was 0.0797 kg/s where the ring carries 0.239, the field solve pushed the radiators' coil to 0.319 against its 0.478 duty, and the whole seed was three times wrong on the stream. Second, `SolutionSeed.Integrate`'s `Anchored` read stated pressures on branch *endpoints* only; `N1 node p=250` sits on a node with two connections, inline (`D-114`), inside a branch's path, so the walk saw no anchor, started at the 100 kPa scale, and met the 250 kPa datum half way round -- a 150 kPa closure error that on the parallel header fell across `HS1`'s quadratic drop, which Newton removed in one step, and on the series header fell across the AHU valve's `a` leg, a `√Δp` law it could not step through: the first Newton step was α = ½ with the AHU's position going 0.5 → 0.30, the second α = 1/16 with the full step wanting −1.7, the third parked it at the 0 bound where the projection (`S-26b`) held it for the remaining 47 iterations while the radiators' side crept at α = ⅛. How it was found: the report's seed table, read against the hand figures the entry itself gave -- the stream at 0.0797 was a third of 0.239, and `TV_RAD__PU_RAD.p` at exactly 100000 while `N1` stated 250000 was the walk's unanchored start. | **Closed 2026-09-19.** `MixingFraction` takes the valve's `a` branch and `FeedTemperature` walks it for the exchanger that last touched the water (a source in the path, else the load discharging into the far junction; a stated `t` on that junction wins), with the hottest source as the fallback; `Anchored` reads every element of every path. With both, the first pass converges from the seed in six iterations, 0.535 → 2.4e-4, both valves at 0.50 and both pumps at 26–27 m; with the valves' Kv stated the run settles in three passes for `kv=4`, `6.3` and `10`. Left to the authority rule the second pass sizes `TV_RAD` to Kv 1.6, which at the mid-travel this mixing point needs asks 15 bar of its pump and runs `NonFinite`: that is `S-58`'s open observation as a divergence, filed as `C-104`, and the ladder script states `kv=6.3` and drops its marker. The two other scripts the ladder had marked `S-63` fail at the seed for a third reason and carry `S-68`. `SolutionSeedTests.ASecondBlockInSeriesReadsItsFeedFromTheFirstBlocksReturn`, `.AStatedPressureOnAnInlineNodeAnchorsThePressureWalk`, `OuterLoopTests.TwoInjectionBlocksInSeriesConvergeAndSettle`. The corpus: the distribution header's anchored seed costs it one more Newton iteration over its passes (7 → 8) and moves two valve positions in the sixth digit; nothing else changed. |
| S-29 | 2026-09-07 | 2026-09-19 | — | [`32`](32-steady-state-newton.md), [`23`](../20-core-domain/23-topology-and-graph.md), [`21`](../20-core-domain/21-fluid-and-state.md), `D-98`, `D-121` | **Two pumps in series terminate `NonFinite`, and one pump on the same count converges** | What the code meant: a closed loop has no absolute pressure of its own, so `D-98` picks the first pump's suction as the datum, sets it to 0 gauge, and reasons that a suction is the loop's low point so every other node sits above the property floor. What it did: with two pumps on the ring the first suction is the second pump's discharge, so the second suction (`N6`) sits its own 3 m head *below* the datum -- 29.4 kPa gauge under, 72 kPa absolute -- and `Water.ValidRange` began at 100 kPa absolute, so the correct solution lay outside the table. Newton went straight for it: the full first step landed `N6` at −29.4 kPa, the answer, and the line search halved five times against the wall (α 1, 0.5, 0.25, 0.25, 0.0625) before `NonFinite`. Why that is wrong physically: liquid water at 20 °C and 72 kPa absolute is the ordinary contents of a pump's suction line; nothing about the plant is impossible, only its bookkeeping zero coincided with the table's edge. How it was found: the valve and pump-curve hypotheses in the original entry were run first (`kv=4`, no valve, duty variants: all `NonFinite` at 4–5 iterations); a per-node probe of the extrapolated trial step then named `N6` at 99.06 kPa absolute as the refused state at α = 0.75, and `N1 node p=50 kPa` on the same ring converged in 0 iterations with `N6` exactly 29.4 kPa below `N1`. | **Closed by `D-121`, 2026-09-19.** `Water.ValidRange` starts at the triple point; the boiling line bounds the liquid by phase (`F-13`); `FS2221` reports a solved node below atmospheric with the fill pressure to state. The ring converges in three passes with `N6` at −29.4 kPa and *'N6' is 29 kPa below atmospheric pressure. State a pressure on 'N1' of at least 80 kPa.* No corpus value moved. `OuterLoopTests.TwoPumpsInSeriesConvergeAndTheSecondSuctionIsNamedAsUnderVacuum` replaces the pinned test; `SubstanceTests.LiquidWaterBelowAtmosphericPressureIsAState` and its two siblings pin the floor. The original entry's suspects -- the curve going negative, both rises on one branch -- were both wrong, and the diagnosis took one trace once the probe printed *which node* the domain refused: `TryEvaluateResiduals` zeroes the residuals on failure and `OutOfDomainNode` already carried the answer. |
| S-67 | 2026-09-19 | 2026-09-19 | — | [`32`](32-steady-state-newton.md) | **A step under the step tolerance was reported as a stall with the residual of the point it left, without measuring the point it reached** | What the exit meant: `‖α·Δ‖ < stepTolerance` with the residual still large is a stall -- the iteration cannot move and is not there. What it did: it fired on the step's size and reported `norm`, the residual computed *before* the step, and never evaluated the new iterate. Under quadratic convergence the last step is the size of the residual it removes, so a residual just above the tolerance makes a step under the step tolerance and lands converged: `S-29`'s ring went 1 → 0.37 → 1.4e-3 → 1.16e-8 → 2.4e-12 and stopped as *Stalled at 1.157E-08*, `FS3004` blaming `CV1`'s Kv law for conflicting requirements, and the outer loop then stopped after one pass with `FS2301`. The `SolveExplanation` for the same run showed 2.4e-12 on every row, which is what said the exit and the iterate disagreed. | **Closed 2026-09-19.** The exit evaluates the point it reached; under the residual tolerance it is `Converged`, otherwise `Stalled` with that point's residual. `NewtonSolverTests.AStepUnderTheStepToleranceIsJudgedByWhereItLandsNotWhereItStarted` pins that `ResidualNorm` is the returned iterate's own residual on that path and that the termination follows from it. `32`'s sketch updated. |
| S-52 | 2026-09-09 | 2026-09-19 | — | [`36`](36-numerics-and-convergence.md), [`23`](../20-core-domain/23-topology-and-graph.md) | **`FS2211` tells the user to add a pressure to a circuit whose deficiency is thermal, and taking its advice turns an honestly refused circuit into a square one that fails in the solver** | Changed 2026-09-19 (sweep tier 3): `Understated` reads the constraint list and the promotions rather than scanning the script. A hydraulic's level counts as fixed when a constraint on it is `EnthalpyLevel` (a lone terminal that paid, `D-90`) or a `NodeTemperature`/`MixedInlet` that no promotion absorbed; a terminal pair pinning a flow and a stated inlet a mixing valve answers are matched and pay for nothing, exactly as `D-90` says. The pressure candidates are kept after the thermal ones, since a circuit can be short on both halves. `FixesEnthalpyLevel` is gone. The header this was measured on has been square since `D-91`, so the test is a ring built to the same shape -- `HS1 in=40 out=70 power=50`, a three-way valve answering the inlet, the pump's head absorbing the outlet -- which counts −1 and now leads with *a temperature on N1* where the scan would have suppressed every temperature and offered *a pressure on N3*. `23` records the rule under promotion. |
| S-20 | 2026-09-04 | 2026-09-19 | — | [`32`](32-steady-state-newton.md) | **`NewtonSettings.RetryFromSeedOnFailure` cannot be implemented where `32` puts it** | The retry already lived where this entry said it belonged -- the outer loop discards a warm start whose pass did not converge and reruns from the sizing seed (`fromWarm`) -- and nothing said so. Changed 2026-09-19 (sweep tier 3): `FS3012` is registered in `SolverDiagnostics` and the loop raises it on that retry, attached to the run's last solve whichever way the loop ends; `32`'s row names the loop as the producer. The discarded pass is counted in `passes` and its Newton steps in `iterations` (`A-4`). `AWarmStartThatDoesNotConvergeIsDiscardedForTheSeedAndReported` runs a solver that rejects its first system: with a warm start the run converges and carries one `FS3012`; without one the refusal stands and no `FS3012` is invented. `NewtonSettings.RetryFromSeedOnFailure` stays unimplemented, as `32` now says it should. |
| S-65 | 2026-09-18 | 2026-09-19 | — | [`32`](32-steady-state-newton.md), [`16`](../10-language/16-diagnostics.md), [`44`](../40-api/44-diagnostics-contract.md) | **A script that cannot be solved reports one `FS2004` carrying the text of another diagnostic, and loses that diagnostic's code, component and range** | What the loop meant to do: refuse a circuit the posedness check cannot solve and tell the user why. What it did: wrap the first check error's *message* into `FS2004`'s `{state}` argument and return that one error, so the wire carried a property-evaluation code with `component: null`, no range, and one of the check's findings spliced into another sentence. What changed (2026-09-19, sweep tier 2): `ResultError` carries the diagnostics it stands for (`Diagnostics`), and `Report(span)` gives a caller those diagnostics -- plus the error itself only when none of them is an error, so a refusal always has one error on the wire. `OuterLoop.RunAsync` attaches `posedness.Diagnostics` to its refusal; `ScriptPipeline` reports through `Report`. `FS2205` now sets its `ComponentName` from the node's component, so the fan-out case badges the node's owner. Verified by `ARefusedSolveReportsTheCheckOwnDiagnosticsWithTheirCodesAndComponents`: an `inlet` with two connections reaches the client as `FS2205`, error, `component: "S1"`, with the check's exact text and no `FS2004`. Still open elsewhere: `FS2205`'s *range* -- the check reports at the node, which has no span until `L-55` gives inferred nodes one. |
| S-62 | 2026-09-15 | 2026-09-15 | — | [`23`](../20-core-domain/23-topology-and-graph.md), [`36`](36-numerics-and-convergence.md), `D-86`, `D-98` | **The picked pressure datum put a closed loop's pump suction below the substance's floor, and the sample was `NonFinite` at iteration 0 for a reason the report could not name** | What the rule meant: a closed loop has no meaningful absolute pressure, so any node can be the zero and the most-connected one is a stable, deterministic choice (`23`). What it did: the most-connected node of the substation's secondary is `NSUP`, the supply header downstream of the pump; the seed placed it at 0 gauge and the suction, 21 kPa of losses upstream of it, at −21 kPa gauge --- 80 kPa absolute, below `Water.ValidRange`'s 100 kPa floor. The first property read there failed and the run stopped with `FS3007` before Newton took a step. Why that is wrong physically: nothing about the plant is at 0.8 bar absolute; the datum is a bookkeeping choice, and the bookkeeping walked outside the fluid's table. How it was found: after `D-97` the counting was square and the seed's flows and temperatures were right, and the sample still died at the seed; a scratch variant stating `NSUP p=150` converged in two iterations, which isolated the datum from the three other things `P4.1` changed. Same family as `S-60`, which caught the height-driven version of the floor and reports it; this is the loss-driven version, and it is not the user's to fix. | **Closed by `D-98`, 2026-09-15.** `HydraulicPartition.Assemble` picks the first pump's suction node (`HydraulicPartition.Suction`) before falling back to the most-connected one; the suction is the loop's low point, so every other node sits at or above zero gauge. A stated `p` still wins, `FS2201` still names the pick. Every closed sample's `FS2201` text changed (`m1-syntax-reference` now names `PU1__in`), and no solved value in the corpus moved. `WellPosednessTests.AClosedCircuitsPickedDatumIsThePumpSuction`. |
| S-32 | 2026-09-07 | 2026-09-15 | — | [`22`](../20-core-domain/22-component-model.md), [`23`](../20-core-domain/23-topology-and-graph.md), `D-97` | **A stated `in2`/`out2`/`power` triple implies a side-2 flow and nothing computes it** | What the model meant: `HX1 power=150 in=40 out=60 in2=85 out2=45` describes an exchanger whose primary runs at 150 kW over a 40 K drop, 0.895 kg/s, and whose secondary runs at 1.79 kg/s over 20 K. What it did: `D-19` made the four terminals a design point so that counting them as demands would not report the substation over-specified by three, and nothing then said what the primary runs at --- `NPS p=600` and `NPR p=350` fix its pressures, the promoted valve absorbed whatever they implied, and the loop ran at 2.21 kg/s through the bootstrap's Kv 630. Why that is wrong physically: an exchanger sized for 85/45 and run at 2.5 times the flow returns at 69 °C, and the whole point of stating the design point is that the circuit is meant to sit on it. Sketched in the open entry as a registry group plus a side-2 `ImpliedFlow`; that would have seeded the right flow and constrained nothing. | **Closed by `D-97`, 2026-09-15, as part of `P4.1`.** The design point pins the flow of a side nothing else pins: `WellPosedness.Constraints` makes a second pass over every exchanger that rates, finds the hydraulic each side's inlet peers into, and adds `out2` (or `out`, or the `dt`) as `FixedFlow` there unless a `FixedFlow`/`EnthalpyLevel` constraint or a stated boundary `flow` already pins it; `EquationSystem` assembles it as the derived-flow row on the side's own ports. On the substation `LOAD.dt` pins the secondary and `HX1.out2` the primary, answered by `PCV.kv`, and the same rule with one side gives Rated mode its pin. Measured with the rest of `P4.1` (`S-62`, `D-98`, the ε-NTU duty relation, the promoted-Kv seed): **converged in two Newton iterations and two sizing passes**, scaled residual 6e-11, rank 24, on `01`'s figures --- 0.895 kg/s primary, 1.793 kg/s secondary, 60.1/40.1 °C secondary, 45.2 °C primary return, `PCV.kv` 2.13, `SP.head` 10.2 m, and the report's rating line `UA 12.071 kW/K rated, 12.071 kW/K by LMTD 12.427 K`. `CorpusStatusTests` records `Converged`; `RatedExchangerSolveTests` holds the numbers; `WellPosednessTests.ACoupledExchangersTerminalTemperaturesAreADesignPointNotConstraints` now expects `[LOAD.dt, HX1.out2]`. |
| S-61 | 2026-09-14 | 2026-09-14 | — | [`32`](32-steady-state-newton.md), [`36`](36-numerics-and-convergence.md) | **`FS3008` reported a bound the iterate passed through on its way, not one the answer sits on** | **Closed in the same change as `C-75`.** `FS3008` means *the circuit is asking for more than this component can give*, which is a claim about the solution, and it was raised the first time `EquationSystem.Project` clamped a column --- a claim about the path. Invisible while every promoted parameter started near its answer; found the moment a promoted `kv` started at the bootstrap's Kv 630 (`D-96`): the first Newton step overshot to a negative Kv, was held at 0, and the iterate then walked up to the 0.773 the loop needed --- and the report said `CV1.kv was held at 0, which is as far as it goes` of a valve three quarters open at convergence. `NewtonSolver` now records each pinned column with its bound and `Stop` raises `FS3008` only for the columns the final iterate still sits on, whatever the termination. The two existing pins --- an unsized pump's head floored at 0 with nothing resisting the flow, and a stated Kv 630 that leaves the pump nothing to do --- are still reported, because they are still true at the end; `OuterLoopTests.AStatedPumpHeadIsSpentByTheBalancingValveAndNothingSizesTheValve` pins the silence. |
| S-60 | 2026-09-14 | 2026-09-14 | — | [`32`](32-steady-state-newton.md), [`23`](../20-core-domain/23-topology-and-graph.md), `D-70`, `D-86` | **A tall closed circuit with no stated pressure fails at the seed with `FS3007`, and nothing says the fill pressure is why** | **Closed the day it was filed, once `P3.9` put a height on every node.** Found measuring elevation (2026-09-14): a 32 m riser and matching downcomer with no `p=` anywhere had `FS2201` pick `N1` at 0 gauge, the seed put `N4` at −213 kPa absolute, water has no state there, and the report said `N4 produced an impossible value in its fluid state after 0 steps` --- a solver failure to an engineer reading it, when the plant as written has 3.1 bar of static head and no fill pressure. With `N1 node p=450` the same script converged in 0 iterations on the right figures, so the physics was right and the message was not. `WellPosedness.ReportStaticHead` now runs before the seed: `p_datum − ρg(z − z_datum)` at each hydraulic part's highest node, at the density the plant is filled at, against the substance's floor; `FS2220` names the node, the height, the shortfall and the pressure to state --- the static head plus the half-bar margin expansion-vessel sizing uses (Flamco, Reflex, IMI Pneumatex, after EN 12828), in whole tens of kPa. On the measured loop: *'N4' is 32 m above 'N1', which puts it 312 kPa below the lowest pressure water can be at. State a pressure on 'N1' of at least 370 kPa.* The demo corpus states no height, so nothing in it changed. `WellPosednessTests.ATallPlantWithNoFillPressureIsNamedBeforeTheSeed` pins the message; its sibling pins that a covering `p=` is silent. |
| S-57 | 2026-09-14 | 2026-09-14 | — | [`32`](32-steady-state-newton.md), [`31`](31-solver-architecture.md), `D-92` | **A fixed-flow constraint's target is the seed's flow estimate, not the duty ratio the statement actually makes** | `D-92` rewrote a `FixedFlow` row with `power`, `in` and `out` all stated as a residual on the branch flow, and the value it pins the flow *to* is `BranchFlows.Estimate(graph)[branch].Magnitude` — the same field `SolutionSeed` starts Newton from. The statement itself fixes the flow at `abs(power) / abs(h(out) − h(in))`, three constants, and needs no estimate at all. Today the two coincide: the header's report pins `HE_AHU` at 0.287095 kg/s and `HE_RAD` at 0.358869, which are the duty ratios exactly, because `BranchFlows.Duty` is what fed those branches. But `S-53` records the same field copying a whole circulation onto both inlet legs of a three-way valve, and the guard (`Basis > Nominal`, magnitude above `FlowZero`) checks only that the estimate came from *somewhere* better than a nominal, not that it came from the duty. An estimate wrong by a factor of two would then be the equation's answer rather than its starting point, and the solve would converge to it. A row in the Jacobian that depends on a seeding heuristic is the coupling every seed fix since `S-26` has been removing in the other direction. | **Closed in `BranchFlows.RatedFlow`.** The arithmetic `ṁ = |Q̇| / |h(out) − h(in)|` has one public home; the seed's `Duty` hands it whichever inlet it decided on, and `EquationSystem.Constraints` hands it the three stated constants directly, so the row's target is the duty ratio whatever the seed did to the branch. Measured: every sample's report is byte-identical, which is what the entry predicted — the two agreed to the last digit wherever a `FixedFlow` was flow-form — and the fourth plant's five pinned flows land on their hand figures. The guard changed with it: the row is flow-form whenever the constants determine a flow, not when the seed's basis happened to be above `Nominal`. |
| S-59 | 2026-09-14 | 2026-09-14 | — | [`31`](31-solver-architecture.md), [`24`](../20-core-domain/24-auto-sizing.md), [`23`](../20-core-domain/23-topology-and-graph.md) | **The omitted-duty closure ran after the first count, so the count promoted the power the closure was about to size and every promotion after it shifted by one** | **Closed in `OuterLoop.Prepare`.** What the code meant to do: close the one omitted duty from ΣQ̇ = 0 (`D-91`'s working change) and let the count treat the source like any other exchanger with a stated duty. What it did: `Apply` computed `WellPosedness.Check` first and `CloseEnergyBalance` second, so on the bootstrap pass the count saw `HS_B.power` free and `HS_B`'s `FixedFlow` promoted *it* — `Candidates` offers an exchanger's own power before any pump — and the pump sizer, seeing `PU_B.head` unclaimed, sized it. On the next pass the power was sized, so the constraint needed a pump; `PU_B.head` was now sized too, so it was not free; and the first free pump in graph order was `PU_AHU`. The report read `FixedFlow on HS_B -> PU_AHU.head`, `HE_AHU -> PU_RAD.head`, `HE_RAD -> PU_DHW.head`, `HE_DHW -> no promotion`, over-specified by 1, with a redundancy direction naming both source branches. Why that is wrong physically: a boiler's outlet temperature is held by its own pump's flow, and a sized head is never released once the overlay holds it, so a one-pass ordering slip became a permanent wrong plant. Found on the fourth plant (two pumped sources, three pumped consumers) by printing the promotions before and after `Prepare`. Fixed by running the closure on the bootstrap graph *before* the first count and re-lowering when it added a duty; `Apply`'s own call stays and is idempotent. Measured: the plant counts 64/64 with every constraint on its own pump; the corpus is unchanged. The second half of the finding is `S-45`'s residue and stays there: a `FixedFlow` with no free local pump reaches across the plant for a consumer's, which turns one wrong claim into a cascade where a diagnostic naming the missing local pump would have stopped it. |
| S-58 | 2026-09-14 | 2026-09-14 | — | [`22`](../20-core-domain/22-component-model.md), [`36`](36-numerics-and-convergence.md), [`32`](32-steady-state-newton.md) | **A junction's arriving enthalpy was the plain average of its inlets, not their mass-weighted mix, so no valve position could move a mixed temperature** | **Closed in `EquationSystem.Arriving`, and it was the whole of the header.** What the code meant to do: a node fed through a three-way valve has no single upstream node, so the enthalpy arriving at it is the inflow-weighted mix of the nodes at the valve's other ports, `Σ ṁᵢ hᵢ / Σ ṁᵢ` — a mixing tee. What it did instead: it weighted each inlet by `Smoothing.ForwardShare(ṁᵢ)` *alone*, and `ForwardShare` is a 0-to-1 smooth step that reads exactly 1 for any inflow above one gram per second. Two inlets both flowing in therefore weighed 1 and 1 whatever they carried, and the mix was `(h_a + h_b) / 2`. Why that is wrong physically: a valve at 40 % travel passing 0.167 kg/s of 80 °C water and 0.063 kg/s of 30 °C water delivers 66 °C, not 55; the split is the only thing a mixing valve controls, and this made the split irrelevant to the temperature. How it was found: the `pos-0.7` experiment in `diagnostics/scratch/` — position stated, `in` dropped — *converged* and both coil inlets solved to **230 645 J/kg**, the same figure for two different valves, which is `(335 238 + 126 051) / 2` to the joule: the average of the 80 °C supply and 30 °C return headers. What it explained: every symptom `S-48` and `S-51` had chased. `MixedInlet on HE_AHU -> TV_AHU.position` asked the position column to move a temperature the column could not touch, so Newton read a residual of 5 K with a sensitivity of nearly zero (the floor's) and ran the position to a stop — to 1 on the 80 °C sample and to 0 on the 60 °C one, since the sign of a negligible derivative is noise. `m2-cooling-loop` never showed it because its mixing happens at an explicit node whose energy balance sums `ṁh` per branch, and a valve at a stop has one inflow, for which the average *is* the answer. Fixed by weighting with `ṁ · ForwardShare(ṁ)`, which is `max(0, ṁ)` away from zero and C¹ through it; the 1 mg/s floor keeps the quotient defined while every other port is an outflow. **Measured:** the header sample at `out=60` converges in **one** Newton iteration and three sizing passes at a scaled residual of 7e-10 and rank 39, landing on `01`'s figures — coil flows **0.2871 / 0.3589**, header draws **0.1914 / 0.2392**, source **0.4307 kg/s** against 0.4306, valves at **0.633 / 0.620** of travel, heads **6.48 / 8.29 m** — and the 80 °C variant converges likewise on 0.1147 / 0.1434 / 0.2581, the two designs `S-53` said were both valid. `m2-cooling-loop`, `m2-simple-loop` and `m4-storage-header` are unchanged; `m2-substation` is still `S-32`. Pinned by `OuterLoopTests.TheDistributedHeaderReproducesTheVisionsFiguresEndToEnd`, which reads the solved unknowns by label and asserts both positions strictly inside the travel. **What is still open behind it:** the heads are high because an equal-percentage valve at 0.6 travel drops 24–45 kPa where its full-open figure is 1–2 kPa, and the authority rule sizes at full open — a sizing-rule question for `24`, not a solver one; and `S-53`–`S-57` were each measured on variants and are not closed by a sample converging. The high-authority mixing valves this entry observed (24–45 kPa across legs that drop 1–2 kPa fully open) were the mild form of `C-104`, closed by `D-122`: the header's valves now take Kv 4 at 0.85/0.82 of travel and the pumps 4.7/5.2 m. |
| S-51 | 2026-09-09 | 2026-09-14 | — | [`32`](32-steady-state-newton.md), [`24`](../20-core-domain/24-auto-sizing.md) | **The injection header is square, full rank and correctly promoted, and still will not converge: its mixing valve is driven to its stop** | **Closed by `S-58`**, with the seed work it produced kept. *Position 1 is the wrong direction and that is the thing to explain* was the right sentence and the explanation was one layer down: `TV_MAIN.position` could not move the 60 °C setpoint because the enthalpy arriving through the valve was the average of its inlets at every position. Chord orientation by the pump (`Field.Orientation`) and the re-span barring a branch at rest stay, covered by their two tests; they made the seed physically right, and a right seed is what let the `pos-0.7` experiment converge to a wrong number cleanly enough to be read. `BranchFlows.Duty` on a lone-`out` source, the remaining item here, is `S-53`'s fix (1) and lives there. |
| S-48 | 2026-09-09 | 2026-09-14 | — | [`01`](../00-foundation/01-vision-and-scope.md), [`23`](../20-core-domain/23-topology-and-graph.md), [`24`](../20-core-domain/24-auto-sizing.md) | **The header asks each consumer for a 50 C inlet from a header whose temperature nothing holds, and states its differential and its source outlet as two facts about one loop** | **Closed by `S-58`.** Four appended measurements and three rebuilt fixtures, and the finding that survives is the last one: with `D-90` paying the level the circuit was square and full rank, every constraint answered by the actuator that reaches it, and it still ran its consumer valves to a stop. The reason was never in the counting, the promotion or the fixture — the junction's arriving enthalpy was an unweighted average, so the position column moved nothing a `MixedInlet` could feel. The plant `01` describes is a plant that exists, and it solves as written with `HS1 out=60`, no header pump and no setpoint. The differential-pressure and setpoint half of this entry (`D-87`, `S-44`) stays as built; the injection fixture stays out of `samples/` because the sample no longer needs it. |
| S-50 | 2026-09-09 | 2026-09-09 | — | [`32`](32-steady-state-newton.md), [`36`](36-numerics-and-convergence.md) | **A promoted valve position is seeded on its upper bound, where the complementary leg's derivative is fifty times flatter** | **Closed in `SolutionSeed.Promoted`.** `S-26` established that a promoted column must not start at zero, because zero is a *bound* and the forward difference dies there; the fix read the component's own value instead, and walked into the mirror image. `ThreeWayValve.Position` defaults to **1** --- right for a valve nobody controls, worst for one the solver has to move. On the equal-percentage characteristic every promotable position uses, phi(1) = 1 with slope 3.91 while the complementary leg sits at phi(0) = 0.02 with slope **0.078**, so the column is dominated by the control leg and carries almost no signal about the bypass --- and Newton pushes the position further into the stop it started on. Measured on the injection header: both consumer valves pinned at 1 (`FS3008`) while the answer they wanted was (50-30)/(60-30) = **0.667**. A promoted parameter bounded on **both** sides whose component value lies on either bound is now seeded at the middle of its range. Mid-travel is symmetric --- both legs at 0.141, both slopes 0.553 --- and it is where a valve sized for authority 0.5 is meant to sit. Nothing else in the corpus is touched: `kv` is bounded below only and `head` not at all, so both keep the component's value, which is what `S-26` requires. **Measured**: both consumer valves stop pinning and the header's residual falls from 0.4555 to 0.2142; no sample moves. **One thing it does not reach, pinned by its own test**: an unsized pump holds head 0, so a promoted head still starts on the bound `S-26` warned about --- the midpoint needs two bounds and `head` has one. `APromotedParameterBoundedOnOneSideKeepsTheValueItsComponentHolds` records it. |
| S-54 | 2026-09-14 | 2026-09-14 | — | [`32`](32-steady-state-newton.md), [`22`](../20-core-domain/22-component-model.md), `D-91` | **The closed-circuit duty check sums user-facing role capacities before lowering their physical signs** | The automatic-duty case proves the component side is correct: `HE_AHU load power=24` and `HE_RAD load power=30` appear in the sizing basis as **−54 kW from the other duties**, so omitted `HS1.power` becomes **+54 kW**. State that same balanced source explicitly as `HS1 heat_exchanger power=54 kW`, however, and well-posedness refuses the solve with `FS2203`: **108 kW with nowhere to go**. Rewriting only the loads as neutral `heat_exchanger power=-24/-30` removes the diagnostic and lets Newton run. The executed pair therefore isolates the error: the physical sum is `+54 - 24 - 30 = 0`, while the diagnostic computes the raw written sum `54 + 24 + 30 = 108`. `ComponentFactory.Exchanger` already lowers `load`, `cooler`, `radiator` and `chiller` to `-abs(power)`; `OuterLoop.CloseEnergyBalance` and `HeatExchanger.EvaluateEnergyInjection` consume that signed `HeatExchanger.Power`. `WellPosedness.Imbalance` instead calls `HydraulicPartition.Stated(element, "power")`, losing the written role before it totals an uncoupled circuit. Thus automatic and explicit spellings of the same physical system disagree before Newton, and the ordinary role spelling `load` is still broken in the exact closed-balance case `D-91` was meant to make safe. This sign leak is real but separate from `S-53`: after neutral negative loads remove `FS2203`, the radiator seed and sized flow remain **0.7177 kg/s**, exactly twice its correct 0.3589 kg/s circulation. | **Closed in `WellPosedness.Imbalance`**, which now reads `HeatExchanger.Power` — the signed value `ComponentFactory` lowered — instead of the written `power` off the parameter map, so the role word is applied once and the diagnostic totals the same quantity the energy balance does. The fix landed in the same working change as this entry and the entry was not updated. Measured on the header with the source stated as `heat_exchanger power=54 kW` and as `heater power=54 kW` beside `load power=24/30`: no `FS2203`, both converge in one iteration on the same 0.4307 kg/s. `WellPosednessTests` carries a `load power=30` case. The refactor this entry asked for — one effective-duty operation every consumer shares — is not done and is not needed for correctness now that only one caller reads the written value; it stays a preference rather than a defect. The doubled radiator seed mentioned at the end is `S-53`. |
| S-49 | 2026-09-09 | 2026-09-09 | — | [`36`](36-numerics-and-convergence.md), [`23`](../20-core-domain/23-topology-and-graph.md) | **The seed's flow field is mass-consistent without knowing which of a three-way valve's ports is the common one, so it can seed a mixing valve as a diverter** | **Closed in `SolutionSeed.Field.Span`.** The mechanism was in the spanning forest, not in the balance. `Solve` gives every chord a free unsigned magnitude and *solves* each vertex's parent branch to close that vertex's balance --- so the parent branch is the one that ends up carrying what the others sum to, which at a three-way valve is the definition of the common port. The breadth-first walk chose the parent arbitrarily: on the injection header it reached `TV_AHU` through `PA2` and made the `a` leg the parent, seeding `ab` 0.2871, `a` 0.5742, `b` 0.2871 kg/s --- a *diverting* split, balanced and wrong, whose Kv law then sat at a scaled residual of **-3.345** and walked a node out of the water domain at iteration **zero**. `Span` now **holds** any edge that would claim a valve by anything but its common port, and uses a held edge only once no preferred edge is left --- a preference, not a constraint, so a valve reachable no other way is still attached. `CommonBranch` reads the port name directly, which `D-88` settles for the *common* port specifically: `ab` is port 0, so positional binding gives it to the first connection written, and that is the common leg for a mixing and a diverting arrangement alike. **Measured**: `TV_AHU` now seeds `ab` 0.5742 with `a` and `b` at 0.2871 each, and the injection header goes from `NonFinite` at iteration 0 to running its full iteration budget. No sample moves. The consumer flows land on `01`'s own figures --- `TV_AHU` draw **0.1914** kg/s against 0.1914, loop **0.2871** against 0.2871, `TV_RAD` loop **0.3589** against 0.3589. |
| S-44 | 2026-09-09 | 2026-09-09 | — | [`23`](../20-core-domain/23-topology-and-graph.md), [`12`](../10-language/12-grammar.md), [`36`](36-numerics-and-convergence.md) | **The distribution header is well posed and still not solvable: the setpoint it needs has no word in the language, and every way of stating it fails at the first Newton step** | Both clauses of that title are now measured false, which is why this is closed rather than appended to a fifth time. It **no longer fails at the first Newton step**: `S-46` rebuilt the seed's pressure half and the variants run 4 to 15 iterations before they stop. And the missing **setpoint is not what blocks** --- swept as two stated node pressures at 3, 5, 8, 10, 15, 20, 30 and 60 kPa, every value fails, best 0.8616 at 5 kPa. The sweep does confirm the arithmetic: the taps are 12 m and 18 m of DN25, so at the design 0.287 kg/s (24 kW over 20 K) they run 0.5 m/s at Re 24,000 and cost about 3 kPa for supply and return together, and 60 kPa across a branch needing 3 drives ~4.5x design by sqrt(dp) --- measured tap flow 0.907 kg/s against a design 0.191. Right physics, second-order effect. | Closed and succeeded by `S-48`, which states what the measurements actually found: a consumer inlet temperature nothing can hold, and a differential and a source outlet that are two statements about one loop. What this entry established stands --- `S-38`'s header differential is real, the seed was the first-step blocker and is fixed, and the language still has no word for a differential setpoint, which `S-48` carries forward as a want rather than as the blocker. |
| S-46 | 2026-09-09 | 2026-09-09 | — | [`22`](../20-core-domain/22-component-model.md), [`24`](../20-core-domain/24-auto-sizing.md), [`36`](36-numerics-and-convergence.md) | **Everything that asked a component what it resists was told zero by every valve in the circuit** | A pressure relation is written `p_in - p_out - dp(law) = 0`, so evaluating it over a flat pressure field leaves `-dp(law)` and the component has stated its own drop without anyone knowing its kind. That is the whole idea behind `BranchResistance`, and it reads the row by `EquationKind.Pressure`. **A valve does not write one.** It writes `mdot - Kv*phi(x)*sqrt(dp*rho) = 0`, an `EquationKind.ComponentConstraint` whose residual is in kg/s, because what a valve fixes is a flow at a differential and not a differential. So the filter matched nothing, the method returned 0, and every caller was told that a control valve resists nothing --- silently, with no kind named anywhere for the omission to show up in. Two callers: the seed's pressure walk, where it collapsed a three-way valve's three legs onto one pressure (`S-25` and `S-35` again, from a new direction), and `OuterLoop`'s branch-resistance total, where a run's resistance omitted its own control valve --- the component that usually dominates it. A second defect in the same six lines: the row was located by `EquationDeclaration.Index`, which is assigned at **assembly**, not by its position in the component's own residual buffer. A tank declares its first equal-pressure row with an index of 0 and writes it third, behind energy and mass, so a tank's energy imbalance in watts was returned as though it were a pressure in pascals. | What a valve resists is now **solved for** rather than read off. `BranchResistance.Across` differentiates every one of a component's rows with respect to every one of its port pressures, keeps the rows that move --- whatever kind they call themselves --- and, when there is exactly one fewer of them than there are ports, Newtons the port pressures until those rows are satisfied at the flows the component is carrying. One short because a pressure field has a datum; anything else (a four-port exchanger, two independent sides) returns zeros rather than an invented answer. No kind appears, a tank's linear rows settle in one step and a valve's square root in a handful, and the row ordinal is now read from the component's own declaration order. The excursion is capped at 10 kPa per component, which is `S-44`'s second half. |
| S-38 | 2026-09-08 | 2026-09-09 | — | [`23`](../20-core-domain/23-topology-and-graph.md), [`24`](../20-core-domain/24-auto-sizing.md), [`32`](32-steady-state-newton.md) | **A secondary's flow constraint and the pump head it promotes were believed to be one freedom too many** | Each consumer states `power`/`in`/`out`, which `WellPosedness` turns into a `MixedInlet` promoting the three-way valve's `position` and a `FixedFlow` promoting the pump's `head`. One consumer was full rank, two were `Singular` at iteration 0 at every line-search floor from 1/64 to 1e-14, and the proposed fix was to *size* the head rather than promote it. **None of that was the defect.** The promotions were branch-local and correct the whole time --- `HE_AHU` claims `PU_AHU`, `HE_RAD` claims `PU_RAD` --- and the one-versus-two-consumer asymmetry was an artefact of `S-41`: with no energy balance being dropped, a single consumer happened to come out square. Once that was fixed the same measurement gave one free unknown for **both** one consumer and two, which is the signature of a plant-level freedom rather than a per-consumer one. The entry's own arithmetic was also wrong: a constraint and its promotion do not leave the counting table together. Only the unknown leaves, the `FixedFlow` row stays, and the dropped enthalpy level pays for it --- `-1/0`, not `-1/-1` --- which is why the one implementation attempt left every sample over-specified by one and was reverted. | **Closed by measurement rather than by code, and the answer was in `FS2211` from the beginning.** What nothing pinned was the **header differential pressure**. Stating `N3 node p=310` against `N1 node p=250` --- a 60 kPa differential --- and freeing `PU_MAIN`'s head gives 45 unknowns, 45 equations, **rank 45, nothing free and nothing dependent**, and the circuit converges in one iteration at a scaled residual of 2.85e-10. That is what a variable-flow distribution header is: a differential-pressure setpoint the main pump modulates to hold. Stating the pump's head instead says the pump runs at fixed speed and leaves the differential floating with load, so the sample was asking the solver to find a quantity it had never been told. `FS2211` advised *"add a pressure on N3"* on every run for three sessions and was read as a counting artefact rather than as physics. **This entry is closed as much for what it became as for what it found.** It accumulated four strata of diagnosis, each superseding the last, until its title and opening paragraph stated a cause its own final paragraph refuted --- so a reader arriving cold got the wrong answer from the first two hundred words. That is `S-40`'s lesson repeated at ten times the length: **a guessed cause left standing in an open entry is a liability, and appending a correction does not remove it.** An entry that has been rewritten three times should be closed and succeeded, not extended. What actually remains is filed as `S-44` (the physics the language cannot yet express) and `S-45` (the cross-branch promotion), neither of which is about the constraint/promotion pair this entry is named after. |
| S-43 | 2026-09-09 | 2026-09-09 | — | [`36`](36-numerics-and-convergence.md), [`32`](32-steady-state-newton.md) | **The rank instrument and the report that calls it used different rank criteria, so one report contradicted itself two lines apart** | `SolveExplanation` measured a pivot against the **largest pivot** and called the header deficient; `NullDirection.Of` measured it against the **matrix norm** scaled by `Tolerances.JacobianSingular` (1e-12) and returned no direction at all. On `m2-distribution-header` with one pump head stated --- largest pivot 385.6, smallest 2.754e-9 --- the norm-scaled floor lands near 1e-9 and calls that collapsed pivot live. So the report printed `rank 43: 1 unknown(s) nothing determines, 1 equation(s) the others imply` and then `(none found)` under **both** direction headings. The instrument went blind exactly where `S-38` needed it to speak, and the silence looked like "there is no direction" rather than "I used a different rule from the one that said there was". | **Closed by having one criterion instead of two.** `NullDirection.RankTolerance` is public, relative to the largest entry --- which under full pivoting *is* the elimination's first pivot --- and `SolveExplanation` now references it rather than keeping a copy. `DenseLu` keeps `Tolerances.JacobianSingular` and should: it is deciding whether a Newton **step** can be taken, and a pivot near the arithmetic's own noise is what stops it. Rank is a different question. The constant is not delicate --- measured, a circuit that solves runs a smallest-to-largest pivot ratio of 4e-2 to 6e-4 and a deficient one runs 6e-12 to 2e-14, six orders of clear water --- and it sits deliberately between machine epsilon and the forward-difference noise floor near `sqrt(eps)`. All 1426 tests pass unchanged, so the wider criterion reports nothing new on a circuit that was already right. **The disagreement, not either constant, was the defect**: two owners of one rule drift, and the drift is silent. |
| S-41 | 2026-09-08 | 2026-09-08 | — | [`23`](../20-core-domain/23-topology-and-graph.md), [`36`](36-numerics-and-convergence.md) | **A closed circuit annotated with its own pressure datum was read as open, so the energy redundancy every closed circuit has was never removed** | `D-86` established that a stated pressure on an *interior* node is a datum and admits no mass, and fixed `HasUnknownFlux`, `WellPosedness.Count` and `SolutionSeed.Free` --- three sites. `HydraulicComponent.IsClosed` was a **fourth**, and kept the sentence `D-86` overturned, three lines above the comment that overturns it: *"a stated `p` lets mass in to hold the pressure"*. So `m2-distribution-header`, whose `N1 node p=250` is an expansion vessel connection, reported `open` with **zero boundaries** and `unknown flux False` --- a contradiction on the face of it. `NeedsEnthalpyLevel` returns false at its first guard for an open part, so **no energy balance was dropped**. A closed circuit's energy balances are always one short of independent: add the same enthalpy offset to every node and each balance is satisfied unchanged, because the offsets cancel against the mass balances, and a duty relation `m*(h_out - h_in) = Q` never sees it at all. That undropped redundancy is precisely what `FS3010` had been reporting --- `N1`, `N2`, `N3` energy balances at weight 1, thirteen more behind them, and the two mass balances the offset argument leans on at 0.91 and 0.73. | **Closed by dropping the pressure clause from `IsClosed`.** A `supply` or a `return` is already caught by the boundary clause; a stated `flow` still opens the part, because it genuinely injects mass. The header now reports `closed`, drops one energy balance as its level, and counts **45 unknowns against 44 equations --- under-specified by 1**, refused before the solver with `FS2211` instead of met as a zero pivot at iteration 0. **The deficiency did not change; where it is reported did**, and the counting table now agrees with the rank measurement (44) that had contradicted it. Found by the first run of `SolveExplanation`, from two lines of one report: a row direction dominated by energy balances beside `less enthalpy levels 0`. Fixing it also exposed that the sample's two loads were written `power=+24` and `power=+30` against `in=50 out=30` --- fluid cooling, duty claiming heating --- which `FS2203` caught the moment the circuit was correctly read as closed, at 108 kW. `m2-simple-loop` and `m2-substation` had it right (`power=-30`, `power=-150`). |
| S-33 | 2026-09-07 | 2026-09-08 | — | [`32`](32-steady-state-newton.md), [`23`](../20-core-domain/23-topology-and-graph.md) | **The distribution header is rank-deficient at the seed, and the counting pass calls it square** | **Closed by `S-41`, and the disagreement this entry recorded was the whole finding.** Counting said square, a full-pivot elimination said one short, and the two contradicted each other in both directions on the same script. The Jacobian was right and the count was wrong: an energy balance that should have been dropped as the closed circuit's level was still in the system. This entry's own strongest clue was recorded and not followed --- *"the row left dependent is an **energy** balance (`N2`), not a pressure one"*, and *"points at `D-65`'s territory (a closed circuit fixes every enthalpy difference and no absolute value)"*. That is the answer, written down two sessions before it was acted on. All three hypotheses it tested and rejected stayed rejected. |
| S-40 | 2026-09-08 | 2026-09-08 | — | [`32`](32-steady-state-newton.md), [`36`](36-numerics-and-convergence.md) | **`FS3010` reports nothing on a circuit that demonstrably has a row null direction** | The rebuilt `m2-distribution-header` terminates `Singular` and emits `FS3002` and `FS3009`, but **not** `FS3010`. The direction is there: a finite-difference Jacobian taken at the same point names seven rows, mass balances at weight 1, which is exactly what `S-39` was found from. `NullDirection.Redundancy` transposes and delegates to `Of`, and on the transpose the elimination finds nothing above its significance cut. So the user is handed only the column half --- the half `S-36` exists to warn against trusting alone. | **Closed, and not by touching `NullDirection`.** With the header's circuit correctly read as closed and its redundant mass balance dropped (`D-86`), the elimination on the transpose has a different matrix and finds the direction: `FS3010` now reports `N1 energy balance + N2 + N3 + 0.91 x TV_RAD mass balance + 0.78 x N5 energy balance ... and 11 more`. The suspicion recorded here --- that `Of`'s significance cut was calibrated for column scales and wrong for row ones --- was **not** the cause and is not evidence of anything. The instrument was fine; it was being handed a matrix built from a mis-modelled circuit. `ASingularCircuitIsToldWhichEquationTheOthersAlreadyImply` now asserts the presence it was written to assert the absence of. |
| S-39 | 2026-09-08 | 2026-09-08 | — | [`23`](../20-core-domain/23-topology-and-graph.md), [`36`](36-numerics-and-convergence.md) | **A pressure datum written on a junction silently turns a closed circuit into an open one, and the circuit is then deficient by exactly one** | `HasUnknownFlux` is true when a node *carries a mass balance* and states a pressure. For a `supply` or a `return` that is right --- mass genuinely crosses there. For a plain interior `node p=250` sitting on a three-way junction it is not: nothing enters, the circuit is closed, and its mass balances still sum to an identity. But the partition now looks open, so `EquationLayout.Redundancies` drops **no** balance --- measured, `0 balances dropped` on a 46-unknown header --- and no external-flux unknown appears to take up the slack either. The row null direction says so exactly: `NM_AHU + N1 + N3 mass balance`, every weight 1, same sign, which is the global conservation identity and nothing else. **Measured decisively**: the same fixture with the datum moved from the junction `N1` to the two-connection node `N2` stops being `Singular` at all. The rule is deliberate --- `WellPosedness` documents the carries-a-balance test in a comment written against it --- so this is a decision to revisit, not an oversight. | **Closed by `D-86`.** `HasUnknownFlux` now tests the node's *kind*: only a `supply` or a `return` with no stated `flow` admits unknown mass. An interior node's `p=` is a datum. `m2-distribution-header` keeps its datum on `N1`, the return header's junction, which is where an engineer puts the expansion vessel --- the workaround of moving it to a mid-branch node is gone. **The rule was written in three places and the fix had to change all three**: `HydraulicComponent.HasUnknownFlux` decides the mass-balance drop, `WellPosedness.Count` decides the flux *unknown*, and `SolutionSeed.Free` decides which nodes the seed may push flux into. Changing the first alone left the table an equation short and advising a pressure on a mid-branch node; changing the first two left `m1-syntax-tour`'s `NB2` seeded 0.167 kg/s out of balance, so the seed's divergence-free claim was false. |
| S-36 | 2026-09-08 | 2026-09-08 | — | [`32`](32-steady-state-newton.md), [`36`](36-numerics-and-convergence.md) | **`FS3009` names the wrong half of a singular system, and three sessions of pump theories came from believing it** | The null direction reports which *columns* move together. On the header it has always named pumps --- `PU_AHU.head - PU_MAIN.head + PU_RAD.head`, then `PU_RAD.head - 0.12 x TV_RAD.position + 0.1 x ...` --- and `S-33` and `S-34` spent four measured pump arrangements on that lead. Transposing the Jacobian and eliminating again gives the **row** null vector, which says which *equation* the others already imply, and on the minimal reproduction it is: `+1.00 NM_RAD mass balance`, `-0.85` on each of the three subcircuit energy balances, `-0.43` on each of the six header ones --- **one mass balance against every energy balance in the circuit**. Not a pump, not a valve. A square system has exactly one of each when it is deficient by one, and the column vector is only where the elimination happened to land; the row vector is the redundancy itself. | **Closed: the instrument is built and the defect it was chasing now has a name.** `NullDirection.Redundancy` transposes and reuses the same full-pivot elimination, and `FS3010` reports the row direction beside `FS3009`. The row vector this entry measured --- *one mass balance against every energy balance in the circuit* --- is exactly `S-39`: the header's pressure datum sat on a junction, so a closed circuit was read as open, no redundant mass balance was dropped, and the global conservation identity showed up as the redundancy. Every cause this entry eliminated stayed eliminated. The remainder splits cleanly: `S-39` owns the defect, `S-40` owns the one place the instrument is still silent, and `S-33` owns the counting-versus-rank disagreement. **This entry's real contribution was the distinction, not the fix** --- a square system deficient by one has a column direction and a row direction, and only the row one names the redundancy. |
| S-1 | 2026-09-03 | 2026-09-08 | — | [`31`](31-solver-architecture.md) | **`SolveContext` and `UnknownDeclaration` are named as this tier's and defined nowhere** | **Closed by ratification.** `31` now carries `SolveContext`'s shape beside `UnknownDeclaration`'s, and says outright that both are defined in `FluidScript.Core.Components` and adopted here rather than specified here first. That is the honest resolution of the choice this entry posed: six components implement the shape, it has held through promotion, component-owned scalars (`D-74`) and the no-allocation rule, and changing it now would cost six rewrites to buy nothing. `31` also records the three properties that are load-bearing and invisible in the field list --- it is a `readonly ref struct` because `EvaluateResiduals` runs N+1 times per iteration and must allocate nothing; `Flows` is signed and **per port** so a component never learns which way its branch was written (`C-25`); and `Parameter(index, own)` is the only correct read of a promotable value. |
| S-5 | 2026-09-03 | 2026-09-08 | — | [`36`](36-numerics-and-convergence.md) | **"Continuous in value and first derivative" does not say how to check it, and the obvious check fails on correct code** | **Closed.** `36` gains *How to test C¹, and how not to*, which states the failing form and the working one. The obvious test --- finite differences either side of the join, asserted close --- measures the derivative's *sweep* across the band rather than the property: over the valve's 100 Pa regularisation the true derivative runs from 0 to about 3.75e7, so the test fails on correct code. The working form probes the one-sided derivative at the join with a **shrinking step** from each side and asserts the two sequences reach the same limit, relative to that limit. `36`'s acceptance line now points at it, so the next implementer does not have to write the wrong test first to find out. |
| S-12 | 2026-09-04 | 2026-09-08 | — | [`36`](36-numerics-and-convergence.md) | **Per-branch flow scaling is right and the reason `36` gives for it does not hold** | **Closed by correcting the reasoning rather than the code.** Per-branch scaling was always right; the justification was not. `36` now says so explicitly: the discarded claim --- that one scale lets the test "declare success while that balance is off by 10 %" --- does not follow from this table's own `newton.residual_tol` of 1e-8, which permits 1e-7 kg/s absolute, two parts per million of the bypass. The two reasons that hold are written in its place: the convergence test stops being a **relative** statement when one scale spans 200x, and the bypass's Jacobian columns come out 200x smaller, which is a conditioning problem that makes `newton.step_tol` meaningless on the small branch. **An argument that reaches a correct conclusion from a false premise is worth fixing** --- the next person to touch scaling would have checked the number and doubted the design. |
| S-30a | — | [`32`](32-steady-state-newton.md) | **The cooling loop's singularity is the seed, and it is the temperature *walk* rather than the datum** | Proven by substitution: replacing five node enthalpies with `01`'s own figures -- the mixing node at 20 &deg;C between a 6 &deg;C primary and a 50 &deg;C return -- stops the solve going `Singular` and lands it on **20.13 &deg;C** at the mixing node against `01`'s 20, and **0.07626 kg/s** on the recirculation branch against `01`'s 0.0763. The residual left is entirely hydraulic (`3WV`'s Kv law), so the energy side is solved. What the stock seed does instead: `Datum` takes the *first stated node temperature*, which here is the 6 &deg;C primary boundary, and every unstated node is then `datum - NominalRise x steps`. So a secondary loop `HE1` states as 20/50 was seeded at **2 to 6 &deg;C** -- every node on the wrong side of the mixing split, and the node *after* the pump colder than the node before it. | **Closed by `S-30b`**, and two earlier fixes were built, measured and reverted on the way. **(1) Placing a component's stated `in`/`out` on the nodes at its own ports**: correct as far as it goes and not enough alone -- the unplaced neighbours stay near 6 &deg;C, the seed then carries a 46 K jump across one component, and the first Newton step throws `N1` out of the fluid's range (`NonFinite` at 1, where it had been `Singular` at 11). **(2) That, plus a datum taken as the mean of every temperature the script names**: makes `m2-simple-loop` converge **at the seed** -- zero iterations, norm 3.7e-9 -- and still walks `3WV__P1` down to **0.20 &deg;C** and the substation's `SP__HX1` up to **115 &deg;C**, both out of range, failing earlier (7 and 2 iterations) and harder than before. One sample better and two worse is not a trade worth keeping, so both were reverted. The part that survives every attempt is that **`- NominalRise x steps` is the wrong shape**: a monotone walk away from a single global level cannot serve a circuit whose nodes sit at several levels, however well that level is chosen. What it needs is propagation from the anchors -- stated node temperatures and stated port temperatures -- along the branch paths, and the honest reason that is not in this entry as a fix is that the rule for a *mixing* node, adjacent to anchors at 6 and 50, is a modelling choice rather than an obvious one. |
| S-34 | 2026-09-08 | 2026-09-08 | — | [`23`](../20-core-domain/23-topology-and-graph.md), [`01`](../00-foundation/01-vision-and-scope.md) | **Every pump arrangement of the distribution header is rank-deficient by one, so the pumps are not the cause** | Measured across four variants, each built and eliminated at the seed. As shipped (3 pumps): rank 27 of 28. Plant pump removed, `HS1` still stating `out`: counting **refuses** it, over-specified by 1. Plant pump removed and `HS1.out` dropped (2 pumps, one per branch, which is the arrangement good practice asks for and the one the user identified): still **rank 26 of 27**. Plant pump kept and `HS1.out` dropped, so `PU_RAD` is sized rather than promoted: still rank 26 of 27. Adding a second pressure datum on the isolated side: counting refuses that too. **Four arrangements, four deficiencies of exactly one** --- and the conclusion drawn from that, that "no arrangement counting will accept is one the Jacobian will", is now **falsified**: one is. Every arrangement tested moved the *pumps* around while leaving the source in line with the header, and `F-21` is that the source needs a **loop of its own**. Measured with the consumers written as real mixing circuits, in line is rank deficient and own-loop is **full rank** at 44 unknowns. | **Closed, and it is the entry that took the longest to be wrong in a useful way.** Four pump arrangements, four deficiencies of exactly one -- and the pumps were never the cause, which this entry had already concluded. What it could not yet name, the row null direction later did: the header carried **two** independent deficiencies at once, `S-39` (a pressure datum on a junction, which quietly opens a closed circuit) and `S-38` (two consumers sharing a header differential nothing states). Every measurement taken before those were separated reported one combined direction pointing at neither, which is exactly why four careful variants each came back deficient by one. The blocker this entry names last -- "attachment cannot express a mixing node" -- is discharged: the fixture writes both ends of each tap by hand and `F-16`/`F-17` are closed. What remains is `S-38` alone. |
| S-30 | 2026-09-07 | 2026-09-08 | — | [`32`](32-steady-state-newton.md), [`23`](../20-core-domain/23-topology-and-graph.md) | **The cooling loop is bounded, physical and still singular at 11 iterations** | **Closed.** `m2-cooling-loop` converges: measured this session at **1 iteration, norm 3.7e-10**, with `3WV` sized to **Kv 4** at authority 0.66. `01`'s figures are met -- mixing node **19.99 &deg;C** against 20, return **49.94** against 50, recirculation **0.0763 kg/s**, and `PU1` asking a plausible **6.4 m**. The diagnosis in this entry was right about the symptom and wrong about the cause: `position` pinned at 1 was not the solver failing to find a reachable path, it was the valve wearing bootstrap **Kv 630**, at which the valve restricts nothing and no position can balance the loop (`C-60`, `C-63`). The seed suspicion was separately real and closed as `S-30b`. Not an `M2a` blocker any more. |
| S-35 | 2026-09-08 | 2026-09-08 | — | [`32`](32-steady-state-newton.md) | **Putting any component in a three-way valve's bypass leg made a well-posed circuit singular at iteration zero** | `Steps` restarted its walk at each branch, so the *first interior node of every branch* took step 1 and the seed gave it the same pressure as every other branch's first node. Two branches leaving one junction element are adjacent **through** it, and that kind of adjacency is the kind a per-branch walk cannot see. A three-way valve is exactly that shape: with a component in each leg its three ports seeded to one pressure, `sqrt(dp)` went to zero, and the Kv law's derivative with respect to `position` went with it -- a column of zeros, `FS3009`, `Singular` after **0 iterations**. `m2-cooling-loop` escaped it only because its bypass leg is **empty**, which put that side on a branch *end* (`N2`, step 0) instead of an interior node. Measured: a `valve`, a `pipe`, and nothing at all in that leg gave three different outcomes on the same circuit, and the pipe and the valve were bit-identical to each other -- so it was never about Kv, balancing, or the valve law. | Each branch now starts its walk at **its own leg index at the element it leaves** rather than at zero, so the legs of a junction begin one step apart and their first interior nodes cannot agree. Within a branch nothing changes. That alone turned the failure into `NonFinite`, which exposed the second half: the band walked one-sidedly *down* from each level, so `Band` steps is 8 K below it -- harmless at 60 &deg;C and **below freezing at 6**, where the seed ends in a failed property call rather than a bad guess. Chilled water at 6 &deg;C is not exotic and the cooling loop is one, so the band is now **centred** on its level. All four leg topologies now behave identically and reach `01`'s figures -- mixing node **19.98 &deg;C**, return **49.93 &deg;C**, recirculation **0.0763 kg/s** -- with no sample's termination changed. `AComponentInAValvesLegDoesNotCollapseItsPortsOntoOnePressure` and `NoNodeSeedsOutsideTheRangeItsFluidIsDefinedOver` pin both halves. |
| S-30b | — | [`32`](32-steady-state-newton.md) | **The seed walked every node down from one global level, so a loop rated 20/50 started at 2 &deg;C** | `Datum` returns the first stated *node* temperature and every unstated node was `datum - NominalRise x steps`. On `m2-cooling-loop` that datum is the 6 &deg;C primary boundary, so the secondary -- which `HE1` rates 20/50 -- was seeded at 2 to 6 &deg;C, on the wrong side of the mixing split and with the node *after* the pump colder than the node before it. `Singular` at eleven iterations. | The level is now **propagated downstream from what the script names** rather than walked away from one number. Anchors are stated node temperatures and stated port temperatures; a node with no anchor takes the mean of the anchors reaching it from upstream, which at a mixing node is the two streams it mixes and everywhere else is the one thing feeding it. Branches meet at junction *elements* as well as at nodes, so a three-way valve links the node before it to the nodes after it -- without that the level anchored on an exchanger's outlet never crossed the valve, which was the 46 K jump that made an earlier attempt worse than doing nothing. **`m2-cooling-loop` now reaches `01`'s figures**: the mixing node at **20.13 &deg;C** against 20, the return at **50.07 &deg;C** against 50, the primary at 6.12 against 6. It no longer goes `Singular`, no promoted parameter runs to a bound on the way, and `m2-simple-loop` now converges **at the seed** in zero iterations. |
| S-33a | — | [`32`](32-steady-state-newton.md) | **A singular system was reported as singular "around" one component, which is usually the wrong one** | `FS3002` named `system.Unknowns[factored.SingularColumn]`, and `DenseLu` uses **partial** pivoting -- so the column it stops at is whichever its row search reaches first, not the one most responsible. On `m2-distribution-header` it named `PU_RAD` and suggested checking for a missing pressure datum, on a script that states one. What is actually undetermined there is `PU_AHU.head - PU_MAIN.head + PU_RAD.head`: no single name can express it. | `NullDirection` eliminates the Jacobian again with **full** pivoting and back-substitutes one null vector, and `FS3009` names the combination alongside `FS3002` rather than replacing it -- one code for why the run stopped, one for what it found. It rebuilds the matrix rather than keeping a copy, because `DenseLu.Factor` overwrites what it factorises and an *n*&sup2; copy per iteration to serve the last one is the wrong trade; N+1 residual evaluations on a run that is over is the right one. **It paid for itself immediately**: `m2-cooling-loop`, whose failure at iteration 11 had resisted diagnosis (`S-30`), reports a direction made of **enthalpies and branch flows** -- `3WV__P1.h + 0.83 N3.h - 0.8 N1.h - 0.47 b1 + 0.47 b2 + 0.47 b3` -- which is the energy side, not the pressure side, and is the first hard evidence about what that defect is. |
| S-31 | 2026-09-07 | 2026-09-07 | — | [`22`](../20-core-domain/22-component-model.md), [`24`](../20-core-domain/24-auto-sizing.md) | **A coupled exchanger created energy from nothing** | `EvaluateEnergyInjection` put the whole duty into side 1 and zero into side 2, with a remark saying so: "duty mode makes no claim about a second stream, and a coupled exchanger's -Q on the other side arrives with the rated model (P4.1)". But a script that wires `in2`/`out2` **and** states `power` has already made the claim -- 150 kW crossing an exchanger leaves one stream and enters the other. On `m2-substation` the district primary sat at **85 C from end to end** instead of returning at 45, because nothing ever took its heat away. | Side 2 now withdraws the same duty whenever the script wired it, with its **own** `ForwardShare` from its own flow -- the streams are usually counter-current, and a shared share would put the withdrawal on the wrong port exactly when they are. `NPR` moved from 85.04 C to **70.58 C**, which is the correct drop for its flow. What `P4.1` owns is how much duty crosses when the script does *not* say (e-NTU, LMTD, a rated point); it does not own conservation. Blast radius is one sample: nothing else in the corpus wires a second side. |
| S-26b | — | [`32`](32-steady-state-newton.md), [`22`](../20-core-domain/22-component-model.md) | **Nothing bounded a promoted parameter, so one ran away as soon as its column could move** | `position` is a fraction and `head` is not negative; the solver knew neither. Invisible until `S-26a` was fixed -- a column that cannot move cannot run away -- and then immediately visible: `m2-cooling-loop`'s `3WV.position` walked to **5.465**, a fraction five times fully open, and `PU1.head` to **-0.49**, both reported as an answer. | `ResolvedParameter` gained `Minimum`/`Maximum`, and the component declares them because the component is the only thing that knows (`D-30`) -- the same reason `SystemLayout` reads the unit off that record. `EquationSystem.Project` clamps every promoted column into its range and needed **no new constructor parameter**: `_promoted` already carried the element and slot, so the bounds were already reachable. `NewtonSolver` projects after the step rather than constraining the step, which leaves the line search's arithmetic untouched. `FS3008` names the parameter that ran out of room, once per parameter rather than once per step. Measured: the position is now held at 1 and the head at 0.2, `m2-simple-loop` is unaffected at 5.2635, and the substation is unchanged. **The cooling loop still does not converge** -- it terminates `Singular` at 11 iterations instead of `Diverging` -- which is a separate defect rather than this one half-done, and is recorded as `S-30`. |
| S-26a | — | [`22`](../20-core-domain/22-component-model.md), [`36`](36-numerics-and-convergence.md) | **A clamped parameter's Jacobian column was dead at either bound, so a promoted position could never step back into range** | `ValveLaw.Opening` opened with `Math.Clamp(position, 0, 1)` and `NewtonSolver.Jacobian` builds columns by **forward** differences. At `position = 1` the perturbation clamped straight back to 1, every entry came out `0 - 0`, and `DenseLu` reported the system singular naming the valve; the same happened anywhere below 0. **A bound is not somewhere the iterate may not go -- it is somewhere the derivative stops existing**, and a step onto it was unrecoverable because the column that would step back off was the one that died. | The clamp is gone. **Equal-percentage needed no repair at all**: `R^(x-1)` is smooth, strictly positive and monotone on the whole real line, so deleting the clamp is the entire fix for the default characteristic and for every valve in the corpus, with values inside `[0, 1]` untouched to the last bit. Linear and quick-open continue linearly above 1 and stay floored at zero below it, because `phi(0) = 0` with `phi'(0) > 0` admits **no** non-negative C1 extension below zero -- a line through the origin with positive slope goes negative, and a negative phi is a negative Kv, which drives flow against its own pressure difference. A leakage floor was tried and reverted: it changed a `phi(0) = 0` that is deliberate and asserted, on characteristics nothing in the corpus uses. `m2-cooling-loop` went `Singular` -> `Diverging`, which is the column coming alive. **Revisited under `D-122` (2026-09-19):** the linear law's dead stop, left because nothing in the corpus was linear, became every three-way valve's the day the registry defaulted them linear; the three-way leg now keeps 2 % at its stop and continues through it (`ValveLaw.LegOpening`), the two-way law is unchanged, and the transformed-variable fix this entry proposed is still the right one for a two-way linear valve if one is ever sized. |
| S-14b | — | [`22`](../20-core-domain/22-component-model.md), [`23`](../20-core-domain/23-topology-and-graph.md) | **A coupled heat exchanger had no side-2 pressure relation, so the substation could not assemble a square system** | `Relations()` counts one pressure relation per non-node component *per branch it lies on*, so `HX1` -- crossed by both the primary and the secondary -- was credited with two while `HeatExchanger` declared one. `m2-substation` therefore counted 23 for 23 and assembled 22 rows: a circuit the checker called square and no solver could be handed. It had never once reached the Newton iteration. | `EquationCount` became `SecondarySideConnected ? 2 : 1`, and `ComponentFactory` tells the component whether the script wired `in2`/`out2` -- lowering is the only thing that knows (`D-63`). The *resistance* is separate from the *row*: side 2 resists only when a script states `flow2`, because no rule can choose it (a rule sees one branch and this component sits on two, the same limit as `C-49`), and an ideal side still carries `p_in2 = p_out2`. That is a statement where declaring nothing was a hole. The substation now assembles 23/23 and iterates. It terminates `NonFinite` after 9 iterations, which is a different defect and a much smaller one. `RowAllowance` -- the test helper that had been subtracting this exact row -- is deleted, exactly as its own remarks said it would be. |
| S-24 | 2026-09-04 | 2026-09-04 | — | [`23`](../20-core-domain/23-topology-and-graph.md), [`31`](31-solver-architecture.md) | **A free enthalpy level was counted as an unknown no layout allocates, so a square table assembled a row over** | `CountingTable.EnthalpyLevels` was added to `Unknowns` and matched by the constraint row a stated temperature contributes, and the two cancelled. Nothing allocates a level column, because the offset is a null direction of the node-enthalpy columns already counted — `SystemLayout`'s own test wrote the subtraction down, with a comment saying the shortfall was "in the energy block's rank rather than in its width". The comment was right and the arithmetic was not: `m2-simple-loop` read `Excess = 0` and assembled 13 rows against 12 columns, and Newton refused it as over-determined on a circuit `23`'s own check called square. | `D-75`. The level moves from a term added to `Unknowns` to a term subtracted from `Equations`, and `EquationLayout` drops one energy balance per level component exactly as it already drops one mass balance per closed one — the two are the same argument, since a branch's two ends contribute the same upwind enthalpy times the same flow with opposite signs and cancel through the smoothing band as well. `Excess` is unchanged for every script and `FS2211` still fires. The `EnthalpyLevels != 0` skip came out of `ASquareCircuitAssemblesAsManyRowsAsItHasColumns`, which had been excusing the one sample that was wrong. Third instance of one shape after `S-16` and `S-22`, and all three were found by holding an assembled artefact against the table rather than the table against itself. |
| S-28 | 2026-09-07 | 2026-09-07 | — | [`31`](31-solver-architecture.md), [`32`](32-steady-state-newton.md) | **The outer loop assembled and factored a system without consulting either verdict, so a malformed script threw `ArgumentException`** | `WellPosednessResult.CanSolve` existed and `RunAsync` never read it. `m1-syntax-reference` is 18 unknowns for 19 equations and says in its own header that it is not a solvable circuit; it reached `DenseLu.Factor`, which threw on a Jacobian that was not square. A pipeline stage throwing on user input, which the contract forbids outright -- and reachable from the API by any script a user is halfway through typing. | Two guards, because there are two ways to be non-square and `CanSolve` sees only one. `CanSolve` is checked before the system is built, which catches the counting table's own verdict and reports the first *error* diagnostic when there is one, so `m1-syntax-reference` now names `PU1, PU1__in, PU1__out` rather than a matrix order. Then `system.Rows != system.Columns` is checked on the built system, because **the counting table is a prediction and the assembly is the system**: `m2-substation` counts 23 for 23 and assembles 22, `S-14b`'s missing side-2 momentum row. That one now reads "assembly gave 22 equations for 23 unknowns, though counting predicted 23 for 23", which names the defect instead of hiding it. `EquationLayoutTests` had been skipping that sample with the gap in its skip message all along. |
| S-25 | 2026-09-04 | 2026-09-04 | — | [`32`](32-steady-state-newton.md) | **A uniform seed field is singular in every variable that multiplies a difference, not only in flow** | `S-21` established that a zero-flow seed kills `∂(R·ṁ|ṁ|)/∂ṁ`. Two more of the same shape turned up the moment promoted parameters became real columns, and neither is about flow. **Pressure:** a valve's law is `ṁ = Kv·f(x)·√(Δp·ρ)`, so a uniform pressure field makes its derivative in `Kv` *and* in `position` exactly zero — the cooling loop's promoted `3WV.position` was measured as a column of zeros against a system whose every other column was O(1). **Enthalpy:** a node's energy balance carries `ṁ(h_arriving − h_own)`, so a uniform enthalpy field makes its derivative in *flow* zero, and the simple loop came out rank 11 of 12 with the null direction running along `PU1.head` and the branch flow together. Both were argued away before they were measured: "pressure enters linearly so Newton reaches the field in one step" is true of the pressure block and says nothing about the columns that multiply `√Δp`. | `SolutionSeed` steps pressure and temperature along each branch, wrapped into a band of five so a long branch cannot walk out of the fluid's validated range. The property is stated once, in the seed's own terms: **the seed must make every difference a residual reads non-zero, not merely every value.** |
| S-22 | 2026-09-04 | 2026-09-04 | — | [`31`](31-solver-architecture.md), [`23`](../20-core-domain/23-topology-and-graph.md) | **A stated boundary flow was counted as needing no unknown, and then entered no equation either** | `WellPosedness` leaves a node with a stated `flow` out of `FluxNodes` because it declares no unknown — correct for the count, and its comment says so in as many words. `EquationSystem` then assembled its flux terms from that same list, so the stated value reached no mass balance and no energy balance. The count stayed square throughout, which is why nothing caught it: `m4-storage-header`, whose *every* boundary is a stated flow, had a circuit at rest as an exact solution and would have reported convergence for it. The same shape as `S-16` — an error that cancels inside one number. | Found while writing the seed, because a seed whose whole claim is mass consistency has to know which terms are in the balance. The flux list now carries a column *or* a constant: a `FluxNodes` entry reads its unknown, and a stated flow contributes its own signed magnitude (negative for a `return`, `D-64`) to both balances. `AtRestEveryBalanceIsZeroExceptWhereAComponentAddsEnergy` was documenting the defect and now exempts exactly those nodes, with `AStatedBoundaryFlowReachesTheBalanceItNames` asserting the value it steps over. |
| S-21 | 2026-09-04 | 2026-09-04 | — | [`32`](32-steady-state-newton.md), [`24`](../20-core-domain/24-auto-sizing.md) | **Newton had no seed to start from, and the obvious hand-made ones were singular rather than merely poor** | `32`'s initial-guess table says the first solve starts from sizing, and sizing was `P3.7`, so `P3.6b` had nothing to call. Both stand-ins produced a **singular Jacobian**: zero flow kills `∂(R·ṁ|ṁ|)/∂ṁ` outright, and one sign for every branch leaves some node with every port an inflow, whose own enthalpy then enters no equation. Measured on the cooling loop, `N2.h` came back with a maximum Jacobian entry of `1e-6` against `1` everywhere else. | `SolutionSeed` builds a field that **satisfies every mass balance outright**, which is the property the two hacks were approximating. The branch graph is spanned by a forest; each chord takes its own flow estimate, each boundary flux is chosen so a hydraulic component's fluxes sum to zero, and the tree branches are then solved leaves-inward, each one being whatever closes its vertex. The last vertex closes identically, which is the construction's whole claim and is asserted on all seven samples. The `S-21` note that the seed must be *roughly* mass-consistent turned out to understate it: making it **exactly** so is easier than approximating it, because the cycle basis `23` already computes is precisely the freedom available. `NewtonSolverTests` dropped its alternating-sign helper for the real seed. |
| S-16 | 2026-09-04 | 2026-09-04 | — | [`31`](31-solver-architecture.md), [`22`](../20-core-domain/22-component-model.md) | **`DeclareUnknowns` was on the component interface and consumed by nothing, so a control volume's own state was modelled nowhere** | Exactly one kind used it: a tank declares one enthalpy unknown and one energy balance, `EnergyBalances` is `Nodes.Length`, and there was no term for a component-owned scalar — so the table counted neither. They cancelled, `Excess` read zero, and `m4-storage-header` reported square while being a row and a column short. Found by holding the assembled row count against the table rather than the table against itself. | Recorded as deferrable and then forced: `Tank.EvaluateResiduals` reads `context.Unknowns[EnthalpyIndex]` at its first line, so the first attempt to evaluate the storage header threw `IndexOutOfRangeException` — a pipeline stage failing on a shipped sample, which is the one thing none of them may do. `D-74` settles it. `CountingTable` gains `ComponentUnknowns` (named, because the layout must allocate them) and `ControlVolumeBalances`; `SystemLayout` allocates them straight after the node enthalpies so the energy block stays contiguous; `EquationSystem` slices them out of the iterate, which costs nothing because they are adjacent by construction. `RowAllowance.ControlVolumeRows` is deleted — and it failed the moment the fix landed, exactly as its own remarks promised. |
| S-2 | 2026-09-03 | 2026-09-04 | — | [`31`](31-solver-architecture.md), [`32`](32-steady-state-newton.md) | **The iteration loop had to pre-evaluate every port state and neither document said so** | A residual may not call the property backend at all: one water property is ~63 µs and a seven-property state ~204 µs, and a residual runs N+1 times per Newton iteration, so a 20-unknown circuit with ten components fixing one state each is ~43 ms per iteration — past `07`'s whole interactive budget before any linear algebra. `P3.3` closed the component half by putting evaluated properties in `SolveContext.Ports`; what stayed open was that nothing filled them. | `EquationSystem` fills them, once per iterate, in the one method in the solve that touches the backend. The Jacobian goes further: `TryEvaluateScaledAt` re-fixes only the node a perturbed unknown can change, and a flow, a flux, a promoted parameter or a control volume's own enthalpy changes none at all — so most columns cost no property call whatever. On a 200-component model the naive full refresh per column is ~N² property calls where N of them changed anything, which is roughly a second against roughly fifty milliseconds per iteration. Built in rather than retrofitted, because the shape of the saving decides the shape of the cache. |
| S-3 | 2026-09-03 | 2026-09-04 | — | [`36`](36-numerics-and-convergence.md) | **Residual scaling was specified and unimplementable where it was asserted** | `22`'s invariant 5 asks every residual to be comparable across component kinds, and a component evaluated alone has nothing to be comparable to — so `P3.3` could not assert it and moved it here (`C-2`). It is the one `36` names as costing a week of "the solver does not converge" when skipped. | `ResidualScales` is `36`'s `DF` diagonal, built from the unit already on each row for the diagnostic's sake — the same fact used twice rather than a coincidence. `NewtonSolver` solves scaled and never solves the raw system. Power is derived rather than tabulated: an energy balance is `Σ ṁh`, so its scale is a flow scale times an enthalpy scale, both already in the table, and a fourth constant would be a fourth thing to keep consistent with three that determine it. |
| S-7 | 2026-09-03 | 2026-09-04 | — | [`31`](31-solver-architecture.md) | **The residual-row-to-component mapping was built and consumed by nothing** | `31` makes the point that the mapping exists only at the component layer — "HX1 energy balance off by 4.2 kW" is actionable and "residual[17] = 4200" is not — and that if that layer does not carry it, no later one can. `P3.3` implemented `DeclareEquations` and nothing read it. | `EquationLayout` assigns every row its index and owner, `ComponentRows.Row` scatters a component's residuals into them, and `SolveResult.WorstResiduals` reports by component and equation with both the raw miss and its unit. Every `FS30xx` message names a component rather than a row index, which is what the mapping was for. |
| S-9 | 2026-09-03 | 2026-09-04 | — | [`31`](31-solver-architecture.md) | **Nothing consumed the counting table, so its terms were asserted against a document rather than against an assembly** | The table said the cooling loop has 20 unknowns and 20 equations, checked against `23`'s hand-tabulated table. When the real system is assembled those are the same two numbers computed a second way — and if they disagree, one is wrong with nothing to say which. | Both layouts are built to consume it and hold their totals against it on every sample. It has earned the argument four times over: `S-11` (an exchanger and its node both claiming one relation), `S-14a` (a valve writing a law for an unconnected port), `S-15` (a row the table counted and could not locate), and `S-16` (a term missing from *both* sides, which cancelled and hid). None of the four would have failed a test that checked the table against itself. |
| S-13 | 2026-09-04 | 2026-09-04 | — | [`36`](36-numerics-and-convergence.md) | **A mass balance belongs to a node while flow is scaled per branch, and nothing said which scale a node's row takes** | The residual is a sum of the flows incident on the node, so its magnitude is set by the largest of them; scaling by the smallest would inflate a residual that can never get that small and the row would never converge. It matters exactly where per-branch scaling was introduced to help — a node joining a 10 kg/s primary to a 0.05 kg/s bypass — so the two rules interact on the one topology they were both written for. | `ResidualScales` takes the largest incident branch scale and `36` now carries the rule as a keyed row rather than leaving it to be rediscovered. |
| S-15 | 2026-09-04 | 2026-09-04 | — | [`31`](31-solver-architecture.md), [`23`](../20-core-domain/23-topology-and-graph.md) | **The counting table counted the ideal zero-drop links and could not say which nodes they joined** | `PressureRelations` aggregates three things, and two of them are some component's own equation reaching the assembler through `DeclareEquations`. The third is not: `D-25` makes a bare `A - B` between two nodes an ideal link, there is nothing in the branch's path to declare `p_A = p_B`, and the assembler has to write that row itself. A count cannot say between which nodes, so the equation layout would have had to walk the branches a second time — the drift the table's own `FluxNodes` note warns about, one term further down. | `WellPosedness.Relations` now collects the pairs while it counts them, and `CountingTable.IdealLinks` names them beside `FluxNodes`, `PressureNodes`, `DatumComponents` and `LevelComponents`. `EquationLayout` writes one row per link from that array, and `EquationRowReconciliationTests` deleted its own walk in favour of `counting.IdealLinks.Length` — which is the point: the second implementation existed, in a test, and naming the links removed it. |
| S-14a | — | [`22`](../20-core-domain/22-component-model.md), [`23`](../20-core-domain/23-topology-and-graph.md) | **A three-way valve wired as a two-way still wrote a Kv law for the port nothing was connected to** | The registry has always made the bypass port optional --- spelt `c` then, `b` since `D-85` --- and the page has always said leaving it open is how a two-way valve is written; `ThreeWayValve` declared three equations regardless, and its residual read `Ports[2]` for a port with no node. `m2-distribution-header` and `m1-syntax-tour` hold two each, so both reported square while the system they would assemble was over-specified by two — invisible, because nothing assembled yet. Lowering now counts every component's connections and the qualified port names, and the factory hands the valve its wiring: with the bypass open it is a pass-through with two ports, one flow group, one Kv law and no mass balance of its own, and its mode reads `two_way`. The count is decided by topology and never by a solved value, exactly as an exchanger's mode is. `EquationRowReconciliationTests` now holds the declared pressure rows against `Relations()` on every sample, so the next kind that under-declares fails a test rather than a milestone. |
| S-11 | 2026-09-03 | 2026-09-04 | — | [`22`](../20-core-domain/22-component-model.md), [`23`](../20-core-domain/23-topology-and-graph.md), [`31`](31-solver-architecture.md) | **The rows the components declared and the rows the counting table counted disagreed, by exactly one per heat exchanger** | Found by `P3.6a` doing what `S-9` asks — comparing the two counts before writing an assembler that would have had to pick one. `WellPosedness.Relations` counts a crossed component once as a *pressure* relation and `EnergyBalances` is `Nodes.Length` unconditionally, so nothing counted the exchanger's duty row, and the excess equalled the number of non-node components declaring an energy row on every sample. Not a miscount but a contradiction about ownership. `D-69` settles it: energy is a flux a component contributes, the counting table is unchanged, and the excess disappears because the exchanger stops owning a row. Recorded from the component side as `C-40`. |
| S-6 | 2026-09-03 | 2026-09-03 | — | [`36`](36-numerics-and-convergence.md) | **The smoothing constants were `36`'s numbers living in tier 20's code, with no shared source** | `valve.dp_regularization` and `upwind.smoothing_band` had been transcribed by hand into two unrelated component files, and a change to the table reached neither. `P3.6a` made the table itself a type — `Solvers.Tolerances`, all twenty-five rows — and `ValveLaw.RegularizationDrop`, `Smoothing.UpwindBand` and `Quantity.DefaultRelativeTolerance` now read from it. The direction is the point: the table is the source and a component is one of its consumers, so a component reaches into `Core.Solvers` rather than the reverse. `ToleranceTableTests` parses `36`'s own markdown and asserts **both** directions — every constant carries its documented value, and every documented row is implemented — because the failure that happened was the second kind: a value transcribed correctly, and then the document moved on without it. |
| S-10 | 2026-09-03 | 2026-09-03 | — | [`23`](../20-core-domain/23-topology-and-graph.md), [`31`](31-solver-architecture.md) | **`CircuitGraph` did not carry the port-level adjacency the assembler needs** | Lowering computed `_peerElement`/`_peerPort` to decompose the branches at all, then discarded them. `Branch.Path` records the order a walk crossed the elements and not *which port* faced which — and a two-port pass-through walked from the other end is entered at its outlet, so the direction is not recoverable from the order. A component's residual reads `Ports[i]` as the state at the node port `i` touches, so no `SolveContext` can be built without it. `P3.6a` publishes it as `CircuitGraph.Adjacency`, and a test walks every branch through the table and reproduces `Decompose`'s own `Path` exactly. Recorded rather than done quietly because it is a tier-20 type changed from a tier-30 package. |
| S-8 | 2026-09-03 | 2026-09-03 | — | [`36`](36-numerics-and-convergence.md), [`23`](../20-core-domain/23-topology-and-graph.md) | **`FS2211` (under-specified) appeared to be unreachable from any script**, so half of the well-posedness count was untested against real input | It was reachable, and the reason nothing reached it was a missing row rather than a dead code path: the count had no **enthalpy datum** (`C-30`, `D-65`). A closed, steady circuit coupled to nothing has one enthalpy its own relations cannot determine, and a script that states no temperature anywhere in such a circuit is genuinely under-specified by exactly one. `P3.4c` counts it, and `Understated` now names a temperature ahead of a pressure — the one candidate the graph could not have picked for itself, since a datum covers the pressure case before it can reach this code. The original entry guessed at the two possibilities correctly and picked neither: the equation set *was* missing a row, and `FS2211` is not dead. |

## Observations

**A free boundary is still offered the fragment's largest estimate (`S-64`'s residue).** `D-115` closed `S-64` by removing the shape -- a boundary now has one connection -- not by changing the rule: `SolutionSeed.Field.Boundaries` offers every free boundary the largest magnitude and shifts them equally to close the fragment. A junction fed by two inlets could hand the closure to a rated exchanger again. Nothing in the tree reproduces it (re-checked 2026-09-24); a script that does reopens the row rather than filing a new one.

**The two counts had to be compared before either could be trusted, and comparing them is what found
`S-11`.** `S-9` asks the assembler to *consume* the counting table rather than re-derive it, and the
cheapest possible version of that — summing `DeclareEquations` over the graph and holding it against
`CountingTable.Equations` on every sample — cost one throwaway test and found a contradiction that had
survived four packages. Neither number was checkable alone: the table matched `23`'s hand-tabulated
worked example exactly, and the declarations matched `22`'s component list exactly. It is the
disagreement that carries the information, which is the whole argument for building the assembler
against the table instead of beside it.

**The counting table's own note said what to do, one term before it was needed.** `FluxNodes` carries a
paragraph explaining why it is *named* rather than counted — "an assembler has to declare these unknowns
and a count cannot say which nodes get one" — and `S-15` is the identical argument one row down, on the
equation side, found only when an assembler actually tried to write the row. Four terms had already been
converted from counts to named arrays for exactly this reason; the fifth was missed because nothing had
yet needed it. The lesson is cheap to state and was not: **a table meant to be consumed should name every
term whose consumer cannot re-derive it**, and the test of that is not whether a count is enough for the
diagnostic, but whether the assembler can write the row from it.

**A count that is short on both sides is worse than a count that is wrong.** `S-16` is the tank
declaring an unknown and a row the table models on neither side. `Excess` reads zero, `FS2210` and
`FS2211` stay quiet, and every reconciliation that compares two totals agrees — because both totals are
short by one. The reconciliation this session added is what caught it, and only because it holds the
*assembled row count* against the table rather than holding the table against itself: an error that
cancels inside one number is invisible until something outside that number depends on it.

**A defect found this way names both sides and neither.** `S-11` was recorded against `22`, `23` and
`31` together, because the exchanger's row and the node's row are each defensible in isolation and only
the pair is wrong. A finding filed against one document would have been fixed there, and the fix would
have been the direction-dependent one that does not survive a flow reversal.


**The nodal formulation's consequences are now structural, not just stated.** `23` argues that loop
closure is a property of the unknowns rather than an equation, and `P3.4a` built the cycle basis as
data with no equation attached: `CircuitLoop` is read by layout and by `FS2214`, and by nothing in the
system. The failure mode `23` warns about — 21 equations against 20 unknowns on this tree's own
reference circuit — is now not reachable by accident, because there is no code path from a loop to a
row.

**Branch-owned flow is implemented, and the unknown count follows from it.** A branch with three pipes
and a valve in series is one flow unknown and four pressure relations, not four flows and three
identities. `23` calls this the most consequential structural decision in tier 20 for solver
performance; it is now a fact about the graph rather than an intention, and `32` assembles against it.

**Node ordering is part of the contract, not an implementation detail.** `23`'s invariant 6 ties the
renderer's placement memory *and* the solver's variable ordering to lowering's deterministic order, and
`P3.4a` asserts it by lowering the same model twice and comparing a canonical rendering. A solver that
re-sorts variables for its own reasons — by degree, for fill-in — breaks the renderer, which is not an
obvious coupling from inside tier 30.

**The energy block spans what the pressure block does not.** A rated exchanger produces more than one
hydraulic connected component in one graph, and the energy system runs over every node in the model.
`23` states the consequence for tier 30 plainly and it is worth repeating where a solver author will
read it: the hydraulic blocks are genuinely independent and could be factorised separately, the energy
block is not, and a segregated solver that split by circuit would iterate against a stale duty. Any
block-decomposition experiment in `36` has to preserve that coupling.

**A residual that returns `NaN` at zero flow poisons the whole Newton step, and the laminar branch is
where it comes from.** `f = 64/Re` diverges as velocity goes to zero while the term it multiplies goes
to zero with it — literally `∞ × 0`. Substituting Re gives `32·μ·L·v/D²`, which is linear in velocity,
exactly zero at rest and has a finite derivative there. `36`'s "regularised where the physics is not"
covers this in spirit; it does not name the case, and zero flow is the initial guess.

**Every residual function in `P3.3` is allocation-free and was asserted so in the package that wrote
them.** `08` said to write that test with the components rather than retrofit it across six types, and
that was right for a reason worth keeping: the first version of the test measured 21 600 bytes, all of
it the test's own collection expressions inside the measured region. Retrofitted later, that number
would have been read as a real allocation in the component.

**The energy block's rank deficiency is tier 30's to respect, and no synthetic row fixes it.** A
closed, steady, uncoupled circuit's energy equations determine every enthalpy *difference* and no
enthalpy, so the assembled Jacobian is singular by one unless the script states a temperature
(`D-65`). `32`'s singular-handling list now names it beside the missing pressure datum, and the two
are not symmetric in what the solver may do about it: a pressure datum can be invented because every
pressure is relative, and a temperature cannot be, because every property call reads the absolute
value. An assembler that "helpfully" pins an enthalpy would return a plausible answer to a question
the user never asked.

**A worked example is a regression test for the case it was written from.** `23`'s counting table
reproduced term for term on the first run, 20 = 20 with both promotions, which read as strong evidence
that the counting pass was right. It was evidence about one open circuit. Both of the count's real
defects — the stated-`flow` flux (`C-26`) and the missing enthalpy level (`C-30`) — are invisible on
that circuit and were found by running the whole sample corpus and by a user asking for a *different*
circuit to be fixed. The sweep over every sample, recorded as a dictionary of outcomes rather than as
a list of exceptions, is what makes the next one visible.

**Well-posedness runs on the graph alone, and that is what makes it testable.** Nothing in the pass
reaches back into the semantic model; it reads `IComponent.StatedParameters`, which lowering now fills
from the bound symbol. So a solver test can build a graph by hand, ask whether it is square, and get
the same answer the pipeline would — and `23`'s invariant 7 stays true without an exemption.

**Stated-ness is the whole input, and it was being thrown away.** `IComponent.StatedParameters`,
`SizedParameters` and `DefaultParameters` have existed since `P3.3` and were empty on every component
lowering built. Nothing downstream can recover the distinction from the value: a stated `position=1`
and the registry's own default are the same number and mean opposite things to the count. Filling them
was the precondition for promotion existing at all, and it is `D-02` made observable rather than a
convenience.

**A dropped component leaves a branch ending nowhere, and the cycle basis threw on it.** A pipe whose
bore no catalogue resolves is not built and its connections go with it, so `Decompose`'s "the branch
ends where the graph does" path produces an end that is not a vertex — and `CycleBasis` indexed it
directly. `KeyNotFoundException` on a script that is merely incomplete, which no stage may do. Found by
a test written to reach `FS2211`, which is the second time this package a test written for one reason
found something else.

**A deliberately self-deleting allowance worked, and it is worth copying.** `RowAllowance` existed to
subtract the one row `S-14b` left undeclared, and its own remarks said why it was computed from the
graph rather than listed by sample name: "it goes to zero the day the equation lands, and every test
that subtracts it starts failing until this file is removed. That failure is the reminder." That is
exactly what happened -- the side-2 row landed, three reconciliations went from passing to failing in
the same build, and the fix was to delete the file. A hard-coded skip list would have gone on excusing
a defect that no longer existed. Worth doing again wherever a test has to tolerate a known gap.

**The substation's failure mode moved from structural to numerical, which is progress that looks like
none.** Before: "assembly gave 22 equations for 23 unknowns" -- the circuit could not be handed to a
solver in any form. After: 23/23, nine Newton iterations, `NonFinite`. The sample still does not solve
and the corpus still reports it as failing, so a summary counting converged samples sees no change.
What changed is that the remaining problem is now the same *kind* of problem as `m2-cooling-loop`'s
and `m2-distribution-header`'s, rather than a hole in the component model.

**A more correct seed made a failure happen sooner, and that was the useful part.** Seeding promoted
parameters from their components rather than from zero changed `m2-cooling-loop` from `Singular` after
three iterations to `Singular` at iteration zero. By outcome that is no better -- the sample failed
before and fails now. By diagnosis it is much better: the old failure looked like step control losing
its way after a plausible start, and the new one says plainly that the column is dead at the seed and
no step was ever possible. The temptation with a change like this is to judge it by the pass count,
where it scores nothing.

**The seed's own remarks named the defect and nobody read them as a defect.** The line was "a promoted
parameter carries no unit yet and falls to zero, which is honest and is the thing the outer loop
replaces when promotion becomes live." Promotion went live in `P3.7b` and nothing came back to this.
A placeholder that documents itself accurately is still a placeholder, and the sentence that would
have caught it is the one that says *when* it stops being acceptable -- which this one had. Worth a
convention: a comment naming the package that obsoletes it should be greppable, because it is the
package that lands, not the file that changes.

**One symptom, two defects, and the second was invisible until the first was fixed.** `S-26` was
recorded as "a promoted parameter has no bounds" on the evidence of a position reaching 180. That was
half the story. The other half -- a clamp plus forward differences making the column identically zero
-- was the reason the solve stopped *before* the bound could matter, and it made the recorded fix
inapplicable: a box constraint clamps the iterate onto a bound, which is precisely where the
derivative did not exist. Fixing them in the wrong order would have looked like the fix not working.
Worth remembering the shape: when a defect's recorded remedy would land the system exactly at the
place the symptom is measured, suspect a second defect underneath.

**`m2-distribution-header` is still `Singular` at iteration zero, and it is not this defect.** All
three of its pumps report a promoted head of **0**, because a pump built with nothing stated genuinely
holds a shut-off head of zero and `Resolvable` reports it honestly. No head means no pressure
difference, and a valve's `sqrt(dp)` law has a derivative in `position` that vanishes at `dp = 0` --
the seed's own remarks describe exactly this failure (`S-25`), one component over. So the header needs
a pump whose seeded head is non-zero, which is a different fix from either half of `S-26` and is
recorded here so the next person does not read it as the same one.

**A bound that binds is a diagnostic, not a repair, and the two look the same from outside.** `FS3008`
now reports `3WV.position` held at 1 on the cooling loop, which reads as "this circuit is asking for
more than the valve can give". That would be a good answer if it were true, and `01`'s own figures say
it is not -- the duty is reachable. So the projection turned a wrong *number* into a plausible
*explanation*, which is a subtler failure than the one it replaced. Worth stating as a rule for the
rest of the bounds work: a parameter pinned at a bound on a circuit whose answer is known to exist is
a solver defect wearing a user-facing warning, and the warning should not be trusted as evidence that
the model is over-specified until the circuit is known to converge without it.

**The data a fix needs is often already plumbed.** `EquationSystem.Project` needed the bounds of every
promoted column and took no new constructor parameter to get them: `_promoted` already carried
`(element, slot, column)` because the assembler has to write parameter values back before evaluating,
and `slot` indexes the very record the bounds now live on. The instinct on reading `S-26`'s recorded
fix was that this would mean threading bounds through a 17-parameter constructor. It was three lines.
Worth checking what a structure already carries before widening it.

**"Side 2 is not modelled" was three defects, and two of them were conservation bugs wearing a
deferral.** `S-14b` (no momentum row), `S-31` (no energy withdrawal) and `S-32` (no implied flow) all
lived under one deferral to `P4.1`, and each carried a code comment saying the coupled model would
supply it. Two of the three were not features at all: a component two branches cross **must** declare
two pressure relations or nothing can assemble it, and a component that moves heat between two streams
**must** take it out of one, or it invents energy. Only the third -- how much duty crosses when the
script does not say -- is genuinely the coupled model. Worth the general form: a deferral that covers
a conservation law is mis-scoped, and the tell is that the deferred behaviour is not optional in any
model, however simple.

**The substation is not an M2a demo, and thinking it was distorted the priority.** `05`'s exit
criteria name three scripts -- the cooling loop, the simple loop and the distribution header. The
substation is the coupled-thermal sample and belongs to M2b. It was worth the two conservation fixes,
which were real defects reachable from any script that wires a second side, but the remaining work on
it is `P4.1`'s and should not be pulled forward again. **The M2a blockers are `S-30` (the cooling
loop) and the distribution header's zero-head seed**, and neither has anything to do with exchangers.

**A correlation across five samples looked like a cause and was not.** Every sample containing a
three-way valve failed and every sample without one converged, which is as clean as a corpus that size
can be, and both remaining M2a blockers had one. It was worth about twenty minutes to test rather than
act on: the cooling loop is full rank at its seed, and the header's null direction names three pumps
and no valve. The distribution header is also the only multi-circuit script, so the three-way valve was
confounded with circuit attachment from the start -- five samples cannot separate two features that
always appear together. **The corpus is too small for correlation to mean anything**, and the null
vector of a singular Jacobian is a direct measurement that costs one elimination.

**Counting squareness and having full rank are different properties, and only the first is checked.**
`WellPosedness` counts unknowns against equations; nothing checks that the equations are independent.
On `m2-distribution-header` the two disagree in *both* directions -- square by count and deficient by
rank as shipped, over-specified by count and still deficient by rank once a head is stated. A counting
pass cannot see a linear dependence among rows, so a script can pass every pre-solve check and hand the
solver a matrix with no inverse. `S-28`'s guard was built for the case where the two counts differ; this
is the case where they agree and the matrix is still singular, which is the harder half.

**The instrument is now in the repository.** `CorpusStatusTests` pins every sample's termination,
including the failing ones, so no session has to re-measure the corpus before it can prioritise -- and
so a fix that changes a sample's state has to say so in its own diff. Three times this session a stale
claim about which sample was blocked on what was carried forward and acted on before being checked.

**The null direction is a measurement, and it belongs in the product rather than in a probe.** Every
hypothesis rejected while diagnosing `S-33` cost a throwaway test file, a build and a run; the
direction that settled it is four lines of back-substitution the solver can do for itself at the moment
it gives up. Two of the three rejected hypotheses would have been rejected instantly by reading
`FS3009` instead. The general form: when a diagnosis requires instrumenting the solver, the
instrumentation is usually the missing diagnostic.

**Four hypotheses died on this one sample, and each looked better than the last.** The three-way valve
(spurious correlation), the missing recirculation leg wired naively (made it worse), too many free pump
heads (every arrangement is deficient), and the promotion pairing being non-local (it is already local,
and forcing it changed no sample). The pattern in the misses is worth more than any one of them: each
time, a plausible mechanism was inferred from a *partial* measurement -- a correlation, a null vector, a
count -- and the next measurement killed it. What finally held was the variant sweep, because it changed
one thing at a time and every arm came back with the same answer.

**A change that alters no observable behaviour is not a safe change, it is an unjustified one.**
Restricting flow constraints to branch-local pumps is defensible on physical grounds and matches what
`Promote`'s own remark claims. It was still reverted: measured against the whole corpus it changed no
sample's promotions and moved `m1-syntax-tour`'s excess from 4 to 5. A refactor with no demonstrated
defect behind it and one behaviour change against it is a liability, however good the argument for it
reads -- the same call as the `MinimumOpening` leakage floor earlier in this phase.

**The counting pass refuses every arrangement that might work.** Removing the plant pump, and adding a
datum on the far side of the cut, are both refused as over-specified before the solver sees them. So the
two models do not merely disagree on the shipped script (`S-33`): counting actively blocks the search for
an arrangement that satisfies both. Whatever fixes this has to move counting, and no experiment on the
sample can proceed until it does.

**Substituting the answer is the cheapest way to find out whether the seed is the problem.** The cooling
loop had resisted diagnosis through `S-26a`, `S-26b` and `S-30`; five hand-set enthalpies settled it in one
run, and the numbers that came back were `01`'s to three significant figures. It is worth reaching for
earlier: a solver that reaches the right answer from a hand-built start and not from its own has a seed
defect, and no amount of staring at the iteration trace says so as directly.

**Two fixes were built and both were reverted, which is the process working rather than failing.**
`CorpusStatusTests` caught each one within a single run -- the first turned the cooling loop from
`Singular` to `NonFinite`, the second moved two samples' failures earlier while fixing a third. Neither
would have been visible from the sample that improved. The instrument added on the same day it was needed
paid for itself twice.

**A better constant cannot fix a wrong shape.** Both attempts kept `datum - NominalRise x steps` and tried
to improve the `datum`. The measurement that ends that line of attack is `3WV__P1` at 0.20 &deg;C from a
mean of 25.3: the walk's own magnitude carries a node out of range regardless of where it starts. Anchored
propagation is the shape; choosing a global level better is not.

**A rated `in`/`out` is a design condition, not a boundary, and that distinction decides where it may
be used.** The script saying an exchanger is rated 20/50 does not assert that the nodes at its ports
are at 20 and 50: in `m2-cooling-loop` those temperatures are *produced* by the mixing valve
recirculating hot return water, and how far the fluid actually rises depends on the flow the pump
delivers. Using them to place node temperatures is nevertheless right, because **a seed is not a
claim** -- Newton moves off it freely, so a design point cannot make an unreachable design look
reachable. The line to hold is that this informs the seed and never a constraint row, which the
counting pass owns. Getting that backwards in either direction is a real defect: as a constraint it
would assert something the script did not say, and refusing it as a seed throws away the only prior
anyone has about where the circuit is meant to sit.

**Path order is not orientation, and assuming it is laid the rated temperatures on backwards.** The
first implementation read a component's neighbours out of `branch.Path`, which is the order the walk
crossed the branch rather than the component's own direction. Measured, that put 46 &deg;C on the node
before `HE1` and 18 &deg;C on the node after it -- a seed asserting the exchanger cooled the water, and
one that still *looked* plausible because both numbers were in range. The port map has no such
ambiguity: a parameter is named after the port it describes. `ARatedInletLandsOnTheNodeAtTheInletPort`
pins it.

**Three attempts, and only the third was a fix.** Placing the ports alone made the sample worse
(`NonFinite` at 1). Placing them and averaging the global datum made one sample better and two worse.
Propagating from anchors fixed it. The two failures were not wasted: each eliminated a shape, and the
second is what proved a better *constant* could never work, because `3WV__P1` reached 0.20 &deg;C from
a mean of 25.3 -- the walk's own magnitude carries a node out of range regardless of where it starts.

**A fixture can hide a defect by being smaller than a real installation.** `m2-cooling-loop`'s bypass leg
is an empty connection, which is not what anyone builds -- the guidance for this arrangement is unanimous
that the bypass carries a balancing resistance matched to the path it bypasses, or the circuit
short-circuits (`C-63`). The empty leg was not merely unrealistic: it was the one shape in which the seed
worked. Every attempt to add the missing component made the sample *worse*, and it took measuring a plain
`pipe` in that leg -- which cannot be a sizing, authority or Kv problem -- to show that the component was
never the cause. **Two variants that fail identically are worth more than one that fails**, because
whatever they share is the mechanism and whatever they do not share is eliminated.

**The seed's own documentation stated the property it did not have.** `Steps` said in as many words that
"what this exists to guarantee is that adjacent nodes differ", and `Band` said its wrap gives "every pair
of adjacent nodes different values, which is the only property the seed needs from this". Both were
written against `S-25`, both are the right requirement, and neither was true -- because "adjacent" was
read as "consecutive along a branch" when a junction element makes nodes on *different* branches adjacent
too. A doc comment asserting an invariant is not a test of it, and this one had been believed for three
defects running.

**The column null direction answers a question nobody asked.** `FS3009` says which unknowns move
together, which reads like a cause and is not one: in a square system deficient by one there is a
left null vector and a right one, and only the left says which equation is redundant. The right one is
whatever combination of columns the elimination's pivoting happens to leave over, and on the header it
picked pumps every time --- so `S-33` tested a pump arrangement, then `S-34` tested three more, and all
four were deficient by exactly one because the deficiency was never about pumps. The measurement that
ended it took the transpose and cost forty lines in a probe. **A diagnostic that names participants
without naming the equation invites exactly this**, and it did.

**Five things eliminated is worth more than one thing suspected.** The header defect now has a minimal
reproduction and five ruled-out causes, each by a measurement rather than an argument: not the seed
(identical direction at six valve positions), not the enthalpy datum, not the missing resistance, not
the second subcircuit, not the closure. None of that was available while the search was following
`FS3009` toward the pumps.

**Counting is right about the unpumped branch, and it is worth saying so.** Two of the four pump
arrangements measured were *refused* rather than solved, and both refusals are correct engineering.
A consumer with **no pump of its own** --- flow regulated only by its three-way valve, which is a real
convention --- comes back `FS2210`, over-specified by one per unpumped branch: "remove `HE_RAD.out`".
That is the tool saying that without a pump the branch's flow is set by the network rather than by the
branch, so the coil cannot be asked to hold *both* its inlet and its outlet temperature. It is the same
degree of freedom the pump would have supplied. After several entries here about counting disagreeing
with the Jacobian, it is worth recording a case where counting says something true and useful that the
rank test could not.

**Three pumps were never the problem, and a fourth would be.** `S-33` and `S-34` measured four pump
arrangements on the shipped header and found each deficient by exactly one, which read as evidence
against the pumps. It was not: the corrected circuit carries the **same three pumps** and is full rank,
because what it gained was the *source's own loop* (`F-21`) rather than a different pump count. But
adding a **fourth** pump in series, which is what a header booster is, does create a real deficiency
(`S-37`). Both facts are about pumps and they point opposite ways, which is why neither could be
settled by counting them.

**The bootstrap `kv` makes every unsized three-way valve look like a different defect.** A valve no rule
has sized wears `kv = 630`, and a Kv law at 630 demands about **78 kg/s** on a plant that moves 0.93 ---
so its residual is two orders of magnitude above every other equation in the system, and whatever the
circuit's real problem is, the solver never gets near it. Five header arrangements were measured before
this was noticed, all reporting norms around 95 and worst residuals that were *always* the three valve
laws. Once `kv` is stated by hand the norm falls to about 1.2 and the actual defect (`S-38`) becomes
visible underneath. **The bootstrap value is documented as chosen "to disturb the first pass as little
as possible", and on a three-way valve it does the opposite**; `C-63`'s sizing pass was supposed to make
it unreachable, and it declines on every header (`C-66`).

**Two hypotheses died to one measurement each, and both looked stronger than the truth.** A promoted
pump head is seeded at 0, which is *also* its `Minimum`, so `PU_RAD.head` pins there and never leaves ---
`FS3008` says so out loud. That is a real property of the seed and it is not the header's problem:
forcing the head seed to 1, 3, 6 or 12 m changes the outcome not at all, and `m2-simple-loop` and
`m2-cooling-loop` are completely insensitive to it. Likewise a 128x sweep of the three valve `kv` values
converges nowhere. Both were plausible, both were cheap to test, and testing them cost less than the one
round of reasoning that would have argued for either.

**The row direction was right the second time, and the first reading cost a commit.** `S-38` was filed
leading with `FS3009` --- "the pump's head and the valve's position control the same thing" --- in an
entry that cites `S-36`, which exists in this repository *specifically* because the column null direction
names wherever partial pivoting landed rather than the cause. Reading the row direction instead named
mass balances at weight 1, which is the global conservation identity, which led to `S-39` in one step.
The discipline is not hard and it is not new; what makes it fail is that the column reading is always
plausible, because those unknowns really are coupled --- they are simply not *the* coupling.

**Two deficiencies in one circuit look exactly like one deficiency, and each hides the other's evidence.**
The header carried `S-39` (a datum on a junction) and `S-38` (two consumers sharing a differential) at
once. Every measurement taken before they were separated reported one combined direction, and the
combined direction pointed at neither. What separated them was deleting half the plant: the
single-consumer fixture isolates `S-39`, and fixing `S-39` there leaves the rank intact, which is what
proves `S-38` needs two. **Shrinking the fixture was worth more than any diagnostic**, and it should have
come before the jitter sweep rather than after it.

**A jitter sweep is the cheapest way to tell a seed defect from a structural one, and it should be
routine.** `S-25` and `S-35` were both seeds producing singular Jacobians, so the prior was reasonable ---
and wrong here. Evaluating the same Jacobian at 1 %, 5 %, 20 % and 50 % random perturbation of the seed
takes about fifteen lines against `TryEvaluateResiduals` and answers the question outright: a deficiency
that survives 50 % jitter is in the equations, not in where they were evaluated.

**An argument that reaches a correct conclusion from a false premise still has to be fixed.** `S-12`
was the clearest case: per-branch flow scaling was right, implemented, and shipped, and the reason `36`
gave for it was arithmetic that this project's own tolerance table contradicts by three orders. Nothing
was broken and no test could fail. What it costs is the next person to touch scaling, who checks the
number, finds it wrong, and now doubts the design it was defending.

**`S-5` is the same failure one step earlier: a specification that every implementer will satisfy
incorrectly on the first try.** "Continuous in value and first derivative" is a correct requirement and
an untestable instruction --- the obvious test measures the derivative's sweep across the band rather
than the property at the join, and fails on correct code. A document that states a property whose
natural test is wrong should carry the working test, not just the property.

**Closing `S-36` needed `S-39` to exist, and that is the normal shape rather than an exception.** The
entry measured the row null direction, named it exactly (*one mass balance against every energy
balance*), eliminated five causes, and could not say what the fix was. It stayed open through two more
sessions until the datum-on-a-junction rule was found. **An entry that has isolated a defect precisely
and cannot name its cause is doing its job**, and closing it early to shorten the list would have thrown
away the measurement that eventually identified `S-39`.

**One rule, three implementations, and the build is green after changing any one of them.** `D-86`'s
flux rule lives in `HydraulicComponent.HasUnknownFlux` (which decides the mass-balance drop),
`WellPosedness.Count` (which decides the flux unknown) and `SolutionSeed.Free` (which decides where the
seed may push flux). `SolutionSeed`'s copy carries a comment saying it is "restated rather than shared"
with "a test holding the two lists against each other" --- and the inconsistency still got through,
because the guard compares the seed against `WellPosedness` and neither against `HydraulicComponent`.
Each partial change produced a *plausible* failure a long way from the edit: an equation-short counting
table advising a pressure on a mid-branch node, then a seed 0.167 kg/s out of balance at one node of one
sample. **Neither failure named the rule that had been changed.**

**A change proposed as "one clause" touched three predicates, four test fixtures and two samples.** That
gap between estimate and reality is worth recording on its own. The estimate came from reading the
predicate; the reality came from running the suite, which found every site in three iterations. Nothing
about the design was wrong --- `D-86` is still the right decision --- but "one clause" was a claim about
code shape made without looking for duplicates of it.

**`S-40` closed without its own fix, and the cause recorded against it was wrong.** The entry guessed
that `NullDirection.Of`'s significance cut was calibrated for column scales and mis-applied to rows. It
was not: the instrument was being handed a matrix built from a mis-modelled circuit, and it started
working the moment the circuit was modelled correctly. **A guessed cause in an open entry is a liability**
--- the next reader would have spent the afternoon in the elimination.

**`S-38`'s fix was validated by substitution before it was built, and the build still failed.** Stating
both pump heads and dropping both `out=` statements is what removing the constraint/promotion pair does
by hand, and it took the header from `Singular` to full rank --- strong evidence for the design, gathered
in one run. The implementation then did not reproduce it: `Promote` deferred the constraint and `Count`
received a list without it, and `CountingTable.Constraints.Length` came back unchanged anyway. **A
validated design and a working change are different things**, and the gap here is in the counting
table's accounting rather than in the physics. Worth reading `Count` end to end before trying again,
rather than editing `Promote` a third time.

**Twelve throwaway probes were a tooling gap, not twelve investigations.** Every question asked of the
header between `C-66` and `S-38` --- what is the counting table, which promotion answers which
constraint, what does the seed hold, which rows are dependent, what did the sizing rules choose ---
was answered by writing a test, printing something, reading it and deleting it. `SolveExplanation`
answers all of them in one call, and on its first run reported something none of the probes had: the
header's row null direction is dominated by **energy** balances at weight 1 while the counting table
says `less enthalpy levels 0`. That is `S-39`'s story on the energy side --- a redundancy nothing
removed --- and it is the lead `S-38` should be attacked from. **Build the instrument at the second
probe, not the twelfth.**

**The report's own first bug was a confident wrong verdict.** It printed "NOTHING --- this is what
reports the circuit over-specified" for any constraint no promotion answered. `m2-simple-loop` has
exactly that and counts square: its `in=50` is paid for by the enthalpy level the closed loop drops,
not by an unknown it adds. A diagnostic that states a conclusion the counting table contradicts is
worse than one that states nothing, so the section now prints the arithmetic --- unanswered
constraints against levels dropped --- and leaves the verdict to the count.

**The report was built as a function and every caller re-wrote the same three lines.** `Render` had two
overloads --- one for a finished run, one for a graph --- and picking between them was left to whoever
was calling, so every probe repeated the same `run.IsSuccess ? ... : ...` branch, and the interesting
case is the one where that branch goes the *wrong* way: a circuit refused before the solver is exactly
the one whose report is worth reading. The fix is smaller than the habit it breaks --- one overload
that takes the run and the graph, and `ToString()` on `OuterLoopResult` so anything holding a result
can print one, a debugger watch window included. **A diagnostic that each caller has to assemble is a
diagnostic that gets assembled differently each time**, and the format has to be identical across
models or two reports cannot be read side by side.

`EverySolutionCarriesTheSameReport` now enumerates `samples/` rather than listing it, and asserts all
eight sections on every script. A list covers the samples that existed when it was written; the
guarantee wanted here is over the directory.

**A zero-duty consumer's three-way valve passes water from `a` to `b` through its own body** (seen 2026-09-20 while re-reading `s56-ahu-off` for `S-70`'s direction label: `TV_AHU.a -> N3` 0.0062 kg/s reversed, `N5 -> NM_AHU` 0.0062 reversed, `TV_AHU.b` out of). The coil branch through `ab` is pinned at zero (`S-56`), and mass balance at the valve's internal node then allows `a`-flow = −`b`-flow: the header's differential pressure drives 6 g/s from the supply header through the valve chamber into the return header, bypassing the coil. A real rotary or plug mixing valve with its common port dead-ended does exactly this -- both legs open into one chamber -- so the number is physically defensible and nobody asked for it. It is not a defect until a script cares; recorded because the old direction label hid it as *reversed* and a reader would otherwise have taken 6 g/s of backwards coil flow for the solver's error.
